Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 14 - Room Database, DAO, @Entity - Wishlist App
Lecture: Day 14 - Quick Overview Of Storage Options - Reading the book
----------------------------------------

A welcome back. In this video we're going to look over. A little bit of the book. So I'm going to read through a portion of the book. Chapter 11.5 Saving Data locally on Android. So this book was written by Loveth Nwokike and me, and we've been working on this book for quite some time. And yeah, basically I'm going to read a little bit from it. Also, the room database example that we have here is also from the book. So the wish list application and let's go through it. This will give you a little bit of an idea how to save data locally, or the reasons why room database is good. So saving data locally on Android stores data on the device itself, such as in a file or database. This can help improve the performance and reduce the need to access external data sources. Additionally, it can help make an application more secure by preventing sensitive data from being exposed over the internet. There are several ways to save data locally on an Android device. Below are the listed options and their explanation. Shared preferences is a simple key value storage system that allows you to save small amounts of primitive data such as strings, int, and booleans in a file on the device. You can use shared preferences to store user preferences, application settings, or any other small pieces of data you need to persist. Then there is internal storage is a file system specific to your app and accessible only by your app. You can use internal storage to save private files to your app, which should not be accessible to other apps or to other users. Then there is the external storage, which refers to storage that is not internal to the device, such as an SD card, a USB storage. You can use external storage to save files intended to be shared with other apps or users. And then there's SQLite and room. These are options you can use to save structured data. With a chance for manipulation. We will be learning about them in detail shortly. So now which of those options should you choose? Well, it depends on the specific needs of your app. For example, if you need to store small amount of data that do not require a structured database, you may use shared preferences. On the other hand, if you need to store structured data and perform complex queries, you may choose to use SQL or room. So why aren't we using SQL? Well, let's talk about SQL real quick here. SQL is a lightweight embedded SQL database engine that is well suited for storing and retrieving data in Android apps. You can use SQL Lite to store structured data in a database and perform queries on the data over time. It was found to be complex, requiring long lines of code for its actualization. So that's where room came in. Room was created to reduce this complexity. It's an Android library that makes it easier to work with SQL databases. So basically it's like a layer on top of SQL databases or SQL Lite databases. It provides an abstract layer over SQLite, allowing you to interact with the database using data access objects called doors. So we're going to set up a Dao in the next few videos so you can focus on building the app's features. Room introduced some key features that facilitate the integration of local database into your application. They are listed below. So it's the entity class which we'll need to set up. Well, actually we already have an entity class. In our case it's called the. Wait a second. How did we call it. We just didn't call it data class or entity class. So it's our wish okay. So we need the entity class. Then we need the Dao which is the data access object. We need basically to set up those Crud operations that I was talking about. So it's an abstract class. In this case it was a person right. So we have the insert. We have the well basically create search or read update and delete. So this query here is another way to search. So this is searching a user by its id. This one is searching a user while all users in a given database table. All right. So we're going to set all of that good stuff up in the next few videos. And then we can basically use that using a database okay. Now you see there is this class people database that inherits from room database using the person Dao. And the person Dao is in turn using well accessing these entity items such as the primary key of the person and the last. For example. Because you see, whenever you add a user, you're passing a person object. Whenever you update a user, you're also passing a person object and so forth. Then there's also something called flow. We're going to look at that as well. But this just gives you a general overview. And if you want to read the book, you can find a link to the book with a discount in the bonus lecture, which is at the very bottom of this course. At the very end, so to speak. So you see, one thing that we have done already, which was to add this plugin, Kotlin Kapt. And now you might. Well, you have been wondering maybe why we added this plugin. Well, now you know we need it for our room database. And then we had this room database implementations as well or dependencies as well. Now you know where or why we want to use those. So performing Crud operations let's go ahead and actually do that with our project. And I would say let's start with it in the next video.