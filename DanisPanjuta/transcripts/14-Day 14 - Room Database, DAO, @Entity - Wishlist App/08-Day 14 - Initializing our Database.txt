Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 14 - Room Database, DAO, @Entity - Wishlist App
Lecture: Day 14 - Initializing our Database
----------------------------------------

All right. So in this video I want to summarize all the setup that is required whenever you want to use the room database to store your data permanently. And that is you have to have a data class that is going to be the information that you want to store. So in our case the data class is very simple. It is our wish which has three. Columns inside that table. Number one is the ID. Number two is the title and three is the description. Similarly to what we saw here. Chap. Well, column one, column two and column three. Okay, so you need to assign one primary key, which usually is the ID. And you need to make sure that it's automatically generated for you, which means it will be automatically incremented. Otherwise you have to take care of that manually. In our case we won't need to take care of that because, well, we set it to be auto generated. Then you set up your wish Dao where you basically say, what are the Crud operations and how should they work for your particular data class? In our case, the data class is wish. So we call this thing wish Dao. We say how a method should be called when we want to insert data and what we want to insert. And in our case, it's going to be our wishes because that's the data class that we want to insert, which is why we needed to make sure that the entity keyword is added to them. This entity annotation here. And then we can set up our different operations, the Crud operations. So the easiest one are the create, update and delete because you don't need to add a query. However, if you want to read data, then you use the query keyword in which you actually use something. That is SQL code you could call. So this is SQL text, where you define what you want to read from the database. And in our case here we're reading the entire wish table. And here we're reading the entire wish table. But then we're filtering it by the ID. And you can also filter it by, for example, a name by well in our case the title or the description or whatever. But we are always just going to filter by ID, so that's what the wish Dao takes care of. Now the wish Dao is ready. We need to set up the wish repository, which allows us to well with the wish Dao to take care of those functions and make sure that they are happening in the right approach. For example, suspend functions. Right? So here we're basically bridging the gap between the the Dao and our view model. Because our view model then can use this wish repository. So that's where we come back to our view model. Our view model needs to have a wish repository which you then can use to call those functions. With the right scope. So with a Co routine. So here these functions run on co routines. Therefore we need to set up the ones that are for example. Well if we want to get all wishes we want to load the data that needs to happen in parallel as well. So once we set that up we are using the identifier. Then we need to initialize it. And we're using our repository for that as well. So now at that point our ViewModel has all the data available whenever it needs it, because it just can say, hey, I'm just going to talk to the repository and it's going to talk to the wish Dao. And that will know what the structure of the data is. And we'll do the actual execution on the room database. Now this brings us back to our Wish database. So our wish database is basically saying what the database should be looking like. So what are the different entities that we have inside of our database? In our case we just have one entity, but there could be multiple entities. So if you have a table for your users, then you have a table for the comments that the users wrote, then a table for whatever other information you want to store permanently. You would set that up inside of your entities. So we then use and we'll create this room database class. So this wish database, which is of type room database in which we just set up our Dao. Now that by itself is not doing anything. As you see they're grayed out. And that's where we come with our graph. So we need to set up an object. And I'm going to put that on the higher layer here. Kotlin file class object which I'm going to call graph. So now this graph needs to initialize the database. So the database is going to be of type wish database. And it needs to set up our wish repository using the by lazy here. And I'm going to explain what this bi lazy is in a second. So here we're using our wish repository and we're passing in the wish Dao which is going to be database dot. Wish Dao. So. Now we're setting up this wish repository, which we have not used before. Right. So now our wish repository is used. Our wish database is used. And now we can basically use our database throughout our project. So now let's quickly look at what this bi lazy is. So bi lazy, just make sure that this variable is only initialized once. It is going to be needed. So the idea behind this is that you don't load everything at the start when you open up your application, but only the moment. Well, this particular repository will only be loaded the moment that it's actually going to be required, which means once we actually want to access it. So that's when it's going to be loaded, not straight at the start, which could delay how the application responds. And that's just an efficient way to make sure that your app performance is great. So the by lazy initialization is thread safe by default, which means that the repository is guaranteed to be initialized only once and even when the when accessed by multiple threads simultaneously. You can also opt for a non thread safe version if you are sure the property will only be accessed from a single thread, which can be slightly more performant, but in our case it just makes it significantly easier by just using this by lazy, because then it's efficient and it doesn't break our like we don't have to think about it too much after that. So that is part one of our graph. Now let's look at the second part. All right. So now this graph needs to provide. Therefore I'm going to create a provide function where we need to pass in the context. So when you want to build your database you need to know which context you want to build that. So we have this database right. And. The provide function will now initialize that database because we're trying to use it here, but it's not initialized at this moment. So we need to initialize it. And we're saying room dot database builder. And this database builder is going to need a few parameters. So it needs the context. It needs the class and the name of the database. So the context is just going to be the context that we pass to the function provide. Then we need to say which. Class we want to use for our. Database. So we're going to use the wish database class dot Java. And then we need to define the database name. And I'm going to call this one wish list dot db. So this will now be the name of our database inside of our phone. Now at this point we can build it using the build keyword. So this will build our database. The moment we call the provide function. So I'd say let's leave it at that. For this video I will explain what's going on under the hood in the next video explaining what dependency injection is.