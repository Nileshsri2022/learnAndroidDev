Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 14 - Room Database, DAO, @Entity - Wishlist App
Lecture: Day 14 - The Room WishDatabase Class
----------------------------------------

Welcome back. In this video we're going to create the room database. So we will create a database class. This must be an abstract class. Extend the room database and be annotated with the database keyword. Here we will also define the table the DAOs and the database version. Here is what the code will look like. So we create this at database. We'll use this add database annotation. And then inside of brackets we need to say which entities we want to use. In our case it's just going to be the wish class then the version of our database. Because if you ever change the amount of columns, for example, that you want to have in your database, then you would need to update your database and change the version of your new database. And then the export schema, which we're going to set to false at this stage. So then. We need to set up the class which is abstract class, which database, which is of type room database. And you can see here we have this abstract function which gives us access to the wish Dao. So let's go ahead and create that new item or new class here. So this class will also be an abstract class. So here I'm going to create a new class which I'm going to call Wish database which inherits from Room Database and needs the annotation add database now as well. And now we need to define which kind of details we want to have here. As I said, the entities which we now need to set up as an array of items. So in this case it's just going to be our wish class. So here how do you use that is you use the wish keyword class name and then colon colon and then the class keyword. That's our entity. Then we have our version. And the version in our case will be one because it's the first database that we're creating. And the export schema which I'm going to set to false. So now we just need to make sure that this is an actually abstract class because otherwise it's not going to work. So now our room database we need to add those brackets here. Now our room database is ready to go so that we can add our function which sets up our Dao. So which sets up our wish Dao like so. And that is already it for our wish database. So now we obviously want to use our Wish database and access it so that we can use this wish star, which then gives us access to the different methods in here, which means the wishing, the addition of wishes as well as getting wishes, updating wishes and so forth. And I would say, let's keep going with this. Or like set up the wish repository, which will then allow us to separate our database execution stuff from the from the presentation layer. So let's set it up in the next video.