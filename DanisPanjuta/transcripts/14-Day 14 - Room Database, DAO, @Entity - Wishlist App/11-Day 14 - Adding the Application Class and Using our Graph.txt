Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 14 - Room Database, DAO, @Entity - Wishlist App
Lecture: Day 14 - Adding the Application Class and Using our Graph
----------------------------------------

Welcome back. So now that we have our graph ready we can set up our application class. So the database initializer needs to be called from a global context. And our application class will have that context. And in Android we have the application class that is useful for initializing any global state that needs to be available throughout the app. This class is instantiated before any other class. When the process of an application is created. To use the application class, you are required to subclass it by creating a new class. Extending it so here. Wish list app. Extending the application class. Below is what our application class should look like. So it's very simple. As you see it just has this application keyword. And then it's an Oncreate function where we provide the graph okay. So let's set up that class so that it can actually use this graph. And then use to provide function with it. So I'm going to create a new Java file. Well, not Java new Kotlin file class. And this one will be called Wish List app. So this will inherit from application which then requires for it to have the Oncreate method. So here override fun Oncreate. And here we can use our graph dot provide this. So this. Is the context. So we're basically saying this wish list app is the context in which you should do the whole database stuff. Okay, so basically the location like our lemonade stand has a certain location. So this is this application is the location where all of the database should be set up. So now our graph should you see it's not grayed out anymore. It's actually being used. So now we just need to do one more thing, and that is to ensure the project is aware of the application class. Now it must be registered within the Android manifest using the Android name attribute. This must be done within the application tag to avoid a crash from happening whenever the app tries to create a database. So. Below is a figure how this is going to look like. It's a little blurry, but let's go over and actually take care of that ourselves. Let's go over to our Android manifest and here inside of application we are going to use Android Colon name. And then we add Dot wishlist app to it like so. Okay, so now at this point, nothing much has changed in our application. We have our application ready, but we still don't display any of the data yet. And therefore we're going to prepare the navigation. Class here. This one too. Well, this function is composable to also take in parameters. We've seen how to use parameters with navigation. So here in the composable where we would add that to the route. Right. But we're going to see that in the next video. See you there.