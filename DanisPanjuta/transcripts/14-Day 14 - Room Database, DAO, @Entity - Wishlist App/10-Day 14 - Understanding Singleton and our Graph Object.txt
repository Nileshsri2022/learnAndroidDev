Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 14 - Room Database, DAO, @Entity - Wishlist App
Lecture: Day 14 - Understanding Singleton and our Graph Object
----------------------------------------

Okay, so there is a lot going on in this file here, and I want to talk over it a little more because otherwise it's really confusing. And I really struggled to find a proper way to explain it. But I believe I have something good now. So let's look at it. So we have this object graph, which is a simple example of a dependency injection setup in Kotlin. So object graph here declares a singleton named graph. In Kotlin, the object keyword is used to declare a singleton. A singleton is a class of which only one instance will exist in the application. So that means we cannot create another graph object that would give a different name or whatever. There is only one graph throughout our entire application. So this graph object is being used as a service locator that will provide the dependencies to the rest of the app. So the dependencies which repository and basically the database. So then we have this late init var database. Here late in. It is used to declare a Non-nullable property database of type which database that is promised to be initialized before being accessed. So the late init keyword is useful when an instance needs to be initialized through dependency injection, or a setup method that is not a constructor. In our case, we're not using a constructor. It cannot be used with primitive types, but in our case we are not using a primitive type. We're using a data wish database which is of type well room database which is a class. Okay. So then. We have the vault repository or wish repository by lazy. This lazy keyword is a making sure that this is a lazy initialized property that creates an instance of wish repository when it's first accessed, so only when it's first accessed the lazy delegate. This keyword here means that the initialization code inside of those curly brackets will only be executed once the first time wish repository is accessed. So once we actually need wish repository only then this code will be executed, otherwise it just won't be initialized. This store saves us a lot of resources. So then we have this fun provide. So this is a method to provide the necessary context for initializing the Wish database. It takes an Android context object as a parameter. Inside this method, the room database builder is used to create an instance of our wish database. And this instance is then assigned to the database property, this one here that we set up. So here. With is built. This is a part of the Android Room Persistence library, which is an abstraction layer on top of SQLite. It allows for more robust database access while harnessing the full power of SQLite. So this build method here that we call this finalizes the configuration and creates the wish database instance. So this graph object here is effectively acting as a simple dependency container, where it initializes and provides instances of the database and repository needed by the app. This pattern is handy for managing dependencies in a centralized way, avoiding tight coupling between components, and making the code more testable and maintainable. So now this is the explanation for someone who understands all of those keywords that I used. But now let's make it a little simpler. And if you understood everything until this point, you can just go to the next video. Otherwise, watch until the end. Because now I'm going to go over a simplified version of what's going on. So. Imagine you're setting up a lemonade stand, which is your app. Before you can start selling lemonade, you need a few things set up so late init var database. Which database? This is like knowing you need a cash register store and manage your earnings, but you haven't gotten it on your stand yet. You're sure you'll have one? It's just not there right this second. So then while repository by lazy, this is like having a recipe book that only shows up on your stand the first time you decide to make a new batch of lemonade. The recipe book. So this wish repository magically appears. Once it does, it stays on your stand, ready for use. Anytime you need to mix up some more lemonade, then we have this fun provide. Now this is like telling your lemonade stand where it's located, at the street fair or in the neighborhood. This is the context you're giving it the specific spot so it knows where it should be set up. Then this piece here is like setting up your cash register. So the wish database, the wish list is like the label you put on the register. So you know, it's for keeping track of all the lemonade wishes or orders from your customers. So in app terms, the graph. It's like you're behind the scenes set up team. It makes sure your lemonade stand, your app has everything it needs, like a cash register and a recipe book. And if you need anything else, it will take care of that as well. Ready to go whenever you open for business, it's all about preparing what's necessary to run your stance smoothly so you can focus on making and selling great lemonade. Building a fantastic app in our case. So that was a summary of what's going on here. Now we can use this graph in a separate class that we will set up in the next video.