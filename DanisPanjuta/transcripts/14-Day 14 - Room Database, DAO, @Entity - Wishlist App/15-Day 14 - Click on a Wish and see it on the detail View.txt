Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 14 - Room Database, DAO, @Entity - Wishlist App
Lecture: Day 14 - Click on a Wish and see it on the detail View
----------------------------------------

Welcome back. In this video we're going to add the feature so that we can basically click on a given item or a given wish and then go to the edit screen which will then load the right item from the database. And then we can edit that item whenever we make a change. So therefore I am inside of the home view and the home view once we click on a item. So on a wish item here, we need to make sure that that wish item is aware of which ID it's supposed to go to. So which ID the wish item should load, and I'm going to set the ID to be wish ID, because this wish contains the ID that we're looking at or for. And then I'm going to use the nav controller to navigate to that particular item. So here this navigate will require our screen dot. And it should navigate to the add screen and to its route. And here I'm saying that I would like to pass the ID as well. So we can do that by using this approach here where we're basically saying okay I want to pass this ID to the screen to this add screen. Route. Okay, now that is how we are adding it. But how do we retrieve it? Well, we saw how to retrieve it earlier on, but there are multiple ways to do that. And the one that we're going to look at here is a little different, because it's a little more complicated where we are going to enable us to make it more complicated. So basically we have this add screen, right? And currently by default it just uses the ID of zero. So whenever we click on the add screen we're just creating a new entry. Well always going to the new entry field. Never actually loading the the item at the details screen. That's what we want to achieve right here on this screen. We want to actually load the data and then allow us to update the wish. Once we click on Add Wish so to speak. Now going back to the navigation, how do we read the ID of our add screen route. Well. We can add that here directly, where we say we also take the ID into consideration. And so inside of this composable. And then we can read the arguments. So that will actually be here. Come on arguments. And now this argument is going to be a list of. Nev arguments. So there's this thing called Nev argument, which in my case is the ID. And then I can say watch the argument builder should do. So here I'm saying okay the type should be nav type dot long type. So the information that I'm passing over is of type long. Then the default value is going to be zero as a long. And it should not be nullable. So nullable is going to be set to false. Now this is the well these are the arguments that we can pass. Now we can pass a list of IDs. But in our case it's just going to be an individual ID that we're passing. So now we get this additional information that we can load. We can now load the ID we can check if entry. We're just as naef backstack entry, right? So we're getting that entry. And now we're checking the entry for arguments and here arguments. And we're checking if they're not null. So if they're not null then we're going to say and this should be in the separate line. If they're not null we will say entry dot arguments. We're going to unpack them. We're going to get the long value of ID from them. We already said that it cannot be nullable. However it would expect that. So get long you see, without the exclamation marks, without us unpacking this nullable, we would get an error directly in the compiler. So here we need to define what should happen in both cases. So if our entry arguments are not null, then take those entry arguments and get the long at the id or at the key of ID. So here we're saying we are creating arguments with the name of id where we are saying it's of type long. So basically here we are saying you can pass arguments to my route to my composable when creating it. And here at this point we are unpacking it. So getting the arguments and we're looking for a particular argument with the name of ID, and if we have that, then we're going to take it. If we don't have that, then we're going to set the ID to null or to zero L okay. Now this add edit detail view should now read the id as this ID instead of always being zero l. So it's going to be zero L if we click on the add button. If we don't, click on the add button, but we click on the edit button, then it will get an ID from form the entry. So basically saying the item that we want to add it and we just have taken care of that. For example in this home view we. Said here. I'm going to add the ID whenever I navigate to a particular wish item on that list. Now I need to go over to the Home View, and here I need to make sure where I navigate to the add screen that my route isn't empty. So basically here I need to make sure that I add an ID as well, otherwise our application will crash. So what I'm going to say here is plus and then zero after the slash okay. So this will now send us over to the add screen without crashing. Let's actually test that and see if our app will crash or not. So we don't have any entries anymore because I deleted all of them while testing. So here we're adding an item. Let's call this one and one add wish. So we have this item here. Now let's click on this wish. And you see it loads the wish for us. So that's because whenever we go on the screen let's actually add another entry just to see if this is actually the case. So this one will be 1212 add wish. Then 131123123. So now let's go over to this one. And you see actually didn't load the one. So it looked like it. And I was tricked for a moment as well. But it actually isn't loading. It's just maintaining the old state. So now we actually need to take care of loading the data from there. And that will be inside of the add edit detail screen here. Add add a detail view. We actually need to load the data. That we were sent. So basically the wish that was sent. Well that has the ID of where we clicked on and where would that be? Well, if we go up here, we have our. If zero string. So we had after we created the scaffold state up here, we need to check if the ID that we get free from here at the top, if that ID. Is not equal zero L. Then we want to load the wish. So we're going to create a wish object here which we get from the ViewModel. So ViewModel dot get wish by id with the id. And then we collect that as a state where we say initially initial. Wish well in the state. The initial state that we want to use is going to be wish with an ID of zero. L zero. L write the title of nothing and the description of nothing. So that's going to be the default wish. And we now, however, get the wish by its id, which will give us the proper wish which contains all of the additional details. And now we can use that to update the ViewModel state. So wish title state here with our wish dot value dot title. And the same goes for ViewModel dot. Wish description state which will be wish dot value dot. Description. And now, what should happen if we get zero? Well, then the view model state should just be an empty string. Or title state. And the same goes for the description as well. So basically that means we got there through creating a new entry. And otherwise we got there through an entry that already existed. And now if we run our application again, we will see that we actually get the view model to work and do its magic. So we click on any of the given items and you see it's loading the title and description. So here 123 you see title 123 and 123. So now we just need to add the update feature. So we need to update our wish. And that's what we're going to do in the next video.