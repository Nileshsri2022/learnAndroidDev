Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 14 - Room Database, DAO, @Entity - Wishlist App
Lecture: Day 14 - Setting up the @Entity for our Data Class Wish
----------------------------------------

Welcome back. In this video we're going to prepare our data class being or it becoming an entity so that we can actually use it with our Dao and with the room database. So it's quite simple. So you take the data class that you want as the object type that you want to store permanently on your device using the room database. In our case it was the data class wish. And you add the entity annotation to it which comes from Android Room. All right. Now you need to say in which table you want to store that information. So here I'm going to say table name. And then we need to add a string. So I'm going to call this one wish table. All right. So the database will now have a table which then contains data about our wishes. Now we will create wishes in every single wish will become a new row. And now we need to define the columns that we want to have inside of that database. Inside of that table. Okay. So you have the level of the database. In which you have tables, multiple tables. You can obviously also have just one table or no tables at all. Then the database is empty, but you can have one table and multiple tables. In our case we will have one table which will be our wish table. And then in that table you have columns which will be the different properties of your data class. And then each entry of your. Table will be a new row. So we now need to say which one of those will be the primary key. Which one of those items here will be the primary key of those properties? Okay. And the primary key is important to understand that it is the one identifier. That is unique, so unique to every single entry inside of that table. Which means that if you look at a table, you need to know by that identifier. If you search for it, you need to only find one entry inside of that table. So you could say, yeah, let's just use the title or the description. The problem is, if somebody enters the same title twice, then it will not be unique anymore. We will not know which one of those to read if we ever want to read and display certain wishes. Okay. Which is why the ID is so useful. The ID is the primary key in our case the. That's generally the case. You would always use an ID as a primary key because it's just a number and it increments automatically or is being incremented by the database basically automatically. Each time we create a new entry, we are increasing our ID by one, and by doing so we're making sure that no ID is used twice. Okay. So here let's use the add primary key annotation. And let's add this statement here. So Autogenerate true. This is what I was saying with it automatically incrementing it for us. We don't have to do that manually. And that happens because as of us adding this line here saying autogenerate equals true. So we start with zero. But the next time we create an entry of a wish inside of our table wish table, this number will increment by one. So it will go to one. The next item that we will generate will be to the next, after that will be three and so forth. And then even if we delete one, two, three, the next item that we will ever create will automatically be with or have the id four. Okay. So that's really important to understand here then. I'm adding another annotation here for my column title, and here I can add something called column info. You see it's also from Android X. So this column info is basically saying what is the name of the column. So how do you want the column to be named. And I'm going to call it Wish Title. Now we need to do the same thing with our description. And I'm going to call this one with wish desk. Desk standing for description. Okay. It's not the desk that I'm having my computer on for example, or sitting under. So now our table is pretty much ready, or our data class is pretty much ready to be used in a Dao to be used to store data permanently. Okay. So let's quickly look at this. Item here, this spreadsheet. So the spreadsheet if you compare that this would be the database okay. The sheet itself. This would be the table. Okay. So this is our table that we will give them. Or we would give the name or will give the name table name of which table. Okay. So this would be for example my wish table. This will be the database that I use for my app. So this could be rich database my wish app okay now we have columns. So this will be column number one which will be ID. This will be column number two which in our case was wish title. And this will be column number three which will be wish description. So now every single entry will go in here. So let's say I have a new entry. And the wish title was an iPhone for example. Even though not actually, let's not use iPhones. We're using Android here, right? And let's say a Honor Magic V2, which is an absolute beast of a smartphone and a smartphone or a foldable smartphone. That's absolutely insane. All right. So now we have the first entry. Now the next time we create a new entry it will automatically get the ID of two. So this one will be let's say a car. And then the description of the car. Brand new Speedy Gonzalez car. All right. Speedy Gonzalez is probably only for the boomers out there. We'll understand who Speedy Gonzalez is. Then the next one would be a house. All right. I wish for a house and big -- house host. Okay. Big -- house. Um. Quiet yet? Central area, something like that. Okay, now let's say I delete this entry here. So three will be deleted. So when I create a new entry this will still be number four. So it will still increment by one. Now this entry will be let's say e-bike all right. And super fast e-bike with big or long range okay. So this is just to give you an idea how all of this comes together. All right. How all of this is connected and, well, will basically be stored inside of the room database for our particular application. So each application gets its own database so to speak. Right. And it has a bunch of different tables. Or it can have a bunch of different tables that you want to store. So that's it for this video to give you a background. In the next one we're going to set up the Dao. So see you there.