Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Calculator App - Learning XML and how to create a UI - Android 12
Lecture: Building The Calculator UI using Linear Layouts part 2
----------------------------------------

Welcome back. And this video, we're going to finalize our user interface and therefore we can just do a bunch of copy and pasting, which is super useful. And that's why I wanted to use linear layouts because with a constraint layout it wouldn't be possible and what would be so much more work to get this same layout done. So what we can do is we can just add waiting here. So layout wait for example and set that to one for our linear layout here. And you see that it will now well its children will now take the entire space available because currently the weight is just one inside of what we're looking at. And here this is the linear layout, the outer linear layout, which is oriented vertically. So it's children are on top of each other because there is only one big child inside of the linear layout. It will basically take the entire height available. Now what we can do is we can just take this a linear layout now and duplicate it and duplicate it once again. So what we need to, of course, change now will be the entries of the buttons. So the first linear layout that we have will have the seven, eight and nine, and the second linear layout that we have will have a four. Which will accordingly have the value, the text value of four. So let's hard code more create this string for us. Then we will have a five here in the center and accordingly we will need to change the text to five. And if we really want it, we can go ahead and create a string resource for that as well. And this one will be six. So let's change that string to six as well. And extract the string resource. And then finally, this one will not be divide, but let's say multiply. For example. And this will allow us to, of course, have to change here this string. So I want it to be a star. So let's go ahead and create a resource name which will be multi ply. Okay. And now let's do that for the bottom row as well. So here I'll have the value of one being at the left hand side, which also means that the string should be changed to one. And we can. Extract the string. This one will be two. So here button to. And extract the string accordingly as well here. And finally, button three. And according to change the string here. And by finally, I mean finally four for the numbers. And also we still don't have the zero. So I guess we will need another line here, but that's fine for now. So here this one will be a button subtract. So here. Sub tract. And to text will be a minus, which we then accordingly have to hard code into minus. Like so. Okay. So now. You see, by the way, that these tools ignore using on click and HD HTML has been added to all of those and that's why it's ignoring the arrows. So now let's add another row down here. So you see this final row will now have to display this zero. So this one button zero, or you could call it null. For example, if you come from Great Britain and then here the text would be zero. And now let's create a string resource from it as well. So this would be the zero and now I will need less buttons. So let me get rid of this additional button. Here, let me add the here c l r button cl r will stand for clear and the text will just be c l r which I let create an extra string for me. And then finally we have the add button which will just have the text of plus like so, okay. So that by itself is cool and all. And here, let's call this one plus, but I would like to make sure that the zero button is going to be wider because it really looks off that we have four items in the row, but here we only have three items in the row really looks off. How can we achieve that? Well, as you might know, we are working with weights here. So this button zero has a weight of one. This button has a weight of one. So the CLR and this other button has a weight of one. And now let's actually get to the point where we will understand how weights work. So because they all have a weight of one, they are equally wide. So what could you do if you wanted for one of them to be twice as wide as the other ones? Well, you could just add the weight of two, so let's do that for our button zero. And that's it. So now button zero will be twice as wide as CLR and the plus button, which now makes this look a lot smoother. And yeah, that pretty much makes our UI look pretty clean, I'd say. And finally, we will just need another line here, which will be the equal button. So let me just copy this linear layout once again. Let me put that in there and get rid of all of the unnecessary buttons. And after I have added the equal button, which will have the string on or off and equal sign here, let's extract that into a separate resource here. Equal. And now we can get rid of all the other buttons that are not going to be necessary in there. So that's pretty much it. That's our UI. Now in our test application, you could see that the buttons were great. So how could we change that? Well, let's go ahead and go over to our themes, because that's where the color is defined. And you can see that this color that we have here looks pretty much like, I believe, this one color primary. So if we instead of using purple here, where to use this grayish color, then our entire application would look different. So let's go ahead and create a new color and call this one color. Name. Let's just call it gray this value here. So hash tag is 90a for a E and while this might not be exactly the same gray that we had in the other example, but it is a good gray, I'd say. So you can really decide which gray you want to use. Let's look at it. And this is going to be the greatest I'm going to use here. Okay. So that is basically our user interface here. Now, we could also use this gray color for our applications theme here, and that is this primary color variant. We could also just use Gray here because then if you run it again, you will see that this bar at the top there also now has this same color. So before that it was this purple color, but that's basically what the variant is used for in our particular theme here. Okay. So that's it for this video. And the next one, we are going to look at how to actually. Use the on click properly and we're going to implement the CLR functionality. So see you there.