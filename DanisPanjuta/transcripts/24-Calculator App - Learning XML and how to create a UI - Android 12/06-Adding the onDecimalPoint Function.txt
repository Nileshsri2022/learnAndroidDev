Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Calculator App - Learning XML and how to create a UI - Android 12
Lecture: Adding the onDecimalPoint Function
----------------------------------------

Welcome back. In this video, we're going to add a decimal point because, well, we could, of course, have this calculator, which can only work with whole numbers. But how about having a decimal point here at the bottom left, which then allows us to use some proper float calculations as well? So what I'm going to do is I'm going to use this button zero here and I'm going to add just another one before that. So this one will be button dot and then we can go ahead and give it a different text. I'm just going to use the point here or dot and let's add a string. Like so. Okay. So now accordingly they both have to have a weight of one now, otherwise it would look a little awkward. So here we have the zero. We have the dot. Now, we could, of course, change the order if you want to, but I think that's fine as we have them now. Now accordingly we will need to have a specific method that will take care of what should happen once we click on a decimal point. And I'm going to create a method here called on decimal. Point. So once we click on it, we will get a view as the parameters here and now. Once we click on it, I want to execute something. Now, the important part is that this should only work if the last entry that we had was not a dot, because if we already had a dot, I don't want to use another dot. So that's really important here. And in order to find that out, I will require a another variable or actually two variables here. So one of them will be whether the last one was numeric and the other one will be whether the last one was a dot. So here we are, the last numeric which will be of type boolean and I'm going to set that to false. And then the other one will be the last dot, which will be of type boolean as well. And we could of course just leave this colon boolean out here. It would work all the same, but that's basically how I want to start this thing because if it was a number that was pressed, I want to treat this dot differently, then if there was a dot that was pressed because if there was a dot, I don't want to be able to create another dot, otherwise it would have 3..0, for example. So that's what we want to avoid. And in order to avoid that, what we can do is we can use an if statement here that will help us with that. So here we can check if. The last numeric is true and it's not a dot, so not last dot. So here you see if less numeric is true and at the same time it's not the last dot, then I want to execute some code. And that is this exclamation mark here which makes sure that this is not last. So if last dot is not true, which means it's false, only then this code will be executed here in those curly brackets. So what do we want to do here? Well, then I just want to append a dot. That's it. That's as simple as that. And at the same time, if that happened, I need to make sure that the last numeric is going to be false, because otherwise it would never change. Right. The same goes for the last dot. That needs to be true now, because now the last entry that we had into our TV input was in fact a dot. So what we're using here are flags. So this is just a flag. Last numeric and last dot. They are flags which help us to know if something is currently active or inactive, so to speak. Okay. So now at this point, let's go ahead and use this on decimal points. I'm going to copy it from here. I'm going to go over and assign that to my button dot ID here. So to this button. And once it is pressed I will use the on decimal point here and it will execute it. And now that we have assigned the tier and if we were to test it, we would notice that it still doesn't work. Why would that happen? So let's see. This button just doesn't do anything. Is it something wrong with this method? Well, actually, no. The method itself is fine. You see, it's connected and it's running and all is good. But the problem is that last numeric is never set to true. And we are setting it to false here. And this means that this statement is always going to be false, so this code will never be executed. So we need to make sure that once we press on a digit that our last numeric will be set to true. And at the same time our last dot will also be set to to false because now it's not a dot anymore that we had entered last, but it was a number. So let's run it again. So 0.0. And you see now I can still go on and on and on to add zeros so I could add IPS here, but that is not the point for now. So that's fine for now. We're going to fix that later on. But at least we do have the capability of adding dots, but not multiple dots after another. So that's it for this video. See you in the next one.