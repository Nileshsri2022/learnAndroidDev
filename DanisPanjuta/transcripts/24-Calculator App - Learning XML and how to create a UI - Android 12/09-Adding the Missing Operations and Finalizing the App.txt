Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Calculator App - Learning XML and how to create a UI - Android 12
Lecture: Adding the Missing Operations and Finalizing the App
----------------------------------------

Welcome back. In this video, we're going to finalize our application and therefore, I'm just going to get started by, well, copying and pasting this code because this is basically where the calculation happens. Right. So we check, is it a minus? If it's a minus, then we're going to split it at the minus. And then we do the calculation here at the bottom. So now we just need to replace this or do the same thing with a bunch of else ifs for the other operations. So here, let me add else if and then else and paste it in. So now I have all four operations here. So let's start with the minus. We had that. Now let's go over to the plus. So let's add the plus here and add it in the calculation itself as well. Now let's add the division slash and do the same thing here. And then we just need another one, which will be the multiplication. So here we just need to add the multiplication like so. And of course we mustn't forget it here at the bottom in the actual multiplication as well. Okay, so now let's try this once again. So 99 times 99 gives 9801. Now 66 well, actually, let's use 66 times three will be 198. That's good. Now. 99 divided by 66 will be 1.5 and 66 minus. 33 will be 33 plus 55 is 88, -68 is 20 and so forth. So you see, that works fine. Now at this point you could say, Hey, I'm not super happy with their constantly being a dot zero at the end. So if we are working with just whole numbers, I want to get rid of this dot zero and you already know more or less how this can be done. You could use a substring, you could use a split here, so both would be fine. I'm just going to show you my solution to it, and that is going to be a new method that will take care of just that. So here I'm going to create this new private function called Remove Zero After. Dot. All right. And it needs to get the result of string and it will return a string itself. So the result will be the result of our calculation, and then it will take that and it will return a string without the dot zero. So first of all, let's get the value, which will be our result at the beginning. So when we start the value will be the result and we only change it if we actually need to change it. Which means if the result contains. The string of dots zero, which means 99.033 3.0 and so forth. If that's the case, then let me set the value to be the result of the substring of zero. Well, start index zero all the way up to result dot length minus two. So what is going on here? Well, let's just use this here and play the cutline. So we had this Helloworld example earlier and here I'm going to use this result length minus two. So what will this return? Let's run it. And of course, my string needs to be used here instead of result. So here is going to be a returned empty space war. So what it does is it starts at position zero, which is this empty space, and it goes all the way to the length of the entire string, which is 11. So we have 11 entries minus two. So it goes all the way to index nine, which means it goes all the way to this R and doesn't use this LD. Maybe it's going to be easier if we actually are going to use these as strings here, like the numbers of strings. You can see it starts at index five, which is this number here, and it goes all the way up to everything except for the last two entries. So this eight nine is going to be excluded. It's the same as if it were saying 90.0 and we start at substring index zero and go all the way to length minus two, which means we just get 90. So we are eliminating the dot zero here with this substring. And now of course we need to return value. So here and return value like so. And by the way, you see here I'm using this if clause without brackets because I just have one line of code that will be executed for this if statement if it's true. So now we return value, which means now we can go ahead and remove zero after dot for each of the calculation results, so to speak. So here where we set the text, we can now just use this remove after dot and pass the calculation in there. So the result, so the string of the result in there as the parameter. Okay, so remove after dot with this thing as a string. Okay. And now we can do the same thing with the other calculations. So here we do the same thing. We just need to make sure that we replace it with the right operation and here it has to be the plus. And then here accordingly, either minus. And. Was it the minus for this one? Yes, it was. Okay. So now let me check, because I believe I have this minus twice. Okay. That's good to know. So let's use it instead. Here. Which creates a situation where this would have been unnecessary. Okay, let's run it again. And 99, -66. And see, we have only 33 now, not 33 zero. All right. So that's pretty much it for this calculator. So you see, we have a proper calculator. We can clear the entry. Now, you could, of course, have zero as the default value that is going to be displayed here if you wanted to. But overall, this is a calculator that works at least for the basic operations. I hope you enjoyed this chapter in the next one. We're going to build an actually cool app because this was just to show you the fundamentals and really teach you how to use all of the good string stuff. And in the next chapter we're actually going to build a cool quiz app. So see you there.