Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Drawing App - How to use Canvas, import Images and Export them - Android 12
Lecture: Adding a provider - path and a image sandwitch maker to our app
----------------------------------------

The next thing I would like to add to the app is to store the drawing. So if I draw something here, then I want to be able to store it and therefore we need to allow the writing onto the device. And once we allow that permission was granted and then we click on it again and then we can see the file was saved on our phone and yeah, let's get started. As usual, the UI is the easiest point. So the first one where we actually see something. So I'm just going to add another image button just underneath this ID brush, which means next to the ID brush and I'm going to call this one, I be safe. And then of course, we also need to use another image here, so let's drag in another trouble. And it will be the ICI safe. So here I am importing it with copy pasting. And then we of course need to change the image here. So I see save. Let's just see if this works and if the UI is as we want it to be. So there we are. The button exists. So the button is there. Of course it doesn't do anything. So the first thing we need to do is to add the permission request, because now we also want to write onto the device and not just read. All right. And in order to write onto the device, we need to know where we want to write, so at which path we want to write. Therefore, I want to create a new path, and I'm going to create a whole new directory in the resources folder. So new and directory and I'm going to call this one XAML. So in here I'm going to create a new XML and I'm just going to call this one path. Or maybe even. Yeah, path should be fine. Okay. So. I'm going to call this path and it's not going to be a preference screen, but it's going to be some paths. So in here you can create your paths and this paths file, which is our path XML file. Maybe you could even call this one paths f xml because it could potentially have multiple paths in there. But we're just going to work with one and I'm going to work with an external path and it needs a name and I'm going to call this one captured and then it needs to know where it needs to store it. So usually you would go like Android slash, data slash and then the package name of your project. So it's this one here at the very top, this one in my case, EU dot tutorials, dot kid's drawing app. So I add that in here. And then slash files. So I'm going to create this entry as an external path here. And then, of course, we need to close that at one point. So this is the path that we prepared. The next step is to add a provider to our manifest file. So in here we already have this user's permission, right? External storage. That's good. But now we also need to add a provider and we do that just underneath the activity within the application. So still in the application, we create this provider. This provider needs authorities. And here I'm going to use the name again of my package, that file provider, and then you need to have a name as well. And I'm going to use Android X dot core dot content dot file provider. So that's going to be the name of the provider that I'm using. But we need a little more information inside of this provider. And actually we're going to do it like this because we need to pass in something in there as well. But the provider is not done yet. And by the way, if you want to know more about the provider, it's something that you can look up in the documentation, of course. So inside the app manifest file you can create a provider. It declares the content provider component and a content provider is a subclass of content provider that supplies structured access to data managed by the application. All content providers in your application must be defined in a provider element in the manifest file. Otherwise the system is unaware of them. It doesn't run them. So we need to set that up if we want to store anything on the device. So that's exactly what we're doing here. And then the next thing. I'm going to set export it to false. And what export it does is it indicates whether the content provider is available for other applications to use. So here you can basically say, okay, whatever I'm providing could be used by another application. If I set that to false, the provider is not available to other applications. All right. If you set it to true to provide is available to other applications. Any application can use the provider's content URI to access it subject to permission specified for the provider. So that's basically what you have to take into consideration. And then you also have to have this grand URI permissions. So whether or not those who ordinarily live would not have permission to access the content provider, data can be granted to do so temporarily. Overcoming the restrictions imposed by the read permission, write permission, permission and exported attributes through if permissions can be granted and to false. If not so, we're going to set that value to true in our case, because granting permission is a way of giving an application component one time access to data protected by a permission. Okay, so let's set that up. So export it is set to false. And then we had to grant URI permissions and I'm going to set down to true now in here I can create some metadata and the metadata that I need to give the provider is of course the resource and another one is the name. So and that metadata should have Android name. And here I'm going to use Android dot support dot file on the score provider. Underscore paths and the actual resource will be our XML path. So XML. Todd path. So it's going to be this passport XML file, which is in our path called XML. So within the resources in the folder in XML. And that's why I'm what I'm accessing here. So now that we have figured the path of stuff out and to provide stuff which is required in order to provide anything or in order to even store anything, the next thing we need to do. And by the way, of course, the authorities. So if you want to know more about the authorities, it's a list of one or more euro authorities that identify the data offered by the content provider. And typically, it's the name of the content provider subclass that implements the provider. As it states here, there is no default. At least one authority must be specified. So in our case, we say that the file provider of our drawing app should be the one being used. Now let's go back to coding and our main activity. Because now what I would like to have is to get a bitmap from our view, because if you look at our game or our app here, this whole area is a view. And now I would like to go ahead and make a bitmap out of it because bitmaps are images and bitmaps and images they can be stored, views themselves cannot be stored. So I need to convert it into a bitmap. In order to do so, I'm going to create a new function here and I'm going to do that a little further down here. And this function that I'm going to create is going to be a private fund. Get bitmap from view and we need to pass an A view because I want this to return a bitmap. Okay. So the idea is we get a view and we return a bitmap and we need to import bitmap here in order for this to work. And of course, we need to return a bitmap in the end. At the bitmap that I want to return is going to be returned bitmap. So at least I'm going to call it like that. Returned bitmap is going to be bitmap dot create bitmap. And here, just create a bitmap. If we look at it. There are so many different versions here and I want to use one where I pass in the width. So the width of the view view that width, then the height of the view view dot height and then the bitmap type. So the bitmap configuration. So here bitmap dot config dot hi are GB and 4/8. Okay. So let's put that in another line. So it's a little more readable. So I'm creating this return bitmap and I'm calling the return bitmap because I'm going to return it, all right? And it will take the whole width of the view, the whole height of the view, and it will take the configuration of a GBE eight, eight, eight and we saw that earlier which pretty much just is a type of. Defining how many colors we want to have and also defining which type of opacity we want to have. The next thing. Instead we want to bind the canvas that is on The View. So pretty much the thing that we drew. And in order to do so, I'm going to create another variable. I'm going to call this one canvas. It's going to be a canvas and it will take in the returned bitmap. So let's import canvas here as well. And then, of course, we also need to get the background. So overall, if we look at this image that we have here, we can have a background, right? For example, we can have our homer being in the background. So let's use Homer once again. So then Homer will be the background of the view. The view itself is going to be the thing that contains our canvas, and on the canvas is where we have all of our colors and the bitmap is going to be the combination of those three. So putting them all together and making one image out of it, it's like putting them into a sandwich maker and you have all of those layers and now you're making one sandwich out of it and you can eat it. So that's what we're going to do here. And therefore, we also need to know the background. So big trouble, which is view dot background. So whatever we assign as background, it can be that there is nothing, but it can be that it's the image that we just used Homer Simpson for. And if it's nothing so if it's nothing, we want to do nothing. But if it is something, then we want it to draw on the canvas. Otherwise I want to have white background. So the canvas should be draw color. White. So color dot white. And here we need to import color as well. Just like that. Okay, so now we draw something, no matter if we have a background image or not, we now draw a background and then we need to draw the view the canvas onto the view. So draw a canvas and finally we can return our return bitmap. So here, return, return bitmap. And by the way, by running this line, what we do is pretty much finalize the thing because what we prepared is our return pad map, right? Which is taking the view views width and the views height. And we then go ahead and we prepare the canvas a little more. We put in the return bid map into the canvas. We get the background. If there is a background, we draw that onto the canvas. If there is no background, we draw white onto the canvas and then we draw the canvas onto our view. And finally we return our return bitmap. All right. And at this point, we are almost done. What we need to now prepare is a bitmap async task. And this is a little bit of code. So I'd say let's make a break here and finalize that code in the next video. So see you there.