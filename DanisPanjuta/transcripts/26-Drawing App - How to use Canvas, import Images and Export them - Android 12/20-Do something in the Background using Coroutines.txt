Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Drawing App - How to use Canvas, import Images and Export them - Android 12
Lecture: Do something in the Background using Coroutines
----------------------------------------

In this video, you are going to learn how to use code routines. And why do we need code routines? Well, in Android. Some operations or tasks take a longer time to complete, and these tasks block the user interface. And until they are completed, the user interface is not interacted with. So the user cannot interact with it, or at least with parts of the application, and which will make the user idle and will make them bored and so forth. So it's not a good process, it's not a good approach, basically. So to avoid this Android UI system was built to only accept delays of maximum 5 seconds. So let's look at that. So I have this little application here and once I click on this button, there will be a very long lasting task running and let's just run it. You see, the UI is frozen. I cannot click on anything. The app is frozen basically. And after 5 seconds, Android decides, Hey, this is a bad application, it's not responding, let's close it, even though you see it's done. So even though the application did finish in the meantime, while this pop up appears so I can either close the app or I can wait and then go back to the app and it works, right? Otherwise the user would just close it because he believes the app is bad and he's going to uninstall it or she's going to uninstall it. So what is going on in this application that I've built here? So I have just this button and once I click on it, it just runs this full loop which runs 100,000 times and it only adds a log entry, nothing to fancy, and once it's done, it's going to write this toast. But I'm doing that in the on create and I'm doing that on the main thread. So on the user interface, Fred, which really is not good and we can avoid that by using code routines which will basically push tasks that last longer into the background, onto a different thread so that the UI thread will not be blocked and can still run and will still run flawlessly and fluidly so that the user can properly use it. So let's look at what core routines are and a little presentation and see the advantages and then build a little application around it. So let's look at code routines and scopes in a little more details. So to summarize the features of core routines, first of all, they are lightweight. This means that you can run many code routines on a single thread due to the support of suspension. Then you have fewer memory leaks. By running a core routine and a specific scope, you ensure that they do not leak as it terminates at the end of its scope. Then you have built in cancellation support. So the launch option returns a job which can be used to cancel the running code routine and it is integrated into jetpack. So many jetpack libraries include support for core routines, for example, room and the work manager. So basically if you want to develop for Android within 2021 and later, this is definitely going to be the way to go. Okay. So then let's look at coaching scopes. So there are built in scopes you can use to launch your core routines to finally execute this function. However, we need to echo routines, scope and coatings follow the principle of structured concurrency, as I said, which means that new routines can only be launched in a specific or routine scope. So if you create a scope and don't clear it at the end, you can run into memory leaks and you want to avoid that. So let's look at the view model scope. It can be used to launch a code regime in a view model class. It will automatically be cancelled once this view model is cleared. And then you have the lifecycle scope which is used to launch core routines in the activity or the fragment and is cancelled automatically when they are destroyed. So when the lifecycle is destroyed, which is good because then you are not running into memory leaks and you can as well create a custom scope. But it is important that you attach it to a job and clear it. When the execution is done to create one in your activity, you create a global job variable. You create a code routine scope and attach a job to it, and then you execute it. And finally you cancel the job when the activity is destroyed. So this is how you could potentially do it if you were to create your custom scope. Okay. So now let's look at this. First of all, instead of running this code in here, I'm going to create a little function that I'm going to then adjust. So I have this private. Fun. Execute, I'm going to call it and I'm going to pass a result into it, which will be of type string and it's just going to execute my for loop. So here I just want to run the execute method and I need to pass a string to it, which will be. For example, task. Here. Task executed successfully. All right. And this should, of course, be used here. So the result should be used as the text of the toast. So once I execute it now well, now, basically the same thing would happen, only that the toast message would change slightly. So that's not really helping us. So how can we fix the problem that we have? Well, in order to fix it, we are going to use the suspend keyword here. So suspend but that will do is it will make it a co routine function. So I suspend function. Now you see here it's unhappy. So you cannot just execute a suspend function on the main thread, which is really the whole idea here. So now to stop the function from blocking the UI, we need to add suspend in front of the function. But also we actually need to make sure that it runs on a different thread. So the suspend function is just part of the code routine class and it has to invoke another suspend function. So now let's modify our function block a little bit. Therefore, I'm using the width context here with context method and I'm passing a dispatcher dot io here. So neither of those is recognized. And that has to do with me not having the right implementation here. So in the right dependencies. So we need to add this dependency and it's the activity. Kate X And in my case the version is 1.4.0 beta zero one and you could of course use a different version. But either way, we need to use Kate X activity here and then we need to sync it. And once this is synced, we will find it. We can now add the classes. And did I write it correctly? No, I did not. So with all the entries, you see now we have the two imports up here and we can now use it. So with context, it's not good to run a code routine. So now of course we need to put our code in there now. And at this point the executing up here is still not happy, but at least our code here is more or less ready. So here we use the with context method to run. The task with context is a method created to move an operation into a different thread until it completes its process. And then it is moved back into the original thread. And then we are passing this Petrus IO. Which moves the task to a different thread, which is the input output threat, which is different from the UI threat. So it's running on a threat that is not blocking the user interface. And when we look at the on click listener here, we can see that it's not happy, right? So we need to make a change here. So in order to make it run, we need to say on which scope we want to run this and I'm going to run it on the lifecycle scope so we can launch it here and then and then in curly brackets, that's where we can execute our method. So in order for this to run, we need to import this launch as well. And you see here code routines launch has been added. So there are different types of existing quarantine blocks where you can launch a suspend function from depending on the class it has been called from. So lifecycle scope is a routine block built to properly handle core routines in any activity class. So now when we run the application and click on the button after it completes the app crashes with an error. So let's test this. So you see in the background, it created all of our entries, all of our 100 thousands of entries. Right. And in the end, it crashed. Okay. So quarantines keeps stop working. So let's close the app. And I still have the other iteration of the app running. And our application crashed because of a very simple thing. We are trying to run this post on our background thread, but the toast itself is a foreground or a UI task, so our UI related action like this toast can only be run on the UI thread, but currently we're running it on this dispatcher style thread and this cannot work. So how can we fix that? Well, we can add something that we want to run on the UI thread specifically by calling the run on UI thread method here. And we can then say what kind of action we want to execute on the UI thread inside of this code routine. So here, I'm just saying run this on the UI thread and there particularly the toast should be run on the UI thread. So let's test this again and see if our application is going to crash and we can see it's running in back in the background. So stuff is happening and our application doesn't crash. Well, I'm starting it again and again, so I have multiple iterations of my task and once it's done, it should display my toast here at the bottom. And we can see here. There it is. It took quite a while, but that's just because it takes a while to run the task 1000 times. So now what we could do is, of course, limit it to 10,000 times. Then we would run a lot less or 50,000, maybe 20,000, something like that. So that by itself is cool. But now let's see what is a good approach if you have such a situation where you want to run something and you want the user to wait for a response, well, what you can do is you can add a progress dialog. So I'm going to create this new dialogue entry here, which will be of type dialog and dialog, not able to be precise and will be null at the beginning. So this one will be a var custom progress dialog of type dialog, let's import it and now we can use it. So this will be the progress dialog which will just be a dialog. Inside of this context I'm setting the content view to be this dialog custom progress bar, which is this custom progress bar which says Please wait, so this thing will circle and then this will say Please wait. And if you look at it, it's just this progress bar with a width of 5050 with a little bit of margin. And then we have this text view which says, please wait. Okay, so then I have a way to show the progress dialog. Now let's add a way to cancel the progress dialog because otherwise we don't have a clean way of canceling the Progress dialog and I want to be able to cancel it once we are, for example, done with our execution here. All right. So what can we do now? Well, we can say once we clicked on the button here, before we launch the lifecycle scope, let's show the progress dialog and then once we are done. And we start running something on the UI thread. We can cancel it in the UI thread. So here cancel progress dialog which handles a progress dialog which should be also handled in the UI thread. So progress dialogs. They show something. If you show something specifically that has to do with the UI thread. So let's run this application again. It will be a lot more appealing. So now we can see. Okay, please wait. And we know that we should wait. And once the task is done, you see tasks executed successfully. And you see even if I click away, the UI still works and the task will still run in the background that will succeed successfully and I can run my application cleanly. So this will make sense for tasks that will take longer. For example, loading data from the internet or like doing bigger calculations that will just take time. So that's where it really makes sense to use your core routines. So just quickly, a summary. If you want a function to run as a core routine, you need to add the suspend keyword, you need to add with context, and then you say in which context you want this to run. So on which thread you want this to run, there are multiple options. We're just using the dispatchers i o here. And then if you want to execute this function, you need to define on what scope you want this to run. And I want to run it on the lifecycle scope here. I launch it and then I can execute my suspend function or my code routine function. So that's generally the structure that you can use for your code routines. There is, of course, a lot more to learn about core routines and even have a YouTube video on it where I go into a lot more depth. So definitely check that one out and yeah, see you in the next video.