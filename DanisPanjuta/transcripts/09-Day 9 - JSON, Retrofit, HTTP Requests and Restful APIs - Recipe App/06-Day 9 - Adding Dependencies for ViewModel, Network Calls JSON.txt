Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 9 - JSON, Retrofit, HTTP Requests and Restful APIs - Recipe App
Lecture: Day 9 - Adding Dependencies for ViewModel, Network Calls JSON
----------------------------------------

Welcome back. In this video we're going to set up our application. And therefore I'm going to show you what we want to end up with. Not after this video but after this entire day. Basically we have our application here. You can see we have beef, chicken, desert lamb, pasta and so forth. And these are just the categories that we want to display. So as a result of today we just want to make sure that we display the categories. We're not even going to go into the actual details about any given recipe or so forth. But we are interested in the categories that are made available. And you can see these are the categories that are available. It's not more than that, but it's already nice enough because we see we will need a lazy column which allows us to scroll through and display the items. We will see how to display images, how to load the data, and so forth. In order to get all of that done, let's go ahead and create a new project. And therefore I'm going to set up an empty activity application. That I'm going to call my recipe app like so, and everything else can stay pretty much the same. Let's go ahead and click finish. And this will now give us this app. I'm going to close my older application. So let it load because as you know it just takes a little while. All right, so now that it's loaded, let's set up the implementations that we're going to need. So inside of your Gradle scripts and we've never touched those before. There are these build.gradle files, one for the project and one for the module. So module colon app. Let's go over to this file. All right. So very important that it's this one and not the other one. So here you can see there are the plugins that are added in my case version 8 to 0 beta six. And then the ID version 1.90. I believe in your case it shouldn't say beta six here, but either way, let's go into Build.gradle dot and what I am interested in inside of this file is if we scroll all the way further down the dependencies. So here you see you have a bunch of dependencies. So these are basically implementations of of what item or what libraries you can use inside of your application. And as I said, we have these libraries that we can always import directly inside of our main activity as you can see them here. But those are dependent on what we have here. So we can only add the ones that we have implemented here inside of our imports here. So in order for us to use what we want to do, we need to add a few items. And I'm going to copy paste them in here. You need to make sure that you type them out exactly as they are written here. So the compose view model. Okay, so it's implementation. Opening bracket Android X lifecycle. Colon lifecycle dash ViewModel dash compose colon 262. Now there might be later versions at the moment that you are using this, but I would still recommend to use the same one that I do. Otherwise you might run into trouble or into errors that I don't run into, and I will not show you how to fix those errors in the future, because I'm not recording it in the future. Okay, now, in order to make a network call, basically connecting to the internet and collecting data from the internet, basically, in our case from this meal DB website, because we only need this little string which is of type Json, but we still need to load that. And then we also need to load the image. So in order to download the image, we need to make sure that we have the right libraries available. That will make this a lot easier. Otherwise we would have to write loads of code to just get the same functionality done. So basically, you could think of this as some external party, like a group of developers that build a bunch of code and now make it available for us or to us, so that we can save loads of time and loads of code in order to achieve downloading data. So that's what retrofit does. It allows us to make network calls, basically calling the network and getting data. And then there is this thing called Json. It's also from retrofit two. And this is allowing us to convert Json files. So Json objects. So basically the code that we saw here how a Json object looks like. So this stuff here or you could also say this Json object converting all of that into a Kotlin object. So into something that we can work with inside of our Kotlin project so that we can access its properties straight away. So it's basically going to be treated like a Kotlin object, like a class that we well, we need to define how the class looks like, what kind of data we need from that, and then we can load that data from the internet and make it available as objects inside of our project, and then directly access its properties, for example, the name, so that we can display the name as well as the link to the image so that we can download the image. And in order to actually download the image and display the image, we're going to use another implementation. So it's going to be this IO coil coil compose. So this one is to convert Json files. And this one is to load images. So instead of the Json file we will note well we will find out which images to load. And here we're going to actually load the images. And this is another third party that just makes it so much easier for us to do that. Otherwise it would be a lot more code that we will need to write and all of that. So once you make changes to your Gradle files, you need to sync your project. So click on the Sync Now button and then your Gradle project will be syncing. So it won't sync but it will sync okay. So and then we can use those inside of our project classes. So now we can well once it's done building the Gradle again we can use the ViewModel lifecycle. We can use the retrofit network calls as well as the converter that allows us to convert Json files to Kotlin objects and maps them directly, as well as load images. So let that happen, and then look at what data we want to actually have from the miel DB website. Okay. And by the way this took a few minutes in my case. So don't worry if it takes a little while. That's just normal because there's a lot going on in the background.