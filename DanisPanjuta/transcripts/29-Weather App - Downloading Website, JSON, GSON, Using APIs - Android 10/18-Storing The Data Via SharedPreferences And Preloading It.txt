Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Weather App - Downloading Website, JSON, GSON, Using APIs - Android 10
Lecture: Storing The Data Via SharedPreferences And Preloading It
----------------------------------------

Welcome back. And this video, we are going to add the feature that we stored, the weather data and shared preferences so that we can then load the data even if we, for example, don't have an Internet connection and we open up the app so that it doesn't look empty or it doesn't display nothing, pretty much. So at least it will display something, even though it will not be the correct data for the current moment. But it will of course try to reload. So we're just going to see how to store data with shared preferences, which is a way to store very simple data, so to speak. Usually you would only store strings in there or integers or doubles, but you can also store whole objects if you make those classes, as we have done. C realizable. All right. So we made everything C realizable. So this allows us to make so-to-speak strings out of it so we can make an object to become a string, which we then store and share preferences. Because if we look at our logcat, we could already see that we get the whole response pretty much. So this weather response is in the end really just a string. So all of this information is stored as a string in JSON, so we can just bring it in the right format and to store it. So let's go ahead and do that. Therefore, we need to go to our main activity. And first of all, start with creating a new variable which will be our shared preferences. So private late in it, variable and shared preferences will be of type shared. Preferences. All right. Next, we need to initialize this shared preferences and I think a good spot would be on create. So just here, let's go ahead and use shared preferences to initialize it with get shared preferences. Now here we need to enter a name and then a mode. So the name that I want to use is a constant. So let's go ahead to our project and go to our Java folder constants. So here I'm just going to create new variables. All right. So first of all, the preference name, so const vol preference name and then whether response data. So these two variables as to which I want to have. So the preference name will be the weather app preference, which will be our shared preferences that I want to use. So going back to our main activity here, I will use constants dot, shared or preference name. And then I need to give it a vote. So let me do that real quick here. So context dot and here are multiple modes available and I'm going to use the mode private which in the end. As the file creation mode where the created file can only be accessed by the calling application or all applications share in the same user ID. So it pretty much means that this shared preference information that we store on the phone should only be visible for this application and no other application. You can, of course, store your data so that it will be accessible from other applications, but in our case, we're not going to do that. All right now inside of our and Q method. And that is when we get the weather details here. And on cue, once we get the response, once we have the weather list, at this point, we can go ahead and store the weather list in shared preferences. So instead of. Passing that to the setup UI or weather list. What we will do is we will say, okay, setup UI doesn't need that. It will get that from the shared preferences because storing something in shared preferences and getting it back from shared preferences is a super quick process. It doesn't take too much time. So what we're going to do is we're going to store the weather list inside of our shared preferences. So I'm going to create a new variable, which is going to be weather response. JSON string where I will use Jason to Jason with our weather list. So basically I'm bringing it into this format. So let me import it some. And now I'm bringing it into a string type of format to JSON will return a string. Okay, so now I have this string and as you know, shared preferences can store strings. So we need to prepare the editor, our preferences editor. So this is the default behavior or default code that you use for this. So you start to edit process at this point and then you put information into this editor and you can see you can put a Boolean float in long string and string set. So you are really limited to basic data types, so it cannot pass in a whole object, even though we do that with a little trick, as you can see, so we can go ahead and store that under weather response data, dot or comma. And what do we store in there? Well, our weather response JSON string. So instead, we could have, of course, just stored something like hello here. And then it would be stored in the key weather response data, which is this weather response data string that we prepared in our constants. So basically it's always a string to whatever type you had afterwards pair. So here string two in this case string pair. So we're putting something at a position. So this is the position where we put it, so to speak, a. Folder in which we put it. And this is going to be the file that we put into that folder. All right. So in our case, let's go. Of course, not going to be hello. But it's going to be the string we just prepared based on the weather list, which is, of course, if we look at it. The response result, which is this whole weather response pretty much. But it's not just one. It's all of them. But we, of course, only have one that we actually use. So we have this editor and then we need to apply the changes. So we prepared it. We put the string in there, we applied it, and now at this point we can call setup UI. And now we of course need to change setup UI because here we don't need a weather response anymore, a weather list. But instead what we do is we get that from our shared preferences. So let's go ahead and get it from there. Vol whether response JSON string will be m shared preferences do get string. So now we get the string back from our constants dot weather response data. So now at this point, we get this weather response JSON string and we can of course, go through it by checking if it's not null or if it's not empty. So we can check both whether response JSON string is not null or empty. So here, by the way, I'm using this exclamation mark for this not statement that I just said. Okay. And then we can make a JSON or JSON from it back. So pretty much we can convert it back. How do we do that? Well, we create a new variable called weather list. Which will be our Jason shot from Jason. And we're going to pass under weather response Jason string and of course this whole for loop. All the way up down here can be put inside of this if statement. Okay. So here. We are pretty much converting it from Jason and Poway. Jason also needs to know how to interpret the string because it's just getting a string. But we saw from Jason before. It also needs to have the type for the class. So actually the class like this here. Okay. So we are passing the class that we want to use for this. And this is our weather response class. The Java and of course, we need to write it correctly. Okay. Well, the response class Java. So as it seems, I still have a little error in there. We have to be super careful here. So when we get the string. We need to pass on the default value. That's something that I missed. So that's the thing. If you. Have way too little space on the screen. Okay, so what I changed is in our shared preferences, that's something that you have to do. Take care of the weather response, JSON string. We get the string from our weather response data, but we also need to assign a default value. So what if there is nothing in there? So I'm going to say, okay, if there's nothing in there, just use an empty string as a default value for weather response JSON string. So then what we do is we check, is that string empty or not? So in this case, if it's the default value, which will be the case at the very first time that we start the application now, then it will be empty and it will do the block of this if statement, which is nothing, but if it's not empty, which means the second time that we run it, what it will do is it will. Go inside of this if statement and then it will get the weather list from our JSON string. And it's going to transfer it into a weather list object, which is going to be, of course, a weather response object which contains all of this information. So we'll coordinate the list with the weather, the string and so forth, so the base main visibility and so forth. So it's going to give us all of that. And then we can use the for loop as we did before. So nothing has changed, pretty much at least for this for loop. All right. So at this point, we need to make sure that setup UI is also called in our on create method. So let's go in and create. And I think it makes sense to set up the UI directly. Under our set preferences. So as soon as we gotten the shared preferences, so now let's test it and see what's going to happen. So the thing is, it will first of all have empty data and then it will load the data, will populate the data, and at the same time, it will store the data and share preferences. So the next time we open up the application. You can see the next time we open the application, it will not be empty as it was before. So let me run it again. Just to show you. So you can see it had data there already, which was the old data, but it had data there already before it loaded the data from the shared preferences. So that's really the idea behind it, even though it's not going to display the current up to date data, but at least it will display something for the user before it. Then of course loads the data from the server. All right. So that's how you can use shared preferences to use JSON objects, basically, and store them on your phone without using SQLite or anything complicated. Really keeping it super duper simple. All right. So I'd say let's check out the next video.