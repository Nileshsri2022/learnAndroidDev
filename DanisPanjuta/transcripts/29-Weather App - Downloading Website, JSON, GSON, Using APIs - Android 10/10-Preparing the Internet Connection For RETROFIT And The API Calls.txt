Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Weather App - Downloading Website, JSON, GSON, Using APIs - Android 10
Lecture: Preparing the Internet Connection For RETROFIT And The API Calls
----------------------------------------

Welcome back. In this video, we are going to implement the functionality that allows us to access the Internet. And then also at one point, of course. Ask the requests or send the request in order to get the API call done and get all of the information about the weather for a specific location. So we have the location all ready now we need to implement the internet features. So let's add those three permissions here. Internet access network state and access wi fi state, because those will allow us to see if we have Internet and we can access websites. All right. The next thing that I want to do is to have a constant object. So let's go ahead and create a new file here. So new Kotlin and this one will be a file. I'm going to call this one constants. All right. So this one will be an object constants and it will contain all of our constants. So the constant that I want to have for now is the content about whether we have Internet or not. So is network available where I need to have the context and it will return a boolean. So this one will help us to see if we have internet or not or if we have a network or not. And the thing is that in the past, that would have been super easy. All right. So in the past, we could have just used the connectivity manager in order to get that. So let me show you how it worked up until API 29 or even 28 only, but well, connectivity manager. So this one here is what we're going to need. So I want to create a new object of it. So context dot get system service. And here for the system service we need to pass. The context dot connectivity service as a connectivity manager object. Okay. So basically what's happening is that we have this line here where we create the connectivity manager object, which will give us the connectivity service of our system, which we get in the context. So basically what we are in the main activity, we want to check in that activity if we have Internet connections available or not. So if we can connect to the Internet or not. And now in the past, as I said, it would have been easy. So let's just look at that first vul network info. So I'm going to call it will be a connectivity manager dot active network info. Okay. You can see this is deprecated, but that is just because it's the old fashioned way, so to speak. But it still was super simple, right? So we get the network info and then we can just return. Network info is not null and network info is connected or connecting. So that's the easy way. That's how you can do it with the deprecated way. But now in the latest version of Android Studio and the Android API, what you need to do is to return depending on the version. So here if the built. Version is SDK on the score int. Greater equal built DOT version codes. Dot am. Okay. So let me import bills here. And then what is M? M is version 23. So what I'm saying here is, is the SDK that is used for Android 23 or younger. If that's the case, then use some other code here. So then use the if code and otherwise use this code that we prepared here. Okay. So we're going to use the deprecated approach for the old versions. So for the old Android studio versions, meaning Android M and before and for the newer ones, we're going to use the following code. So here we need to create a network which will be the connectivity manager, dot activity or here active network and not network info this time. And then if that is empty return false. So if we can't get access to the network manager or the active network, then we will return. False. So this whole is network available will return false. But if we don't have an empty network, then what we want to do is we want to get the active network. So here active network which will be the connectivity manager dot get network. Capabilities. With that network. So first we get the network, the active network, and then we check for the network capabilities. And if they are empty as well, then return false. So when do we return? True? Well, let's look at it. So here, what we can check is return. When? So this is like an F statement? More or less. So return when the active network. This one here has transport. Network capabilities dot. And here we can use the different ones. So wi fi, ethernet and so forth. So if we have transport wi fi. Then we want to return. True. Okay. Here. Return. True. So we already had return here. So we're saying return when this is the case. True. Then when something else is the case. So for example, the same thing up to here, but this time we look at the transport cellular. Return. True. And then finally the transport Ethernet. So maybe somebody uses LAN in order to connect his phone. There are actually ways to do that. And then finally else return false. Okay. So what we're doing here is we're checking for the version. If it's a. Newer phone or a newer version on the phone, we get the network first. So the active network, if it doesn't exist, we will return false. Then we use this active network to check its capabilities. And if it doesn't exist. So if it's null, then we return false here as well. Then we return true in the case that we have wi fi, that we have cellular internet connection or that we have Ethernet, and if we have none of those three, we will also return false. This is basically for the newer versions of Android and for the very old versions. We are going to use the old approach, which is to use active network info and then return is connected or connecting. So this will return a boolean here and you can see it's deprecated. So it's old Java code, so to speak, that is deprecated, which doesn't mean that it's not going to work. Right? It just means that. It's not recommended. And when you want to know more about it, you can, of course, check out the StackOverflow. You can see here the connectivity manager active network is deprecated also in API level 29. So there are alternative ways to do it and that's what we've just done. So this one here, so where we get the transport, wi fi, transport, cellular and transport Ethernet. I just wanted to show you the different options that you have here. All right. So at this point, we will be able to know whether we have Internet or not. Now, the next thing that I want to implement is another. Cradle. Implementation here, which means another third party library. And in this case we are going to use retrofit. So let's implement that and sink to file and look at retrofit. So retrofit is a type safe HTTP client for Android and Java. So if you want to know more about retrofit, then check out this website. Here you can see square dot GitHub, dot IO slash retrofit and this is going to be the API to it. So it's not super long, but basically what it allows you to do is to use requests a lot quicker and a lot easier than what we have done so far. So it allows you to very quickly get an API call out and then get the result very quickly. So you can see retrofit class generates an implementation of the GitHub service interface. So here we can use an retrofit object with the builder and we can just add the base URL and build and then run that service in order to finally then call the HTTP request so you can make a synchronous or an asynchronous HTTP request to the remote web server, which in our case will be the open weather map API. So the link that we have here, if we use any of those links, for example, the one with the latitude and longitude. So this is going to be the link, even though it will not be the sample, but it will be another link. All right. Now that it is synched, we can use retrofit. So let's go ahead and go over to our main activity because that's where the magic happens. And let's create a function which will get us to weather. So private fun. Get location, weather details. So I'm going to call it. And what we need to check is if we have an internet connection so we can go to constants. DOT is network available? And we need to pass in the context which is this. So in this activity, do we have an Internet connection? And if we do, then we want to write a little toast for now. So here we will later on implement the code that will, of course, need to be executed. Once we have an Internet connection, we want to get the weather details, but for now it's just going to be this little toast. And otherwise we're going to write a little other toast, which is just going to say no Internet connection. All right. So no Internet connection available. Now, what would be a good spot to get the location weather details? Well, in our location callback, which we use to get the location of the user, once we have the longitude and latitude here, we should call this get location weather details because we're going to use variables for that later on that we can then either pass to this function or we can have global variables which take care of that. So either way, we now have prepared the code. Now of course for now we can just test this and see if we have Internet in our on our phone. So you can see I'm on flight mode now, so it should display that I don't have Internet. So let's see. No Internet connection available now. Let's deactivate flight mode. Let's turn on wi fi and the other data. So just cellular data. Let's run this again. And you have connected to the Internet. Now you can make an API call. All right, so now everything is prepared. And in the next video, we're going to see how to make a call to our API. So see you in the next video.