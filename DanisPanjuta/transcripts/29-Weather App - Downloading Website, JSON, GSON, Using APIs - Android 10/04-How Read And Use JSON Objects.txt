Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Weather App - Downloading Website, JSON, GSON, Using APIs - Android 10
Lecture: How Read And Use JSON Objects
----------------------------------------

Welcome back. And the last video you saw how to download data from the Internet and then display it. In this case, it was just a JSON object which contains a bunch of text, right? It's just a text file, so to speak. But now let's look at how we can deconstruct this whole JSON object. All right. Let's go ahead and do that in here. So, first of all, I need to create a JSON object. So, well, JSON object is going to be a JSON object object and you can pass in a string here, which is our result, which is this huge string that we saw in our locket. So this message and so forth, this thing here, now this JSON object will now understand the structure of it and now we can access elements from that JSON object. So I can just go ahead and say I want to have the message which will be JSON object dot opt string of our message. So if we look at up string returns, the value mapped by code name. So whatever we enter it as name if it exists, coercing it if necessary, or the empty string if no such mapping exists. So in our case, if we look at our logcat result, we can see that such a message in fact exists. So this here is the name that we passed to string to this method here, and then it will return the value of it. So we pass in the key and then we get the value. Okay, so now this message, I will just lock that to my console and let's just see if that's actually going to work. Okay. So let's go to our Logcat and see if something like message exists. Okay. So we have here message. You have successfully logged in. All right, so that worked. So now we can access elements from our JSON object. So that's how easy it is. Of course, you can now go ahead and do the same thing with the user ID and all of the other objects. Right. So the. Name and so forth. So whatever the name is going to be in our JSON object here. Whatever the name here is. So if we look at our logcat again, you can see there is a name in my case it's Pong tutorials and so forth. Okay, so you can get all of those values and you can see each time you have to search for the specific name of the key. Okay. So in a key value pair, but what if there is an actual choice, an object in there? So if we look at it again, we can see that we have this JSON object, this is this whole thing. And in there we have another JSON objects which is this profile detail. So this year is a JSON object. So if we want to get the JSON object, well, we can use the following approach. So we saw that we can get strings and we can get ints because the user ID, as you know, is an integer. Right? But we can also get opt JSON objects. So just going to create another file profile detail object. So I'm going to call it and it will just be a JSON object dot opt JSON object. And what's the name of it? What's the name of the string? Well, let's look at the cut here again. It was called profile details. So let's paste that in here. Profile details and now let's use that. So well, is profile completed? Will be the profile details, object, opt. And now what do we have there? We had a boolean, so opt boolean. And what's the name of the boolean. Well, is profile completed? And now. Let's lock that. So let's do this. Locked out. I is. Profile completed and then we're just gonna use this s profile completed value that we prepared here. So that is how you can access an object with is of type JSON or JSON object within adjacent object. You can still access it via its key, which is in this case, profile details. And then you can just use this opt JSON object method in order to get it by its name or by its key, so to speak. And then we have another very important and interesting aspect here, which is a list. So you can see here we have a list of JSON objects. So how do we get that? Well we get that by using an apt JSON array so. Well data list array. So I'm going to call it and it's going to use JSON object again and get or here it's actually opt JSON array. And now what's the name of the JSON array? Again, you can go here and see. It's called data list in my example. All right. So here we use data list. And now I can use that data array to access its parameters, for example. So if I want to know the size of the array, so how many entries are in there, I can just go ahead and say data list, array, dot length. So it can just. Display that in the log. But then if I want to know about every single value in there, so every single entry, then it's a little more complicated. So here I use a for loop for item in zero until data list array thought length. So I want to go through the whole list and I start at zero and I go to whatever the amount of entries are in there. And here I can access the individual element. By using data restore line item. But then this item would be a JSON object, right? So I need to get that as a JSON object again. So I'm just going to say data item object, which will be a JSON object. And I get that from data list array dot or actually here item. As Jason object and this, of course, as a variable. So this is how you can now create an object out of whatever was inside of the list. Or in this case, it's a JSON object, right? So that the list array item will be first this chosen object, then it will be this chosen object and it will be this JSON object and so forth. So now we have to do the same thing with every single data item object as we have done before with those JSON objects here. So now we can get its strings. It's in its whatever we have in there. So in this case, we have an ID, which is an integer, then we have a string, which is this value here. So in order to access them, you can of course pause the video and try to do that yourself. So pause it and try. Okay. Let's try to access the ID. So I'm just going to say, well, ID is equal to data item object dot opt int and it was called ID. And now to make sure that it's really an integer. You can see it's a number without the. Exclamation points here. Or exclamation marks. So. Now let's log the ID, log tot I ID is going to be a message. Will be it. Now let's do the same thing with the value. So file value is data item object dot opt string this time. And what's the name of it? It was value. So that's the key. And then we can go ahead and lock that as well. So log that I with the value being the. Value. Now let's test it again and see if we actually get those different values and IDs out in our logcat. So let's search for them. Let's search for, for example, value. And you can see value zero was the ID one. Value one. And the value itself is one. You can see it totally works. Now, let's say we want we want to have the ID and there we are. So ID one, ID two, ID three. All right. So now you know how to pull everything apart. So how to reach Jason objects and use them as JSON objects. So this is super powerful stuff and it will come in handy so many times, believe me. Now there is one thing that you might have wondered from the start Why do I call this one call API login async task? Because I'm not logging in or anything like that. And how would I even log in? Well, in order to log in, you need to send a post request to the website, whichever you are using right to the service that you're using. And in our case, we're not doing that yet. And that's what we're gonna look at in the next video, because then we will need the username and the password in order to log in and in order to then of course send this information to the website and the website will then. We in our case, do nothing. So unfortunately, we won't be able to test that yet, but we will see later on once we are at the Firebase example, how that will come in handy. But I think it still makes sense to also talk about it here while we're already working with API calls and everything. All right, so see you in the next video.