Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Weather App - Downloading Website, JSON, GSON, Using APIs - Android 10
Lecture: Using The GSON Library to Make Working With JSON Easier
----------------------------------------

Welcome back. In this video, we are going to check out the JSON. And JSON is a library that allows us to work with Jason object. So Java, serialization, D serialization and library to convert Java objects into Jason and back. Perfect. Jason is a Java library that can be used to convert Java objects into their JSON representation and it can also be used to do the other way around. So that's exactly what we need. And you can see in order to use it, we need to add the dependency here in Gradle and yeah, let's just do that. So let's go over to our project and then go to the build gradle down to the dependencies here. And let's just add this. I'm going to put that in here. Code JSON, JSON or JSON. So sync. Now this will download everything that we will need for JSON. And Gradle needs to rebuild. Of course, once that is done, I will go ahead and make my life easier by, first of all, putting some effort in. So I'm going to create a new class here, which will be called response. Data. So this will be a data class. And this data class, it should have several. Values. And in this case, it will be variables, of course. So vul message will be of type string. Then what else do we have in our json file? Let's look it up. We can see we have the user ID, the name, the email, the mobile. Then we have another JSON object, which will be the profile details, which is also its own class, so to speak. And then we have the data list, object or data list, detail object. So this is another different type of class. So let me just paste that in and explain what's going on. So what I did here is I just created a response data data class which contains the different key names. So we have the key name, message, key name, user ID, name, email, mobile profile, details and data list. Of course, we want to store values behind those keys, but we make our life easier by using a model, which is this response data. It's a model of our response data, and we can then use it very easily when downloading the data from the website that we're using. So whatever website it is, in our case, it's this mock up site, but let's say it's a weather site or it's a football results site or whatever. Then we could also just prepare the model like that. But you can see something that is a little more complicated than that is this vol profile date details. So we have profile details, which is another class, and then we have data list, which is actually a list of data list details, which is this class here which has an ID and a value. So it's basically the same thing that we have here when we look at our JSON representation. So here. We have the message, the user ID, email profile details, that's where it becomes interesting is an own class. So it's another JSON class here or JSON object. And then we have the data list, which is a list of data list details. So these are data list details, so to speak. So it has an ID and that has a value. All right. So. You can see that here. We use the list keyword. So this is the, let's say, more complicated part. But I wanted to have that in there so that you really understand this part of it as well. It's really important to also see how you can handle lists from JSON files. All right. And now how does that even change our future work? Well, let's look at our main activity and all the way down, we had all of this effort that we needed to put in. So, for example, when we wanted to get the message, well, we used this up string and then we got the message and then we could log it. But then the beauty is it can be done a lot easier when using JSON. So let me show you real quick. I'm going to create a new JSON variable, which I'm going to call response data, and that will be JSON. Start from Jason and then we need to put in well in port Jason first of all so Jason here in port that then we need to put. Well you can see there are multiple different options. So Jason element, Jason reader, reader string. So we have a string, right? Our result is a string. So let's pass that in there. Let's pass the result, which is our JSON file, so to speak. And then we need to map that with the data class that we prepared. So here I'm just going to map that with response data, which is a class and here I use dot Java. So what I'm saying is, is the response data as the model for the result that we have here. And it's going to map that. So it's going to map the different variable names that we have here with the variable names of our. JSON data that we get here and now we can go ahead and use it. So now instead of creating this message variable and getting it from the JSON object of the string, we can just go ahead and say log dot, I is going to be message, which will be our response data dot message. Again. And like that, I can go ahead and do that with all the other variables as well, so I can now access the user ID with this approach the name, the email, the mobile number, everything. And when it comes to the profile details and that is where it's a little more interesting there, I can just go ahead and say profile or response data profile details dot is profile completed so here. We access the profile details and in the profile details we accessed a variable ES profile completed. So that's how easy it is done in JSON. That's really amazing. And the same goes for the rating. So here profile details are rating. And now if you want to access elements from a data list. Then we have to use the following approach. So you can see here we use data list dot indices. So for item in response data to data list indices and then we get the value, which is the response data list item and then we get the ID of it and the value. So similar to what we have done before with this for loop here. But now we are using JSON, which makes our life a lot easier. So we don't need to create objects all the time, but we just have one response data object which we then can use in order to get the data very quickly and access it very easily. All right. And now let me. Comment. This whole part out here up to the point where we had the for loop here and run it as well or run it again just to see if it's going to work just the same. All right, so the cradle is done building. Now let's look for. Here, not the JSON, but for example, the message. And you can see we get the message here. You have successfully logged in. And then if we look for the user, you can see the user ID is there. Let's say we want to name the name is there and everything. So now, of course, you can also just look at everything. So show only selected application and then you will find that it will pretty much give you all the data. A little overhead is in there as well, but it's still pretty good stuff. So you can see mobile rating value data list details and so forth. So you can see it's a lot easier or quicker to use JSON. And you also have the data model directly here as well, which is pretty clean and that is the improved and best practice approach, so to speak. So I hope you enjoyed this part. We are going to use that quite a bit. Of course, in the future it's going to be important to understand all of that. But if you didn't get it completely, no worries. We're going to have some more examples for this. So see you in the next video.