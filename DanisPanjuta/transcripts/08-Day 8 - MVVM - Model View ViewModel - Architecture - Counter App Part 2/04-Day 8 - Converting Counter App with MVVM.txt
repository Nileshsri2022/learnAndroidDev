Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 8 - MVVM - Model View ViewModel - Architecture - Counter App Part 2
Lecture: Day 8 - Converting Counter App with MVVM
----------------------------------------

Welcome back. In this video we're going to set up our view model class. So therefore we are going to set or say that a class is going to be of a certain type. Now that is basically what inheritance is all about. So we're going to look at inheritance in a separate video also. But for now let's stay with our example and see how we can make sure that our code and classes are organized for specific roles that they fulfill. Okay. So because so far we just had this one class, the main activity file, which took care of everything. Now we can separate that very cleanly by using a view model. So let's go ahead and create a new file in here new Kotlin file class. And I'm going to call this one count our view model. So you just have this view model in the name of your class. And then whatever else the class is about. So this class is about counting. And well it's basically the manager between the view and the model. So it's going to be the counter view model class. So let's go ahead and create create that. Now if we look at our main activity and we go all the way up here, we can see that our main activity is inheriting from component activity. Now we will look at what inheritance is in the next video. But it's just still important to see that here because we're basically saying main activity should behave like a component activity. And by doing so, we suddenly have these things like override fun Oncreate we have super Oncreate otherwise we wouldn't have that. So basically these are predefined things or like code pieces that just say, hey, this is what you have to do if you want to use me, so to speak. So now our counter view model will be of type ViewModel. So there is this class called ViewModel. And I'm just going to say that our class should use or be of that type ViewModel. So this ViewModel class here defines how a ViewModel is supposed to behave, what kind of things a ViewModel is supposed to do, what kind of methods it has that it can use, and some properties that are predefined as well. Before we go into more about inheritance. And that's what we're going to do in the next video, let's have a quick look at also what has been imported for us. So you can see import lifecycle ViewModel has been added for us okay. So it's inside of Android X and we can now go ahead and use that inside of our program. Now if we look at our ViewModel class, what are the things that we need to have. Well number one is going to be we need to have a variable that stores the count. Now we're going to use a mutable state to hold the count data. So we have seen mutable states before, but this time we're not going to use. Remember we're going to use, first of all the private keyword val count. And whenever you make a variable private, which means this variable is only going to exist inside of our class, or is only accessible from inside of our class and cannot be accessed from outside of our class, then it is common practice to add the underscore to that variable so that you can directly see, okay, this is a private variable. That's what this underscore is for. And then I'm going to say this will be mutable state of. And it doesn't offer me mutable state for quite a bit. So I'm going to write it out mutable state of. And then I'm going to say it's going to be of type or it's a zero. So it's going to be of type int. So now let's import mutable state of. And you can see it has been added here to our imports at the top. Now the next thing is our count variable as I said is now private. So we can only access it from inside of this ViewModel. And that is a good practice right. But if we want to expose it to outside. So if somebody wants to access the counter variable. So for example, our user interface wants to access the counter variable to know what the variable is is at at the current state we need to expose the count as an immutable state. So how can we do that. We can do that by using vowel count and then mutable state of type int. And we need to import mutable state here and say that it should be whatever count at that point is going to be. Okay, so now we can not change the value of count from outside because it's of type val. So it's set once and that's what the value is going to be. But but as it is a state it's going to still be quite flexible. So whatever count is that's what our count, that exposed variable that we can access from outside is going to be as well. And now in here is where we want to have the logic about how calculations are done. Okay. So here it is increment and decrement. I'm just going to copy them out from the other function I'm going to put or like from the other file. And I'm going to put them here. But now I'm not editing the exposed count variable. But I'm editing this count variable. So this underscore count, that's the one that I want to modify the value of whenever we're incrementing or decrementing. So we're basically. Only modifying the variable that we have privately, but then the one that can access it from outside. We basically store it in there. So you do that because then you can modify what should happen when somebody tries to access it. So you can give a lot more instructions as to what happens. So in this case it would seem a little arbitrary. Why would you do that? Right. But that's just a way of ensuring that your internal variable, the private variable, is not exposed to the outside, only its value is exposed, not the variable itself. All right. And how do we now manage our main activity? So we can get rid of the logic from our main activity counter app. And now we need to pass our counter view model in here. So I'm going to pass a view model of type counter view model. And now we can use the view model in order to modify, for example, what we display. So instead of displaying count value. And we can also get rid of this count variable. By the way we only have the column in there. We don't have the count value. However we have the view model dot count dot value. So view model dot and I have the URL. I forgot the view model that count dot value. And now the same goes for our increment. So we need to use view model dot increment and view model dot decrement. So now by doing so we are using the functions. From here we're using this function as well as this function. And we're accessing this variable instead of the private variable and modifying it accordingly. Now however when we go to our. Main activities Oncreate function. We can see that counter app now needs a ViewModel that we can pass to it. So where do we get this ViewModel from? Well, inside of our set content. There we set a ViewModel object up. So we're going to say counter ViewModel. And this will just be a ViewModel object like so. And now in order to use this ViewModel, we need to add the dependency here so you can hover over it and you can see add dependency on Android X lifecycle lifecycle ViewModel compose. So let's import this here. Then it will take a little while until the import is done, and you will see that we have now this import where we have Android X lifecycle ViewModel compose ViewModel. And now we can pass this ViewModel to our counter app. So to our composable that needed a ViewModel data type object whenever we want to run it, and we want to run it right here or display it in our case. Now let's rerun our application and just see whether our variable is going to be stored or not. Because that's what this whole thing was about. Not only but that was one of the important aspects why we set it up this way. And there we are. So I had to restart my computer for it to run. But anyways, now we have our little application again. Now the mVVM variant. Right. So the optimized variant so to speak, let's increment a little bit and then let's turn around our phone. And we can see even though it did refresh the user interface basically rebuilding everything. But our state has maintained. And now our number five is still there. So what you could probably just take away from this video is that mVVM helps you to maintain the state better. But as soon as we get to the M part, so to the data part of mVVM, it will make more sense. What we currently only have is really the VM. So you could say that. This is our. Our model, even though it's not really so the model is a separate class that we need to set up as well. But then we have the view which would be the. Then we have the view which is our main activity. Which takes care of the view. So basically what we can see on the screen. And then we have our view model that manages the communication between the data and the UI. All right. So that's it for this video. See you in the next one.