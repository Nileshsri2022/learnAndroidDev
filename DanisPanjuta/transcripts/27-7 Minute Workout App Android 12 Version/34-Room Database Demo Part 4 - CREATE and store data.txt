Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: 7 Minute Workout App Android 12 Version
Lecture: Room Database Demo Part 4 - CREATE and store data
----------------------------------------

All right. So let's go ahead and finalize this application. And therefore, I'm going to go very quickly over the user interface because it's a very generic user interface. You can see we have a text view within the text. We have another text here with another edit text next to it. Then we have this button, we have a text view, and underneath we have this recycle view. So we're using a recycler view. This is a scrollable view here you can see it when you well add enough items, you will see it even better, but you can even see it from the animation. And then we have this little item UI. So this item UI has one text view, which is the name, another text with a little bit of distance, which is the email ID. Then we have one button and a second button. Which is more like an image view. So we're going to use an image view here instead of a button, but it's going to be behave like a button. So that's generally the user interface. You can try to build this yourself. You will need to use an item adapter for it. So this individual entry here is an item. So you will need to create an adapter that allows you to store all of these information. So I'm going to go over these parts of the video very quickly because we want to focus specifically on the room aspects of it, because everything else we have covered in the course so far. So let's go ahead and jump right into it. So first of all, what we need to do is we need to, of course, change the main activity XML file, and I'm just going to paste in my code real quick and go over just very briefly. So generally speaking, this is the user interface that we have. You see, we have this no records available, by the way. Also, this is a text view that will only be visible if we have no entries in our database. And there you see, so we have this text view, then the edit text, text, you add a text button, text view and recycle view. So I'm using a constraint layout with this text view. You can, by the way, also just download the XML file. I would recommend to do that so that you don't have to type all of that stuff in, even though you could use this as a little challenge for yourself. So here I'm using an edit text instead of this entire edit text layout stuff that surrounds it because I don't have a specific UI style here. I'm keeping it simple, just using this edit text. Then we have this other text view with the edit text which is just next to the text view email label. So you see here starts to end off. That's how you can get this. Thing where you see that the text is on the left of the text and then underneath we have this button which has the idea of button add. Then we have this text view which just says all the inserted records, which is this bold text that you can see here, which is the top of it is at the bottom of the button. And then we have our recycler, which is just underneath the text view name. And then finally we have this text view which is only visible if there are no records available, which is at the bottom of the button add. So that's pretty much the user interface. And as I said, we also need to make sure that we have the individual items. So this item here, you see texture, text, view, image, view, image, view. So all next to each other. So let's go ahead and look over how this is going to look like. So I'm going to import it here. This is going to be how my row looks like. You can see it's pretty empty because there are no images here because I also need to add the jobs so you can download, you can use your own troubles, but I'm just going to use those ones that you see here. So the edit icon as well as the delete icon like so. Okay. So now we have those two images here. You see edit and delete. We have a text view and the text view. And how does this look like? Well, I'm using a linear layout to keep it simple here. Items are oriented horizontally, so right next to each other, not on top of each other. We have a text view, another text view, another text view, which is, well, this text is just a colon. If you have want to have it in there, you see name, colon, email, and then we have the two image views. So one image view being our edit image view and then the image view that is to delete image view. And then we have this little dialog that pops up when we want to edit something. So you see update record is the text you at the top. Then we have another text view. This is an edit text, another text view with another edit text. And then we have two buttons here at the bottom which one says update and the other one says Cancel. So we need to design this as well. You can try this of course yourself and pause the video, but otherwise let me just quickly put it in here. So I'm going to call this one dialogue update and this is going to be the code. So I'm using constraint layout once again with a text view, which is this title thingy. So let's look over it. It's this update record text view. So let me make it a little bigger. Here on the side of that record. Then we have this text view which says name which is next to this enter name, edit, text. You see here they are connected to each other. So. Start to start. End of TV name label. Then we have this text view. Which is underneath this other one, but next to the added text, which is the enter email, edit text, which is this one here. And then we have this update button, which is also a text view I'm using in text view here and then this cancel button. But either way you could have also just used buttons here. Either way would have been fine and that's pretty much it. Okay, so now that we have the user interface out of the way, let's add some room code. So first of all, we need to find a way to add a record because otherwise we don't have anything to be displayed on the screen. Right. And we don't store anything in the database. So if you don't have anything that you can retrieve or edit, then you cannot work with the database. So you need to add something first. So let's go ahead and create a function add record here, which is going to be the requiring an employee now. So we need to pass the double that we want to work with. And now we can use view binding and therefore we need to set up a binding object here at the very top. Let's make sure we have that. So I'm just going to use the activity main binding, set it to null by default as always. And then let's set up the binding and content view here accordingly with standard multiple times in the course already. So activity main binding inflate using the layout and and setting the route as the content view. Okay. Now we can use view binding in our ad record method. And why do we need that? Well, because in order to add something, we need to get the data that has been entered. So here. This data as well as this data needs to be gotten as well as we need to add an click listener to this add record button. So let's get the name first which will using be using our binding object dot adage takes name and get its text property as a string to string. And then we need to do the same thing with the email. So here binding again we get the added email ID or edit text email ID and here we get text from it to string. Okay. Now we can check if both of them aren't empty. So if email is not empty. And Double E and sign here. The name is not empty and maybe we should do it the other way around. Email is not empty here. This should be name. Just because of the order of how we created the variables, it doesn't really matter. But just for. The looks of it. So if both of this is the case or both aren't empty, then we can insert the data so we can take this name and we can take this email and use our dowel to insert something. But because this has to be done in the background, so we need to use code routine content here. We are going to say that we want to use the lifecycle scope for that. So we're launching our code routine. So here you need to import launch, which will then be available up here. So we see lifecycle, scope and routines launch have been both added to our namespace is up there and now we can use the employee doll that has been passed to this ad record method. In order to call its insert method. So if you recall, we had this insert method, the suspend fun insert, which is a routine function which we are now calling inside of the lifecycle scope. And here we can now pass the data. And what is it that we need to pass? Well, we need to pass an employee entity object. So let's go ahead and create an empty entity object like so where we can pass the ID, but we can also skip it because it's set by default and also incremented because it is if we look at it, it is the primary key, so it will be auto generated. We don't need to manually set the ID, so the only thing we need to set is going to be the name and the email. So let's set those two things. Now we can also set up a text letting people know that the record was saved. And then while we're at it and by the way, I'm using the application context here, not just this, because we are inside of this scope. Otherwise, if you just use this, you can run into errors. You see, this will not work. So we need to pass in the application context and then I can clear the entry. So you see if I enter Dennis here and some email address and I add the record, you see that those two fields are being emptied after. So let's just do that real quick by using binding dot added text name. And let's use the tax property which could be unavailable as well and clear it and now do the same thing with our email. So here, email ID, text clear. Okay. And then if those two are not not empty, so if either of those two and three feel so added text is empty, which means the variables that we have here are going to be empty. Then we can just display a toast. Saying email or name cannot be blank or name or email cannot be blank. Okay. Now, before we can test this, let's actually execute this ad record method at the point when we click on the button. So we need to make sure that we implement the button. Otherwise, of course, nothing will work. Okay. And now we need to call this add record method once we click on the button. Right. So let's do that. But the problem will be we will see in a second that we don't have any doubt that we can pass to it, at least not in the format that we needed yet. So let's use our binding button, add set and click listener here in order to get started. So here we can then call the ad record and we can pass an employee down. But where do we get the employee now from? Well, that's the thing. We don't have it yet, so we need to pass an employee down to our ADD record method. So here to do add call add record with employee Dow. Okay. So how do we set up this employee down? Therefore, we're going to create an application class and initialize the database. So let's go ahead and do that real quick here. New. And in class, I'm going to call this one employee app. And it will just be a class and it will inherit from application. So it will be an application class now and here inside of it we can create a DB. Lazily. Using our employee database to get instance. And we need to pass this, which means this employee app application. So now we have this database that we can use. For every application class, we must declare the app that we have set up in the Android manifest as well. So let's go over to the Android manifest and I know it gets more and more tricky, but that's just how it's done. So now we can add the Android name property and use our employee app here with a dot at the beginning here. So dot employee app. And now it will use our employee app which we can now set up in our main activity. So here let's set up our employee dot using our employee app. So employee DAO will be with our application as employee app. This one here dot get its database object and use the employee down, which is this abstract function that we created inside of our room database. All right. So what is going on here? Because this one line, there is so much stuff going on in one line. Let me go over to the activity. So what we need is we need an employee Dow to add a record. Right. Otherwise, we cannot store the data because we need to use the Dow in order to call its insert method, which needs an employee entity, which we do have, but we don't have the Dow itself yet. So we need to have access to the Dow. In order to get access to the Dow, we need to set up an application which we just did here, an application class, and then we can set up the database in there to now have our database ready. So here we are creating it lazily, which means that it loads the needed value to our variable whenever it is needed. So not directly, but only when it's needed. So that's the point about this lazy here. And the database will then be the instance of our employee database that we have because we can only have one instance as we have covered in the last video. So we have this companion object where we had this instance get instance method, which is a singleton that only exists once in the entire application. So there's only one instance of this instance object at once. So there's only one database in the entire application. So now we can go ahead and get this instance by passing this as the application. So because the get instance needs a context and we're passing the application context as the context, and we need to set up the application accordingly inside of our application tag of our Android manifest. So not inside of an activity or anything because we are not using the database only in this activity, we're using the database in the entire application and now in the main activity, we can go ahead and get the Dow from the database. So we need to get the application in order to get this DB object that we just created, which gets us the database. And then we can get its employee DOL, which is this abstract function. So the database that we're creating is our employee database, which is a room database which we had set up. So. At this point, we have our dowel and we can pass the doll. So let's call the ad record method and pass the employee down to it. So now our application will know which doll to use in order to use the insert method, which will take an employee entity with a name and an email and will store that in our application. So quite a bit of stuff that you need to build around it, quite a bit of boilerplate code. But the thing is this is quite safe. So this is a safe approach. You have to set it up once, but then from now on it's going to be significantly easier. So once you wrap your head around this, it's just going to be significantly easier because now you can just use the doll that you have there and you can call its methods because the doll, if you look at it, it has all of the methods that we need for crewed operations. So create. Read. Update and delete. Okay. So we have all the crude operations here and now let's actually run the application because what we will see is that here in this app inspection here, there is a tab at the bottom and this app inspection has a database inspector. So now if I run the application and add an entry here, so let me add my self again then is with then is add email or info dot com. And now let's add the record and you will see that the database inspector has made the change. So it added this employee database and it has this employee table. Once you click into it, you will see the entries. And let me make it a little bigger here. You see we have the ID one, which is Dennis with the email ID of Dennis at Infocom. So now let's try this again with Frank and Frank at tank dot com. We add the records. And the record will be added to our database. Let me refresh it. We see here, too. So it is to Frank, Frank, a tank dot com is in our database as well. So this is inside of our employee database, inside of the employee table. So everything now is set up. We're using our employee database. We're using the employee entity which contains the employee table. So that's exactly the name that you see here. So this is where we define the table name and now you see the table name. So in here with all of its columns. So these are the properties and you see this is the primary key which contains this key here as well. So if you look at it, you can see here primary key, which was the integer, which we call the ID, and then we have the name and we have the email, but the email internally was called email ID. So if you look at it, you see like in the table it's called email ID even though the name that we are using for our objects and to pass data to our objects and stuff is going to be email. So in the code we're using email, but the actual column name is email ID as you see here. So we overrode that using this at column info where we passed a different name. So this is pretty much how you can set it up. You can also see, by the way, that here this is an integer, this is a text property. So in the database internally it uses different names for the different data types that we have. So it doesn't use INT uses integer like a full name, it doesn't use string, it uses text. So SQL uses different wording here. So that's pretty much it for how to add a record. But in the next video we're going to finalize the application because this is just going to be the beginning of it. As you see, it's quite a long video already, so see you in the next one.