Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: 7 Minute Workout App Android 12 Version
Lecture: Displaying the Recyclerview with the Exercise Numbers
----------------------------------------

We'll come back. And this video, we're going to set up the recycler view so that it actually uses the adapter that we had set up. So you can see here we have those 12 exercises here at the bottom, even though they will not be highlighted as the selected one, for example, as of yet. But we're going to implement that later on. For now, it's important to just display the recycle view. So that's what we're going to do in this video. So let's get back to our project and inside of our exercise activity. We will need to create an adapter object of our exercise status adapter that we had set up. So let's do just that private var exercise adapter and it will be a exercise status adapter object which will be available for now and I'm going to assign null to it. So this is basically just the adapter that we created in the last video. It's our own class that we created and now we can create objects from it, which we just did. Now we can use this adapter to assign to our recycle view as an adapter. So where would be a good spot to do that? Well, I would say in the create method, but as the create method is something that we don't want to overload, let's create a new method which will take care just of displaying this recycled view at the bottom. So let's create a private function called setup exercise. Status recycler. Phew. Okay, so this recycler view now will take care of all of the good stuff, which means, first of all, we will set the recycle use layout manager. So here are V exercise status dot layout manager. And this is, by the way, our exercise that is recycler view, which is this item directly inside of our user interface. And now we can assign a layout manager property to it. So the layout manager property allows you to make such changes as whether you want to this to be a grid view where items are positioned like in a grid or whether you want to position items next to each other. Then the layout manager should be a horizontal linear layout manager. If you want to put them on top of each other, it would be a vertical linear layout manager and so forth. So let's use a linear layout manager object here which needs to have a context. And I'm going to pass this as the exercise activity should be the context of this layout manager, and then I can use the different settings. So you see we have horizontal, vertical and invalid offsets. So I want to use horizontal because I want my items to be positioned next to each other and then you can say whether it should be displayed in a reverse order. And I'm going to say false, so I don't want to have a reverse layout here. So set that to false if you don't want it to be reverse. This is just a linear layout manager. For example, there are grid layout managers, for example, as well. So you could, for example, create a grid layout manager here and check what it requires. It requires the context and then disband, count the orientation and reverse layout. So I would use this third option here. So if you wanted to display it in a grid format, which I don't want to do, I want to use the linear layout manager, which allows me to put them next to each other. But just to give you an idea of what you can use as your layout managers. Okay? And now I need to assign a value to my exercise adapter because I want to assign it as the adapter for my RV exercise status. So you always need to do two things for that recycle view that you have as an XML. Bridget. Okay, so you need to assign a layout manager and you need to assign, well, the layout manager property and you need to assign a value to the adapter property of that right recycler view. And yeah, let's go ahead and prepare the adapter so that we can assign it. And this will be our exercise status adapter and it needs a list of exercises. So I'm just going to pass our exercise list at this point. Now if I force unwrap it, it's really important that this method set up exercise status recycler view will not be called in the create method before we have assigned the exercise list. So I'm just going to call it at the very bottom here. Now, of course, the alternative would be to double check if the list isn't empty and only then create this object. But by following this structure where we get the list first and then we call this method, we're good to go. That's why we can focus on rapid. But this is something we have to be careful of, right? Just so you're aware, this could go wrong if you don't have the right order of your code. Okay, so now I just need to use my recycler view exercise status. Adapter. I just need to assign the exercise adapter to the exercise status respective use adapter. So let's just do that exercise adapter and that will be pretty much it. So now let's go ahead and test this. And let's see whether it will display that recycler view. As we wanted, so not as we want it, but it displays it. All right. So there we are, you see. Get ready for jumping jacks. And at this place, our recycler view here at the bottom. Now, I want it to be centered. And how I can achieve that is by going over to my exercises. And also let me jump out of it, because I don't want to have sound in the background and make sure that the start and end of are the parents. So here lay out constraint, end off and start off. This will make sure that our item is centered so our recycler view will now be centered. Let's rerun it and see if that's actually the case. So. Not yet. Let me check what else could be the issue here? And I believe we could fix it by also just wrapping the content in terms of width. That should do the trick because just adding start and end off will not be enough. And let's see, there we are. So now it's wrapping its content and it's being dragged to the sides, but it's also being pulled to the bottom and it has while it is underneath our jumping jacks. So that's pretty much what I wanted to have in terms of the recycler view behavior. Okay. So let's summarize what's going on here. So we have this recycled view here at the bottom, which is basically just this recycled view XML widget. So at the very bottom of our activity exercise, we have this widget, this recycle view, and the recycle view needs to know how items are positioned. So it needs to know about the linear order layout manager. It doesn't have to be a linear layout manager, but that's what we are using because we want to put items next to each other. As you can see here, they are not on top of each other but next to each other and it needs an adapter. So we created this linear layout manager here in order to assign it. So this one in order to assign it to our recycle view. And then we need to create an adapter in order to assign it to the adapter property of the recycle view. So this recycle view needs to have an adapter. And now in order to assign an adapter, we of course need to prepare an adapter, which is what we did. So we created this adapter which needs to have an array list or a list in general, doesn't have to be ArrayList. It could be another type of list, but it needs to have a list of items that it's supposed to display. And in our case, it's going to be a list of exercise model items. So which are just our exercises. Every single exercise has an ID and name and so forth. And now we are just saying that our adapter should inherit from recycle view adapter and here we're passing our own view holder. So we're saying use the view holder from the exercise status adapter for your type. So we're saying, okay, we create our own view holder class inside of our exercise status adapter class and we pass in our binding object, which is the item exercise that is binding. And then we can also use the binding in order to use recycle view view holder, which is just a normal view holder. And we pass our binding route, which means we are passing our recycler view by item itself, and then we can now access its item, so we can access that text view item, which is this item exercise status, which is this text view item. So this text view that we created an extra XML file for item exercise status. So this is how an individual item should look like. So now we have to implement those three methods to get item count is the one where we just say, how many items do we have inside of this items? Proper array list. So how many different elements are or exercises are inside of this list of exercises? Then we say what we want to do for every single binding of a view holder. So for every single one of those little circles and what we're saying is just assign the ID of the number of the exercise to the circle. So to the text of the circle, to be more precise, because this TV item has a text property that we're now overriding. So we're setting the text of this TV item where we are while binding it, in our view. Holder Before, right, we're just assigning our ID to it of the model, which is our exercise model, which is basically our exercise. And now we're just assigning it here and in the create view holder there, we need to return our own view holder that we just created. At least that's our approach to it. And we are passing something of type binding to the view holder. What is the binding that we're passing where the binding object is our item exercise status binding, which we get through using view binding and we inflate. And therefore we need to pass a layout inflator and we get that from the parents context. So the parent is the view group and then we need to pass in the View group itself as well as say whether we want to attach that to the parent or not. And now. At this point, we can assign this adapter that we just created to our recycler view. So we do that inside of our exercise activity. So set up exercise status recycler view is how I called the function and let we call it correctly the recycler view like so. So I had a typo in there and now this will basically do the trick of assigning everything as setting the layout that we wanted in our case that items are positioned horizontally, which you can see here clearly. Okay. So that's it for this video. And the next one, we're going to see how we can display at which exercise we currently are. Because, you see, I'm at high knees running in place, which is we could check that out item number or is it triceps? This it's number nine. So I would like to make sure that nine is going to have a different background. So see you in the next video.