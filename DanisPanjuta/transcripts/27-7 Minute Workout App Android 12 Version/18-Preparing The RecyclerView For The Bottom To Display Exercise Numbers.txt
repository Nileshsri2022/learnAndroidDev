Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: 7 Minute Workout App Android 12 Version
Lecture: Preparing The RecyclerView For The Bottom To Display Exercise Numbers
----------------------------------------

Welcome back. In the next couple of videos, we are going to set up a little progression bar here at the bottom, which allows us to see at which exercise we currently are. And this is something that I wanted to do dynamically. So it shouldn't be something we have to manually change if we want to change the amount of exercises that we have. It should really be done dynamically. So what we're going to use for that is going to be a recycler view. And that's why we looked at recycler views because they are great to basically have lists, right, and display lists. So in order to work with it, I will need to prepare it and therefore I'm going to have a new XML file as well as I will need to make a change to my activity. Exercise XML. So let's first of all, look at how one particular item is going to look like. So I'm going to call this one item exercise status. And this item exercise status will not be a constraint layout, but it will just have a text view in it. So text view like so. And this text view will need to use namespaces because here xml and SE Android which will be http. And then the rest android. This one here. And then we'll need another namespace. And let me put it into a different line. And by the way, let's close this text you like so. And we need to add the tools namespace here. And width and height. So I'm going to set that to 25 DPI. I'm going to give it an ID of text view item. I'm going to add a little bit of margin of one density pixels towards all directions. I will create a background which will be a draw able. So let me create this drobo create resource file. Now let's look at what this is. This is just going to be a shape with a solid. Content and the solid content will just have a color which will be our. Primary Grey. I think this colour primary. This grey colour. Yes. And then or actually no I want to make it lighter. So let's use something like C for C for C for all right. Which will be this light grey colour and the shape should have a shape of oval. So here shape. Oval. And we've seen that multiple times. Right. But that's what I want to have as the background for my individual item. So an individual item will basically just be a number because we want to know at which number of exercises we are. And currently, if you look at it, we well, let me make sure that my apps are closed because what we have right now is that we don't display the exercises, we don't know what which exercise we're at. So we're at jumping jacks, but is jumping jacks, number one. Number two, number three, we don't know that. And we could, of course, also just enter or display the number out of total. But we're going to add a recycler view which will make it look a lot nicer. So now I have the background for my exercise. I want it to be gravitated so the content of it to be gravitated, I will have a little bit of padding so that this circle is going to look a little bigger. You can see it here. It's just this circle and I'm going to have our dark grey colour as the text colour. The text size will be 14 SP the text style will be bold and the text that I want to display is going to be a one. So you see, this is basically going to be how one individual item is going to look like and now they should be next to each other. So I want to have 12 of these items next to each other. And usually how you would use a recycler view is like a list view that you can scroll through, right? So something that you would use, for example in, uh, in Facebook or so, right, where you have this big scroll view. But you can use recycled views also to display multiple different items next to each other if you want to do that dynamically as it is what we want to do. All right. Now, let's set up a recycle view in our activity exercise because it should basically be at the very bottom. So here underneath my very bottom frame layout, underneath this one here, which contains this linear layout inside of it, I'm going to set up a recycler view, which is this Android X recycle view widget. It needs a width. So I'm going to say to say it should be matching the parent and it should only rep the content, so it shouldn't be higher as it requires. All right, then it will need an ID so that we can access it. And I'm going to call it RV for recycler view exercise status. Then it needs to have constraints and margin. So this should actually be an ID. So at I'd like so RV exercise status and there's a plus that that's missing and then we can go ahead and add the layout margin towards all directions, which I'm going to set to five density pixels. And then I need to add a constraint. So top to bottom of the f l exercise view. So it should be underneath our frame layout exercise view, which is this frame layout here. So now you will see that we now have this item which is underneath it. Okay. And then. It's going to be underneath, but it should also be on top of the parent. So the bottom should be at the bottom of the parent, so to speak. So bottom at bottom of parent. Otherwise, you saw that it would just disappear. All right. So this is how a recycle view generally looks like. And now what we want to happen is that it, of course, will not display it like a recyclable view like this, but it should be displaying the data in a horizontal fashion. So right next to each other. And now we will also need to get rid of this bottom to bottom off because for our friends out, by the way, for this full exercise view, because otherwise it would overlap. So. Now. We should be good to go, even though it doesn't look great here. But it's all right because it will be fine once we get into the actual application and going to use it. Okay, so this was just a setup. Now we can go ahead and use the recycle view and therefore we will need to create an adapter and so forth. So let's do that in the next video.