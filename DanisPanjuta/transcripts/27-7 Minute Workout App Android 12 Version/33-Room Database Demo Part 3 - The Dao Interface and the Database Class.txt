Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: 7 Minute Workout App Android 12 Version
Lecture: Room Database Demo Part 3 - The Dao Interface and the Database Class
----------------------------------------

Welcome back. In this video, we are going to set up the Dow interface and the database class. Therefore, let's start by creating the Dow interface. The Dow interface is basically an interface file that we need to create in our project that has the Dow keyword annotated to it. So let's go ahead and add it to our project by simply creating a new. Scotland file class and here select interface call it employee. Dull and dull stands for data annotation. And dull is short for data access object. So your DAO must be an interface and it should be annotated with the Dao keyword. So at dao here. Again. This is from Android X Room, so you need to import that as well for it to be usable inside of the DAO. We now define the functions in it to be used for querying the database. There are available annotations already simplified for frequent operations, which we are going to use. So let's get started with the first one. The first one will be, if we want to store data, we need to use that at insert keyword and here with a capital I at insert from Android X room. And then we need to make it a suspend function using the code routine class because this is an operation that will take quite some time or relatively speaking will take some time. And this must not be done on the main thread. It has to be done on a background thread which is achievable by using code routines. So suspend fun insert is how I'm going to call this function. And what it will do is it will store an employee entity in our database. So now we need to do the same thing with an update function. So here update from room as well. This will also be a suspend function and I'm going to call this one update. And what should it update? Well, an employee entry. So employee entity. Then the next thing needs to be done with the delete function as well. So we now know how to insert, how to update. Now let's delete. And this will also be a suspend function I'm going to call delete and we need to pass the employee entity that we want to delete. So this will permanently delete the entry from our database. So now we need to have a function to retrieve the employees. And there are two different ways. One of them is to retrieve all employees that we have in our database, and the other one is to only get a particular one where we have the ID that we want to delete. So let's go ahead and use that query keyword here. This is also from Android X Room, another annotation. And you see at this point it just imports the room namespace entirety. So here for the query in brackets, you can define what the query should be and here you can enter SQL code. So something like select star from and then the table that you want to use. So in our case this would be, for example, the employee table. You need to use these quotation parentheses here and you see that the ID so Android studio directly gave me the table that it finds. So it found our employee entity table, which was this employee table. Now we of course, need to create the function and this will not be a suspend function, even though it will also be a function that will run in the background. So I'm going to call this one fetch all and blue G's. Okay. Like so. And this will return a flow. And it's going to be. This one here. So the flow from code routines, not the Java utility or anything like that, you see there are a bunch of different flows, but we're talking about this flow interface and it will be a list of employee entities that we are going to get. Like so. And now you might wonder, what the heck is this flow? Well, a flow is part of the routine class used to hold values that can always change at runtime. That's because it automatically emits a value. More like a live update. With flow. All you need is to collect the value from the variable or method without needing to always repeat codes to update the user interface. The collect method keeps emitting data as it changes, and that's really, really cool because otherwise you would always have to notify your recycler view that data has changed and all that kind of stuff. Now you don't have to do that. The flow takes care of that. There are other ways or methods you can use to listen to a flow depending on your needs. For example, collect latest. Just like the name would make you expected. It returns the last value from an update and forgets the previous ones. Then with collected indexed, you can get an index of an element with its value. With combine, you can transform flows and return its values automatically when they change. There's a lot that flow can do in our code with so many utility methods, but these are the basics that we need to get starting using it. So this will select START, which means all entries from this table. And then this is the function that we need to call in order to get all the entries, and it will return a flow which is part of the code routine class. Just like the suspend keyword, it runs the operation on a different thread, but with an additional feature of omitting updates automatically when they occur. And then we have a final one which will be getting us a specific entry. So we can also use this query and I'm going to call this function fetch employee by ID. Okay. And I need to pass the ID that I want to specifically fetch and I'm going to select start from. And here I need to define where the ID is equal to our ID and that's how you can achieve that. So it's going to be equal to this ID that we are passing. So basically this will allow us to get an individual employee by the uniquely identifiable ID. So basically the row where the ID column fits with the ID that we pass to the function call. And it also returns a flow, but this time not a list of employee entity, but an individual employee entity object. So just one row, that's the idea behind this such employee by ID. Because for any given ID there is only one entry. Okay. So now we need to create a database and that means an abstract class of type room database. So let's go ahead and do that. And is there an abstract class? Doesn't seem like it. I'm just going to use class here. I'm going to call this one employee data base. Like so. Okay. So this will be an abstract class that will inherit from the room database class here. So room database, this one here from KCTCS. And what you need to define here is some additional information about this database. So you need to define the version and the entities that you want to use. So here you add the add database annotation and you need to import a database. You see, it's this one here, and then you can enter the entities that you want to use. So this can be an array. In our case it will just be one entry and that is going to be our employee entity, which is a class. So we need to add colon, colon class and the version name. So let's say this is version one of our database. If we were to update our database, meaning, for example, we change the amount of properties that we have in the table, then we have to change the version and then we would have to go into data migration, which would make it super difficult in some cases, because let's say you need the new property in your project for it to work, but you don't have it for most users, then you would have to auto generate it and so forth. So this would go to far from this video. So for our purposes, we are just going to dump the entire database and restart it, so to speak. So whenever we make changes we're going to delete it and then start from scratch, which is not something you can do with an existing database. So just so you know, you can of course do it, but you shouldn't. All right, so now we need to connect the database to our DAO. So let's create an abstract function called Employee Dao, and this will return our employee Dao. So this employee dialogue class that we created this interface, better said, it's not really a class. So then we need to define a companion object which allows us to add functions on the employee database class. For example, classes can call employee database, get instance context to instantiate a new employee database. So let's create a companion object here. And here I'm going to create a private, for instance. Which will keep an employee. Database instance. And this will be null by default. And we're going to set it later. So this will keep a reference to any database returned via get instance. This will help us to avoid repeatedly initializing the database, which is expensive in terms of performance. The value of a volatile variable will never be cashed and all rights and rights will be done to and from the main memory. It means that changes made by one thread to share data are visible to other threads also. And we need to add the add volatile keyword here. So this will be a volatile variable. Then we need to have a heap of function to get the database. So if a database has already been retrieved, the previous database will be returned. Otherwise, we're going to create a new database. This function is going to be thread safe and college should cache the results for multiple database calls to avoid overhead. This is an example of a simple singleton pattern that takes another singleton as an argument in Kotlin. So if you want to learn more about singletons, I would recommend for you to check out the Wikipedia page. So the singleton pattern is a software design pattern that restricts the instantiation of a class to one single instance. This is useful when exactly one object is needed to coordinate actions across the system. So you don't want to have multiple different instances of the same object. Therefore, you use the singleton pattern where if the object already exists, delete the object and create a new instance of the object or skip the creation of the object if you have the object already. So there are different ways of handling this. So what we're going to do here is we're going to create get instance with the context. But we need to pass the context. Right. And let's import context here with the employee database. So it's going to return an employee database and here we need to synchronize it. So we used the synchronized method, we need to pass the lock to it, which will be this, and then we can execute some code. So here multiple threads can ask for the database at the same time and to ensure we only initialize it once by using this synchronized function, only one thread may enter a synchronized block at a time, so that's why we're using it. So now we need to copy the current value of instance to a local variable so Kotlin can smartcast. So smartcast is only available to local variables. Therefore, let's create a local variable called instance, which will be our instance that we have created here, our NPC database object, and then we're checking if instance is null. So at the beginning it is null, but then if it was created before, it will not be null anymore. So if it is null, let's create a new entry. So we're saying instance is room dot database builder. So we're building the database with the context and we're going to use the application context. Therefore then we need to pass the employee database. So employee database, class Java and then we need to say which database we want to use. And we called our database employee database. Okay. So this will be the database name. And now. Let's call fallback to destructive migration and then build it. Like so. So what's going to happen here is that it wipes and rebuilds instead of migrating if no migration object exists. So migration is not part of this lesson, as I said. So if you want to know more about migrations, check out this article. Understanding migrations with room from Android developers under medium that com so quite advanced topic and Florida Mountain Rescue has written an article here I would recommend for you to check it out if you want to know more about it. So migrations. The idea behind that is really if you are making changes to your data base, you will need to migrate your old database to the new one or the old version to the new one. So this would go too far for this course. So now at this point, we can assign the instance to our instance object. So the instance that we just created will be assigned to our instance global object here and now. Of course, our get instance method needs to get an instance. So here we return the instance and let me see if I return it at the right spot. I believe I have to go a little further in like so. Okay. So that will be our employee data base class, where we basically just return the instance of our database. I have talked a lot about some details and have gone into a lot more detail than necessary, I believe. But this will give you maybe a little bit of a better idea. What's going on at the same time might confuse you a little more if you just want to be able to use it, just follow along. If you want to dig deeper into it, you will definitely have to check out the documentation for it. But for just using the room database with its basic functionalities, what you're going to learn is going to suffice. So let's go over to the next video where we're going to finalize this project, and then finally we'll be able to store data, delete data, and update data in our SQL database.