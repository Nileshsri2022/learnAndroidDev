Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: 7 Minute Workout App Android 12 Version
Lecture: FInalizing the BMI Calcutator with the us system
----------------------------------------

Welcome back. In this video, we're going to implement calculating the BMI using US units, which means we will need to use a different formula. As you see, dividing weight in pounds by height and inches squared, multiplying by conversion factor of 703. So yeah, it's quite complex. The metric system makes it a lot easier, but let's just implement it. You can try to do this yourself, of course, but basically speaking, you need to get the data first. You need to validate whether those other texts are empty or not. If they're not empty, then do the calculation and then displays. Okay, so let's go over to our code. And something that would be very easy would be to validate basically we need to do the same thing as we did here, but with the other added text. So with three edit texts now instead of just to add a text. So I'm just going to paste it in here and go over it real quickly. So I'm validating the US units now. This is a new method I'm going to set a valid to true by default. Then I'm using this one statement when this added text metric unit weight, which is this one here at the top is empty or this one is empty or this one is empty. In either case I'm going to set is valid to false and I'm going to return as valid. So it's going to be true by default. But then depending on whether one of them is empty, we're going to override it. Okay. So now we need to do the calculations. So let's go ahead and create this new function that I'm going to call calculate units. And first I need to check which view is currently visible. So if it's the matrix view, I just want to do the code that we did once we clicked on the button, I believe. Let me check. So we have this. Yes, click if validate metric units here. That's what we're doing. So let me take all of that and put it into this method. Where we calculate. The units, how we would calculate BMI, basically. Okay. So we validate if the metric units is true or not, and only then are we setting up all of those values, right? And then we calculate the BMI and we display the BMI result. Otherwise we're displaying that there is nothing to see. So that's one thing. But then if we are in the other view, which means we are not in the metrics unit view but in the US units view, then we want to validate once again if the validate US unit. So here where the US units is true and only then do we want to get the height, value and feet. So here US unit height value FT which will be a string. We get that from our edit text US metric units height ft and we convert its text property value into a string. Then we do the same thing with the height value. So I'm going to do that with both the height values. So we're doing it with both values here with the FT and Inch. And then we're also getting the weight. So here the weight value would be a float. We're getting it as US metric unit weight, text to string and then we're converting it into a float. So here, let me put it like this. And put this into the next line. I think that we should be good to go. Okay. So. Like this. Okay, now we have the values. The problem is, we now need to calculate the height value. And the height value is in inches and also in feet. But one feet or one foot is a 12 of an inch, so we can calculate it using the following following formula. So we say the height value is going to be the US unit value in inch, which is a string. We convert it into a float plus the US unit height value in feet, which is in a string and we convert it into a float as well and we multiply it with 12. Okay, so this will give us the height value because in order to calculate the BMI, we need to, as you have seen, multiply with 703 and what do we need to multiply while the weight divided by the height squared so the height value times the height value will give us the height value squared and we take the weight value and divide it by this. And then we take the 703 value to get the BMI. So whoever came up with this, congratulations. So then we can now take this BMI and display the BMI results with that BMI. And if we do not have the valid US results, we can display this else block that we have here as well, which will. Just give us a toast. Text. Like so which says please enter, build a values. Okay, so now we just need to call this method, calculate units. Once we click on the calculate button. So all the way in our on create, we have this button calculate units set on click listener. Here, we need to calculate the units. So I believe this should be it. Let's just try this. So let's go over to the BMI calculator weight. Which would be 156, in my case. Fee. Uh, heights. I'm not very tall. What is it? It's 5.5, four, five, five and six, five, five. Something like this. Calculate them. We see we have a similar BMI than I had in the metric units. So if I enter 71 with 169, then we see, well, it's it's not exactly the same, more or less. Right? So that's the thing with Ft and H and everything. But yeah, basically, you see our application works now flawlessly for both ways. And now instead of just having the BMI calculated here to bottom, I would like to also add a history of my exercises. So that's what we're going to do in the next video.