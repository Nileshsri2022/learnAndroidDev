Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: 7 Minute Workout App Android 12 Version
Lecture: Room Database Demo Part 2 - Setting up the Data Class - Room Entity
----------------------------------------

All right, so let's get started building this room demo. Therefore, we're going to first set up the entity, and the entity is basically the data class that we're going to use here, and we could call it employee, for example. So we have the employee name and employee email ID and of course, you could store a bunch more information about your employee, but we're going to simplify it for now. And you can then easily extend your data class to incorporate all of the properties that you want to store for a given object. All right, so let's create an empty activity project for that. And I'm just going to call this one room demo. You can, of course, call it differently if you want to. Everything else can stay. Default API 21 is what we're going to use here and let's click finish. Okay. So once our project is loaded, let's go over to our Build Gradle file here. We need to go to the app file. Therefore under plugins we need to add another plugin, the cotton annotation processor. And in order to add it, you just need to add ID and then in parentheses and single parentheses here specifically Kotlin minus K apt, which stands for Kotlin annotation processor. So let's add that. And then I also need to make sure that I use the right dependencies here. So I need to add the room lifecycle dependencies and the code on extensions for code routines, support with the room. And finally, the extensions for code routines, support with activities. So let's add all of those. Therefore, I'm going to go ahead and create a new variable here called room version. And in my case, I'm going to use the version 2.3.0 and then the activity version of one. .3.1. And now let's go ahead and use those. So in order to use the room and lifecycle dependencies, I'm going to add the Android X Room Room runtime with the version that we have defined up here and adding the Kotlin annotation processor here with this capped, then we need to add the extensions for code routine support with room. So here room room Kate X standing for Kotlin extensions with the room version that we want to use. And then we also need to add the Kotlin extensions for code routine support with activities. So here Android X activity activity Kate X, once we have added all of those, we also need to make sure that we use view binding. Therefore inside of this Android tag further down here under Cottle options, I'm going to make sure that I activate the view binding feature. So build features. And here in curly brackets, I add view binding. True. So this statement is also going to be required if you want to use view binding throughout your project, which is best practice. And I highly recommend. Okay. Once we have added all of those, let's sync our project and this will basically just allow us to use a bunch of classes that which will help us to store data permanently using room. Okay. So now that all of that is done and our build gradle is ready to go. Let's go ahead and add an entity. Therefore, I'm going to create a new class here. Scotland class file and this will be a data class. Which I'm going to call employee. Entity. So this data class employee entity will be using the IT entity keyword which comes from the room library here, Android export room. You can see that here and now. It added the import for us. Okay. So we can also directly specify the name of the table that we want to use here. So I can specify the table name. By now giving it a name. I'm going to call this one employee minus table. Okay. So this will now be the table name in your database and you can define it with whatever you want, but it really makes sense to use something similar to the name that you have here. So in my case, employee okay, now let's define the different properties that we want to have inside of our entity. And these are going to be the different columns, so to speak, that you will have in your data table. So you have this table with a bunch of different columns, and every single entry is going to be a separate row. So what are going to be the columns that I'm going to require? Well, first of all, I want to have an ID, so every single employee must have an ID of type integer. And I'm going to set that to zero. And this, by the way, will also be the primary key. The primary key should be auto generated. So I'm going to auto generate it. I'm going to say that the auto generates setting should be set to true. So what is this going to do for us? Well, it's going to make sure that every single entry that we have in our table is going to be unique. For example, let's say you have two john dos in your table and you have no additional information about them. Maybe they even have the same email address, which would be highly unlikely, but let's say that was the case. Then you would need to have a way to distinguish them from each other. So those two entries, if you have nothing else about them, would be the same entry and this could screw up your entire system. So therefore you have to have something that is uniquely identifiable and that is your ID and the ID will be unique for every single user. And when you have something like that, you use it as your primary key. So you have only one primary key for your table. So this will be unique. So every single user will get a new ID. And what automatically happens here is when you create a new employee entity object. So each time you add a new entry to your database into your table, employee table, it will auto increment the value. Let's finish this employee entity. And we also want to store the name of a given user. And by the way, we should use a comma to separate the entries here so the name will be empty by default and we can overwrite that. And then I'm going to add an email here as well, which will be of type string and also an empty string. Now if you want to give this a different name internally, you can add something called a column info. And here you just need to say which name you want to use here for. So I'm going to use the name email ID for my email here. So this will be the name that we are using when working with the employee entity objects and internally it will be stored as email ID, so the column will have the name of email ID. So you could have gone ahead and created such a column ID for every single property that you have in your table and feel free to do so if you feel like it. Also, you can add more information if you want to. Such as? Such as birthday or the gender to size whatever information you want to store about your employees. Maybe the job position, if you want to store that, that wouldn't be a bad idea, I guess. And the phone number, if you want to call them, or maybe even an address, even though with the address, I would go a little further, I would create an extra entry. So an extra entity with such an address where the address contains different kinds of information. So for example, the country they live in, the state potentially the zip code, the street name. But we're going to keep it simple for now. So you see, we basically used the add annotation here in order to define some additional details. And by the way, if you don't override the name of a column, it will by default get the name that you gave the property. So here this property will name will be named this property name, which means this column name will be ID and this one will not be email, but it will be email ID as we have overridden it. Okay. So that is the first step on our way. So now we have prepared the entity class. In the next video we're going to set up the dowa interface and the database class. So see you there.