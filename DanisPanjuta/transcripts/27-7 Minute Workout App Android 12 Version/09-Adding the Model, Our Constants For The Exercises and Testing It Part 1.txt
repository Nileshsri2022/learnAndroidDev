Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: 7 Minute Workout App Android 12 Version
Lecture: Adding the Model, Our Constants For The Exercises and Testing It Part 1
----------------------------------------

Welcome back. In this video, we are going to implement the exercises and we're going to add them to our application. Therefore, we will actually need to videos. One was not going to be enough, but we're going to set up at least the model for our exercises. And then we're going to also look into adding this image to our exercise view, as you see here, and adding all of the different exercises that we have. So it's going to be a list of a bunch of exercises. So let's get started. Therefore, what I'm going to start with are going to be the actual troubles, because we are going to require all of those images for different exercises. So I'm just going to copy them in here. You can, of course, go ahead and create your own images or just use your own images from a third party website that you want to use. So it should be 12 images in the end at least. That's going to be the case for me. So I have this launch the plank and so forth. So all of those different exercises, my wife created those very basic images for me and we're going to use them for our project. Okay, so the next thing that we're going to require is going to be a model because we need to know how every single exercise is going to look like. So let's go ahead and create a new class and it will be Kotlin class here. And I'm going to call this one exercise model. You could have also just called it exercise, of course, but I'm just going to call it exercise model because it is a model and this exercise model will need a bunch of data. So first of all, I want it to have an ID, so power private ID, which will be an integer. Then we need to have the name of the exercise. So private bar name which will be a string. We need to have the image that we want to display. So our image, which will be an integer, then we want to know whether we completed the exercise. So private bar is completed, which will be a boolean. And then finally whether it is the selected exercise. So is this a lected. All right, this will also be a boolean. Okay. So these are all the values that we need for our model. And now I'm going to add the functions so the getters and setters for all of those properties. So for a getter looks like this fun. Get ID and it will return the integer. Which is this integer here and what it will return is going to be the actual ID return ID. So that's the function. And then we have the set ID, which will set the integer value that is passed to be that this ID will be the ID that is passed. So this ID is the global ID, which is this one here, and this ID here is just going to be the value that is passed, the parameter that will then be used to to assign the ID to the ID of the exercise that we're currently looking at. And now I just need to add those for all of the other properties as well. So it's just going to look like this. Set name, get name, get image. Set image get is completed, set is completed, get is selected and set is selected all with the same structure as I've just explained. Okay, so this will be the model and now we will be able to access those functions directly to modify our exercise model. This is just a cleaner way to do it. You make your variables private, but you access them with public methods. So this is giving us control over those variables and how we want to secure them and make them accessible. Okay, then I'm going to require a constant companion object. So let's go ahead and create a new Kotlin class file. And you can directly create an object here and call it constants like. So this object will then be accessible throughout the entire project. We have seen that before. We've used it and. What I'm going to have here is a function that will give me access to the default exercise list, and this will be an array list of type exercise model. All right. So this one here. So what is it that we want to do once we call this method? Well, we want to return an array list of exercise model, so let's go ahead and create such a list. So I'm going to call this one exercise a list, which will be an array list of exercise model, of course, because otherwise I couldn't return what is required. So exercise model like. So now I have initialized this exercise list and I will need to return that exercise list accordingly. So now our function is functional, but of course the exercise list will be empty in the sense that it will be an array list, but the array list will not have any content, so it will not be null, but it will not have any values assigned to it. So now what we can do is we can create those exercises. And how does an exercise look like? Well, you just need to go over to your exercise model and you will know. So you go to the exercise model and you see you need to pass an ID, you need to pass in a name and image and boolean as well as another boolean. So let's just do exactly that. Let's create the jumping jacks, for example, as the first exercise. And you use the exercise model object and now you pass in the values. So as we saw, this exercise model needs an ID, a name, an image and boolean and another boolean. So let's add the ID, this one will be ID one, then we add the name, that's one will be jumping jacks and then the image which is an integer. And the cool thing is if we want to access items from our Drobo folder, we just need to go ahead and use its integer value by using adorable dot and here it's going to be. ICI underscore jumping jacks. And then we just need to add two more points, and that is whether the exercise is already completed. So false here because it's not completed and whether it's selected so false here as well. So both of them will be false and that is going to be fine for our jumping jacks. Now, we just need to make sure that we add this jumping jacks exercise to our exercise list. So we go ahead and say Add and use our jumping jacks. So now our exercise list will not be empty, but it will have one exercise. Of course we want to add more exercises because one is not going to do it. So let's go ahead and add all of the other exercises as well. And I'm just going to copy them in here because I believe that you are capable of adding the exercises yourself. You can call them the way you want, but I would recommend to use the same names as we have here in the images. So push up would be an exercise, push up in rotation, side plank and so forth. So let me add those here and see if I have an error. That is usually because I'm missing images. So let me add those images here as well as well as well I need to add the wall set and the abdominal crunch. So I have forgotten to paste those images in here as well. So let's skip this one. Let's see, maybe I have some wrong spelling or it wasn't found at first, but now it seems that I have no more errors. So just quickly, you see, we have the wall set where it has the ID two, then it has the name of wall set. It's using the wall set image and is completed and selected is always set to false by default. So we could also in the exercise model predefined that those values will be set to false by default. So here, for example, is equal false and is equal false. Then those would become optional. Or we could make them optional. Then we wouldn't have to manually set them each time in our constants here. But it's going to be fine either way. All right. You don't have to do it this way. So now we're manually setting them. And then you see we have the abdominal crunch. We have step up onto chair. Squats. Tricep dip on chair. I hate this one. It's super tough, really painful exercise, in my opinion. And, uh, plank also since too much fun, but that's fine. Yeah. So basically we have all of those exercises with the draw balls and each time that we created an exercise model object, we also add it to our exercise list. So now whenever we call this function, it will return the exercise list to us. So now in our exercise activity, we can get the exercises. All right, so let's set up the exercises first. So private var exercise list, because we are going to require it, right. And it's an array list of type exercise model like so. And I'm going to set it as a label and going to assign null to it. And then we need to know at which exercise we currently are. So I'm going to say where current exercise position is going to be at minus one by default. So we're going to start at minus one. And then once we increment this value, we're going to be starting at zero. And zero is the first index of an array list, which is going to really make it easier for us. Okay. Now that we have that, we can get this exercise list and initialize it in our on create method. So let's go ahead and do that. We can do it in here. So let's just say exercise list. It's going to be constant dot. Default exercise list. And this method, of course, will now return the default exercise list, which will be the exercise list with all of the exercises. So you see there is a pattern here with constants. It's really cool. You can just create a well, we basically did the same thing with our quiz app, right, if you recall. So that really helps us out here. So now that we have the exercise list, we can go ahead and use this exercise list to set the image resource for every single exercise. And before we do that, where we set up the image view, I would just like to add one thing and that is here in our unfinished. Let's use this variable that we just created, this current exercise position and let's increment it. So once the set rest progress bar is done, we can say, okay, we are at the next exercise. And the exercise view currently doesn't have anything to do with image views. So we will need to make changes to the XML file where we add another image to the XML file and then we or image view and then we assign this image for of the exercise to this image view and display the exercise name instead of saying exercise name, which we hardcoded as of now, I believe. So it says get ready for. Where did we put it? It seems like we had set this in our code and our activity code somewhere higher here. There it is. So instead of displaying exercise name, we of course want to get the exercise name from our exercise lists current exercise position, but more on that in the next video.