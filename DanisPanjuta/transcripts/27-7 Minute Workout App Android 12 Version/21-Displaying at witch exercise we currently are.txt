Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: 7 Minute Workout App Android 12 Version
Lecture: Displaying at witch exercise we currently are
----------------------------------------

Welcome back. In this video, the goal is to display at which exercise we currently are at and therefore we are just going to change the background colour of the item that we're currently at, so of the exercise that we're currently at. So let's go ahead and implement that. Therefore, we will need to go over to our exercise status adapter. So here what we're doing is we're binding the view holder, right? So we are binding the view holder item, which is currently just a text. But I also want to update the current item and I want to make sure that the completed item in the UI is also changing the colour, and that's pretty much it. So let's go ahead and set all of that up. So here I'm going to check when. The model dot get is selected is true. Then do something. Then we have the case where model dot get is completed, is set to try to do something and then we have the else. BLOCK. Where? Either of them or both of them are inactive. So we have this else block as well. So now we need to think of what do we want to do in each of the cases? So the thing that I want to change is I want to change the text view items, background property. So what is this text view item again? It's this binding text view. So this thing here, this little text and this text view, I want to set its background to not be item circular color gray background, but I want to use maybe a white background instead, for example. So how do we do that? Well. We can set the background by using the context compat. We have done that before, but it has been a while, so let me go through it again. So we use this context compat and we get a draw. So we need to pass in the context. And we cannot just say this because this doesn't exist. So we're not in the context of a class at this point. We need to get the context of the class so we get it from the item view dot context. The item view is the view of the recycler view view holder. So it's from the internal class and from there we can get the context. So we just say, okay, where is this item being displayed? And then we get this context and then we can now use the trouble. So our dot draw able and then I'm going to use item and I think we don't have it yet. So let me just create another one and I'm going to call it, so let me duplicate. Which one would it be? I think there's great color background. Let me duplicate it. So copy. And paste and we call it circular. Thin color accent border OC. And now there I just want it to be oval and the color should be white. So here we can just use white as the color. But then I also want to have a stroke surrounding it. So let me add a stroke with a width of five. 0.5 super thin. 0.5 density pixels. And our excellent color. So this green color. So we just want to have a green border surrounding it. Maybe 0.5 is too little one DPI maybe is a little more visible. So basically a super thin stroke around the color. So I want to use this item now. So that was the item thin color accent border. So this is the trouble that we just created and I want to assign this as the background of my text view item. So of this number that we see here. All right. And. I'm going to change the text color of it as well. So all the dots, TV item, dot set, text color, there's a method for that and not key here, but color and here we just can use colored dots, parse color and not colors, but color dot parse color from a string. And I'm just going to hardcoded here. You could of course just use an internal color as well, but I'm going to be fine like this and then I can just copy this code and put it into get is completed and there I just need to set it to well I'm going to set it to the color accent background to the green background. So we know that these are the ones that we have completed. So what we will have is green for the ones that we completed, white for the ones that we are currently at and gray for the ones that we haven't finished yet. So what is the block then? Well, there I'm going to set it to be gray. So here background will be. The item. Circular color. Gray background. Which is the default that we have already. And as a text, we can use this same text color here as well. And by the way, when we're using this green color, I think having this gray color is not as nice as just having a white background. So here I'm going to use triple or six times F in order to get this white background. Okay. So now we get it based on the model value, so based on the value of the individual item, but we're never actually triggering this change. So now we need to find a good spot where to trigger this change, and that would be in our exercise activity. So what do you think? What would be a good point to change the background color of the ones that we finished and change the one that we're currently at? I would say once we're done with an exercise and once we start with the next one, so we have our timer where we set up the exercise view, for example, and where we set up our rest. Timers. So those two. So let's start with the Set Rest Progress Bar. So here in our rest timer on finish, before we set up our exercise view, what we should do is we should. Check for the current item that we're at. So exercise list at the current exercise position set selected. I said if selected, we should set that to true. So when we are at the rest progress bar, we set the current exercise to true. And at the same time, we need to make sure that our exercise adapter is being notified about that. So we need to call notify data set changed because if we don't do that, it will not change the recycler view's appearance or anything because it doesn't know that the data has changed because what we have now is an entirely data driven recycler view. We're not changing the appearance of the recycler view manually, but what we're doing is we're saying we change the data and recycler view itself should change the appearance based on the data. So now we're well notifying the adapter that the data has changed. So the adapter will run on bind view holder once again and also on create view order. So basically we'll call these methods once again and it will then see. Okay, well, I'm just going to use the latest data or the latest property values for those data. So I'm going to see, is the item selected so the current exercise or is the exercise completed? Is the case the case? So either way, it's going to do the right thing now. So that is once we set the rest progress bar. But then once we are done and we just copy that once we are done with an exercise which is in the moment where we have the timer for our exercise progress bar. So this one here, then we need to do the same thing but in the other way around. So we need to go ahead and on finish here. So once we are inside of well, actually in the unfinished in general, we don't care if we are inside of a particular exercise item or not. So here we're going to set is selected to false. And set is completed to true. So let me copy this line here and set is completed. I'm going to set that to true because now the current exercise that I was at is completed. And then. We are setting up the rest view which internally will then. Which then internally is going to share increment by one. So once the exercise is done, it's going to prep for the next one. So let's run it and see if this is actually the case. So there we are, jumping jacks. Let's see if it's going to change it once it's finished. So on it should get the next exercise, which is going to be two. And you see it's the current one is set to white and the next one is still gray. So now after those 30 seconds are over, it will take the number two will make it white and make this one green because we finished it already. And if we look at our exercise status adapter, we set it to be green. So let's see this transition where it goes from white to green. There we are. It goes to green, but this one doesn't go to white yet because it only goes to white one. Once it's done with the relaxing phase or the rest phase and you see now it goes to the white color. Perfect. So now you see how you can create such a recycled view. Very customized, really? Like the appearance is all custom made, and you could change it to whatever you like. But generally speaking, it now has this appearance that we wanted where we now see which exercises we have already done, which one we are currently ads, and then we see how many are still to be done for us. So now it will turn green once we're done with wall sit. And I hope you did the wall set exercise. And now the next one will be to do 30 seconds of pushups. So maybe you can do 30 pushups in that time. Well, depends, of course, on how clean you you do them. Okay. So that's it for this video. See you in the next one.