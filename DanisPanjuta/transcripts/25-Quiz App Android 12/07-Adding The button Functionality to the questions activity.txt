Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Quiz App Android 12
Lecture: Adding The button Functionality to the questions activity
----------------------------------------

Welcome back. The results that I want to achieve in this video is going to be that we have this little border around our buttons once we click on them. And this is going to require quite some effort because we also need to reset the old buttons or the other buttons that we had selected before that. So in order to get to this point, we of course also need to create this, well, disappearance for each button. So we need to set the style for the button that we clicked on. And therefore, let's go ahead and create a new file. And what I'm going to do is I'm going to. Duplicate this file or I'm going to create a new but we'll really learn from this file or we'll be pretty much the same. So let's create a new trouble. And let me call this one selected option border. Background So. BG And we can click okay, here and now let's go out to our default options border. Let's copy that. And now go over to the selected options border and really just replace this. The only thing that I will need to change here is going to be the color that I'm using and the color that I want to use for the stroke will be the color primary primary. So the default color primary. So it will be this purple color. And you see well, you barely see it here, but there is this purple border that is now surrounding our entire screen. If I make it five, ten, 60 pixels, you can probably see it better or even 25, ten, 30 pixels. Now you see that we have this purple border now, but I just want it to be one density pixel. So it's going to be super thin. The rest stays pretty much the same. So we just have this shape which is rectangular. We have the stroke, which is this fine line that surrounds it. And we have this solid color which makes the background white as well as this little radius, which makes the corners brown that all right. So that's our selected options border begin BG more background and I want to be able to set it based on what we clicked on. So in order to make things clickable, I will need to add something to our app compat activity and that will be that we add an on click listener here like so, so view on click listener. And you see I separated this so our quiz questions activity is still inheriting from app compatibility, but it's also implementing the view on Click Listener. So now it's going to allow us to click on to items inside of it and we can now implement the members. So you can see we get an error here. What we can do is we can now hover over it and implement the on click method. So you see there is this override fun on click where we get a view and then we can do something in here. So currently there's nothing implemented yet, but we're going to change that in this video. So first of all, we have this big code where we assign the questions, where we get the current position and so forth. We will need to change that because I don't want everything to be in the on create method. We will need to separate it clearly and then it will be a lot easier to read our code and understand what's up in the code and so forth. Therefore, what I will need to create are going to be a couple of private variables up here. Which are going to be, for example, the current position. So I will create it outside of any method so that I can use it in all methods inside of this class. So I'm creating this global variable and current position so that we get the current position. And you see, we used the current position here. We're going to have to override that and then we will need another variable and questions list. So this one will be the array list of questions. And we need to make it knowable at this point because we cannot assign it at the outside of any method. We need to assign it inside of the create method and then we can go ahead and set up the selected option position integer because we need to know which option was selected. And by default, I'm just going to say the selected option was zero and we're going to override that accordingly once we click on to any of them. So this will be position to this will be position zero and so forth. So we need to know which one was selected because our questions, they have this option one, option two, option three, option four. But then the correct answer is an integer. And we need to compare whether the selected option is the same as the correct answer for us to know whether we were right with our selected answer or not. Okay. So now let's go ahead and create a method that will take care pretty much of all of this stuff here. So now we can extract it, refactor into a function. And let's give the function a name of set question. So this will then say private fund set question and you see it just cut out everything from it and created this private function called set question. So that's how you could do it. You could just right click the part that you want to extract and then go to refactor and function. You can also use the shortcut here control alt m and this will then create the function at the same time call this function. Okay, so now we need to adjust this set question method a little bit or this function. So first of all, I'm not going to get the question this way here. So first of all, we will need to get the questions actually inside of the create method. So that's one point where I was too quick with the copy cut thing out. So the questions list now will be a global variable as we saw. So we need to use this global variable that we had created up here. So we need to assign it at one point, which we now do. We assigned the questions list and now we can use it throughout our project, which means we can use it in the This Questions method, but we can also use it in other questions where we need it. So we do need it inside of the set questions because we need to know which question we currently are at. So we have this question which gets it from the questions list and let me get rid of this lock stuff because we are not going to use the log entries. This was just for our testing purposes. And now, because the questions list is a knowable, I need to force unwrap it here with two exclamation marks because I'm sure that the questions list is not going to be empty at this point. Also, the current position we need to use the HM current position so our global variable and assign the value to it. Even though we did already assign a value to it, we set it as one so we can actually get rid of this set thing statement here as well. But we need to replace our current position at the other spots where we used it. So now this will pretty much do the trick and it will be fine. Now the current position has to be. Now there is something that I have not assigned yet, and that is the button. So here we have the fun view by ID for all of the text views and so forth, but the submit button never has been created. So let's go ahead and create a variable here, private var and this was button submit and this will be a button knowledgeable. So let's import button here with alt enter. Just will add the widget here and now. We can use this button inside of our on create to find it. So Button Submit will be found by its id r dot id dot button submit, which is the submit button. Of course this one here at the bottom. So I want to change this to finish once we are at the last entry of our options. Okay. So let's go back to this questions activity. And when we set the questions, we get the questions here, we then set everything right, we set the image, the progress bar and so forth. But what I'm also going to do here is I'm going to set the button based on the current position to either display, submit or finish. Okay? So we can just go ahead and say here if the current position. Is equal. The questions list dot size. And here I need to use the double exclamation mark because it's ineligible. So if that's the case, if we are at the last entry of our questions because we have nine questions, right. In our particular case. So if we are at the last question, I want to set the text of our button. Submit. So because it's available, I need to add the question mark here but and submit or how did I call it? I call it button submit like so. Dot text to be equal to finish and otherwise I want to set the text to be next or submit. Okay. I know what I want to do is once we click on to any of the views, I want to set all the views that weren't selected to be grey and or have this default border, this grey border, and the one that is selected to be purple. So I'm going to create this new method that I'm going to call default options view. So a function to set the default options view when the new position is loaded or when the answer is re selected. So it's going to reset basically this the colors of the selected answers and then we can just set it to be gray. So private fun default options view. So this method will take care of that and therefore I need to know the options that I have and I get those by having the array list of text views. So the options will be an array list of text views, which are my four text views that I can select from right in this case, this text with one text you to text to three text for our text view option one to option to option three. Option four. So let's use option one here and add that now because option one is ineligible. I need to use this letter statement and I can now add this option to the options. So this TV, this text view, which is what we would need here for our options array list, I can now add it at the index zero. I'm going to add it and it is the actual text view. So this TV option one, that's the lambda expression that we are using when we are using lit and now we need to do the same thing with the other options as well. So here option to. Like so, and it will be at index one and then option three and option four accordingly. So let me add those. Option three, question marked outlet with option add at index to you with it. And then the same with option four. And now what I want to do with all of those options is I'm going to go through them with the for loop. So I'm going to go through each option inside of options. And I'm going to set the colour of this particular option, which means of this text view. So set text color. And here I'm going to use a pass. Well, I can use a given color that is already created, but I can also use a color that I'm defining here manually. So I'm going to define this color like so. So this will be the color that I'm going to use. And by the way, just to see how this options or default options view method is actually working, let's call it just for this purpose, for the testing purpose inside of our on create method and see how this is actually working. So instead of using this color, let me just use red, which will be F, F and then a bunch of zeros. So are GB red, green, blue. And let me comment this line out. Let's test this. Let's just see whether all of our options will now have a red text. So start and you see all of the options now have a red text. So that's basically what I'm doing here. So this default options view is just going to reset all of those options that we have and we can select from. Okay. So that's really the purpose of what I'm doing here and here. I'm setting the text color, but I'm also going to set the typeface. So I'm going to set the typeface to be default again and because I want to change it once it's selected. So here typeface is another property of the option and I'm going to use the typeface default setting, then I will choose a background for it. So the background for this button, so for this text view, it's not a button really, but it's a text view should be gotten from our context compat. And this is something that is some feature that we haven't used yet before I believe. So where we get a draw all from the context compat and to draw all that I want to use. Well first of all I need to pass in the context and then I can say where the file is that I want to use and I want to use the default options port of BG. So this is going to be the our trouble. So our stands for resources, then double is the folder and then it's this default options border. BG And by the way, just so we can test it with this other AR that trouble dot and we called it a thing selected selected options border. So let's just run the code real quick with this selected options border and you will see that now all of our selections have this or text. We just have this grayed out border surrounding them. So that's what we're basically doing here. We're setting it back to its default. The thing is, if we run it now, we won't see the difference because we're just basically setting it to what it was by default anyways. But what I want to do is I want to call this method each time that we have selected another option, I want to see the first set everything back and then set up the new color for the selected option. So therefore what we will need to have is of course going to be a method that will allow us to display the selected option. So fun selected option view and here we will need a text view. So we need to know which text we need to change and then we need to know which option it was. So which option number selected option num which will be of type integer. So once we have selected an option view, so we select it one of those buttons. We clicked on them. Basically, I first want to call this default options view method, which will set every single button to its normal state. And I'm going to set our global variable, which stores, which option we have selected. So I'm selected option position to the selected option NUM, which is going to be the variable that will be passed to this function that we have created here. And then I'm going to set the text view. Well, it's color, so set text color and I'm going to use a purple color. So here, set text color to be this color, parse color with this number, the color string that I'm using, you could just copy that if you want to use the same and the typeface. So set type face, I want to set the text view typeface. To be bold. So typeface bold, which will just make the selected option be bold. So if we move over to the final solution, you can see here the selected option is bold and that is what I'm achieving with this line here. And that is also why we had to add this option typeface, typeface default here as well in our default options view method. Otherwise it would just stay bold after we have clicked on it and everything would be bold at one point, super bold text views. And then of course I also need to set the background property and here we can just copy this code here basically. So. And we get rid of this part. So context, compact, get trouble. And here we used a selected option border background that we had created. So this purple border background and now we of course just need to add the on click event for every single option that we have here. So here in the click event. But let me change that to view on that happy with the name P zero that was given me from to me by default. So I'm going to say when the view that was clicked on and we are really talking about views, we're not talking about buttons even though buttons are our views also, but our views are our text views. So when we're using the one clause here, when the AR dot ID was TV option one, so when we clicked on option one, I want to call this method that we just created. So this selected option view. With our text view as the view that we passing. So unfortunately, TV option one is ineligible, so we need to add this outlet here. So it's a knowledgeable question mark, but let and then we can do the select option view with it and the selected option number being one. So what's happening here? Well, we're calling this selected option view method, which is created, which will, as I said earlier, highlight the selected option. So it will change its appearance, but at the same time, it's also going to change the selected option number. So selected option position. And that's really important because we need to know if we selected the correct option or not. So that's what we do. And you see, we are passing it, which is the TV option one text view. So here it text you this grayed out stuff here that you can see here it's colon text view. So we are passing a text view, which is exactly what the selected option view requires because we want to then change that text view accordingly. And now we just need to do the same thing with option two and so forth. So TV underscore option two and here TV option to. And the rest stays pretty much the same except for the selected option. Now of course, needs to be true as well, and now we need to do the same thing with the other options that we have here. So TV option three as well as TV option four and be careful with the selected option number. So when your copy and pasting always make sure that you are changing every single property correctly or every single piece of code correctly. Otherwise you will be confused when suddenly your code doesn't work properly. Okay. Now, in order to make this run, we need to add on click listeners to all of our options that we want to be clickable. So the text views that we want to be clickable. So here we can just go ahead and say TV Option one. Should have an on click listener set on click listener. And we saw this set on Click Listener before where we would implement it manually like this, right, for every single listener. But if we were to do that, then we would create a bunch of code inside of our create, which is really not what we should do, because otherwise the create method itself would be hundreds of lines and we don't want that. So what we're going to do instead is we're going to pass this as the on click listener. So because TV Option one. Is a Nullarbor. I need to add a question mark here. So now we need to do the same thing with the other text view options text the option to three and four. And what I'm doing here is I'm just saying that this, which means the class questions or quiz questions activity should be the on click listener. And you see it is an on click listener. So we are passing it as on click listener and then we're executing this on click method that we have defined here, or which is an override method. So here we are then saying what should happen once every single item is selected? So we are basically exporting the on click what should happen once we click on it by setting the on click listener here, by making sure we have this view on Click Listener here, and by implementing the on click method with the different options where we say what should happen once we select either of those? Now this by itself is good and all, but let's also add to submit button to it because the submit button doesn't have an on click listener and this is actually going to be the most important one. Right? So here, let me pass this here as well and get rid of this empty space. Now, of course, we also need to have the on click event for the button. So what should happen with the submit button once we click on it? Well, what I'm going to do is I'm going to set it up here, button submit, but I'm not going to do anything here to do. Implement button submit because this would be a way along long video if I do it now. It's a really long code that we need to do here, but at least we have prepared it. So now let's run this and see if we can actually select any given button. You can see now we can select the text views and we are resetting them as well. Now of course, the submit button doesn't do anything, but that's because we haven't implemented it yet. So you see there is a lot going on here. We needed to set up a lot of code, but now you know how to modify your UI items. And this particular case, the text use in your code directly interactively. All right. So that's it for this video. See you in the next one.