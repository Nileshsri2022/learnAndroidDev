Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 15 - Navigation and Menus with a Music App
Lecture: Day 15 - Making the drawer appear
----------------------------------------

Welcome back. In this video we're going to set up our draw item as we have started in the last video. So therefore we have this scaffold that we started with. And in that we have the top bar which then we created the top bar of. And we have these icons. But then this scaffold here can have multiple parameters. Right. So we did set up the scaffold state. So let's add that scaffold state in here as a scaffold state. And if I get the error that it doesn't accept my scaffold state then it has to do with my scaffold not being the right scaffold. So let me make sure that I get the right scaffold here. I don't want to use the material three scaffold. I want to use the default material scaffold here. So now I can use my scaffold state that I created earlier. This one here. Okay. And it should be the lower s scaffold state. Otherwise we're going to run into an error. So that's one thing. Then we have this thing called draw content. So our scaffold. Which takes care of the UI of the entire page you see here. If I hover over it, it has this thing called draw gestures, shape, elevation, content, color, and so forth. And what we're interested in is the draw content itself. So the draw content we can now define with those curly brackets once again. And what I want to have as the draw content is going to be a lazy column. So basically this item here can have as many entries as possible as required. So in our case it's just three. But let's say you want to have like 100 in there and people can scroll through it. Then you would use a lazy column for that. So what I'm going to say is I'm not going to define the content in here. I'm going to define the content like so. And now this lazy column will have a modifier dot padding in, well let's say all direction 16 density pixels. We can play around with that later on if we want to. And now this will give us a list of items inside of this lazy column. And now I can say how I want these items to be. And these items are going to be screen in draw or screens in draw. Which is a list that we need to create because we need to define which items we want to display inside of our draw, which is what we're going to do inside of this screen class. So actually just in the file itself. So I'm going to create. A variable here. Wall screens and draw. In draw which will be a list of and then this will just use our screen dot draw screen dot. And we have these three list items. So the draw screen account the subscription as well as add account. All right. So let's use subscription here and add account. Okay, so now we are using the screens draw screens screens. So which are these objects that I create in the screen in or screens in draw. And now I can use these screens in draw by importing them. And these are going to be basically the items that I want to create draw items off. All right. So now. I can take these individual items like so and I'm going to call them item and now I can. Make draw items out of them so I can get the item itself, which will just be the item. This one here. And. I will also need to know whether it is selected or not. Where do I get this information from? Whether the drawer item is selected or not? We will know if it's selected based on its route. So is the route, for example. Account, then we know account is selected is the root subscription. Then we know the subscription is selected. So we can just say that this will be the current route. And here I'm going to compare item dot d route. Now where do I get this current route from? Well, the thing is, we need to create a new variable here. While current route, which will be taking well, which will get that information from the Backstack. So nav Backstack entry destination route. Now where do I get that knife back? Stack entry from which I now get from my controller. So I need to have a controller. So here Val nav, let's actually use this name here. Val nav bag stack entry is going to get from my controller dot current pack stack entry as state. So where do I get this controller from. Well that's going to be a nav controller that we need to set up. So val controller dot is going to be of type nav controller and it will be of type remember or it will be initialized with remember nav controller okay. Now we just need to import this current Backstack entry as state and also get value. So it added the get value function as well as the remember entry as state. Okay. So this is a lot of setup right. But basically this here these three lines. Allow us to find out on which view we currently are. Or you could say which screen or which page and so forth. And we're setting the current route to be whatever the nav Backstack entry says. We. I have as a destinations route. Now we're setting the current route and comparing. So we're saying this selected which is a boolean. We're setting that to either be true if the current route is the items the route, and otherwise we're setting that to false. Now for the screens in Draw to work we need to import lazy item scope reading. So here it says implement function lazy list scope items. This allows me then to access the items. We've seen that before. And now the arrows disappear. And now we can actually go ahead and set up our draw item. Here we are going to launch the draw item. And by well we do that by using scaffold state draw state dot close. This is for example to close the the draw when we click next to it or away from it or onto an item. So you see we click on an item and it just goes away. Or we click next to it. It also goes away. So this draw state close will basically close our draw. Now we're going to check if item the route is going to be add account. Then we want to open dialogue and otherwise. We're just going to set our controller to be well to navigate to item dot D route. And then the title. The value will be whatever the item the title is. So now the title is something that I want to have is basically what is displayed here. So you see here I click on account. This title up here should be changed whenever I click on it. So you see subscription it changes I click on add account. This pop up appears. So you see when the Add Account button is clicked this pop up should appear. That's what I want to do here. And in the other options, I just want to navigate to that other page and set the title. Now we don't have the title yet, so the title is basically just going to be a state holder for the current screen title. So here, while title will be a remember. Mutable state of, and here it should hold the current screen's title. Now we need to import. Remember, for this to work we import. Remember function. So I'm going to add a line here saying change that to current screen dot title. So we will need to set up this current screen thing I don't want to do all at once. We will need to set up our ViewModel before we can take the current screen title, but that's just something to to keep in mind. And basically now if we run our application, we should see our draw appear. There we are. We have our draw with the account. Well, that's still crashes the app obviously, because we have not implemented the onclick event properly yet. But yeah, we have our draw and we can click away from it. You can see that closes the draw. So yeah, that's pretty much it for this video. Now you know how to add such a draw. We will obviously need to do a lot more to make it fully functional. So see you in the next video.