Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 15 - Navigation and Menus with a Music App
Lecture: Day 15 -Making the DrawerItemsClickable -Setting up our Navigation and ViewModel
----------------------------------------

Welcome back. In this video we're going to take care of the view model because it's really in the way of everything. So it really will help us throughout the entire application. So it's just good to have it ready. And therefore let's go ahead and set it up. It's going to be quite simple. So let's just create this main view model class which will inherit from view model. And now what should it do. What do we want it to do. Well basically we just want it to hold the current screen. So that's pretty much it. We don't need anything else, just need to know. Private file current screen which will be a mutable state of screen which I'm going to set to a mutable state of our screen dot. And let me see. Well, draw a screen dot. Add a count will be the default for now. We're going to need to change that later on. So let's import mutable state. Now I'm going to have an Exposable variable current screen which will just be a mutable state of screen. And then we will get we'll have a get method which will just give us the current screen. And if we ever want to set the current screen, we're going to have the set method as well. Current screen which takes in a screen. Which then basically just overwrites the value of the current screen with whatever screen we are passing. Okay. And here I need to write it correctly like so. Okay, that's going to be our ViewModel. It really just holds the state of the screen that we're at. So are we on the main screen? Are we on the add account screen and so forth. Because now we can go back to our main view. And as I said, we want to set the current screen title accordingly. Therefore we're going to require our ViewModel. So let's go ahead and create our view model which will be of type main view model which will be a view model like so now we need to import ViewModel for that. And now we can use our ViewModel. Here inside of another variable. Remember variable which will hold the current screen. Okay. Current screen which will be remember view model dot current screen and not like so view model dot current screen dot value. Like so. And now we can use that current screen for the remember title. So instead of it always being an empty string or like an. Yeah. A string that is doesn't have any content. Then we're going to use a current screen dot title instead for our title. So now our title will be dynamic because our view model is taking care of the current screen, which is of type screen, mutable state screen, and the mutable state screen holds the title as well as the root. So now we don't have to manually do that anymore. It's going to be done by our view model. And now, in order to be able to click on and on any of those, we need to set up our navigation, because basically what we're doing is we're navigating to a different screen once we click on any of those. So let's set up our navigation. Therefore I'm going to well see do we have a navigation composable. Well we could create a separate file for this. I'm just going to put it in here. However into my main view I'm going to have a fun navigation which will need a nav controller as well as the ViewModel as well as the padding. So I'm going to call this one Padding values. Okay. So let's set those up. So here. We're going to have a Nav host which requires a nav controller. So let's pass in our nav controller. And I'm not going to use the one with a graph. Wait a second. So nav host I want to use the one with the start destination. Okay. So start destination as well as the builder. So the nav controller will be our nav controller. As a nav controller, so I'm basically casting it into a nav controller. The start destination should be. Well, actually, if we look at our screens, we only have our draw screens. We don't have any other screens yet, right? Yeah, let's keep that for now. So the start destination will be. Let me put that into a separate line. So screen dot draw screen dot add account dot route. And then the builder I can set it in here but I can also set it somewhere else. However I'm going to use the PD so the padding values and at the modifier. So modifier dot padding. PD. So why do I do that? Well, the thing is, I just want to get rid of this. PD that we need here inside of our padding values. So that's inside of the scaffold right. It gives us these padding values. And if we don't use them then it's going to complain. And that's what I want to get rid of by using this padding here. Now this nav controller, it gives me the option to use this nav graph builder. So I could use it as the builder property, but I also can use it as the content of the nav host so that I don't have to do that in the nav builder. And here I can have my different composables. So the different screens that we wanted to have, right. So screen Dot and we're going to have our draw screen dot account dot route. And this one will send us to the account view. Currently we don't have that. And then we have the composable. Which requires the screen dot draw. Screen dot subscription, dot root. And here again we would have the subscription screen. Now, why do we only have two here then? We have three options. Well, the thing is, when we click on add account, we don't want an open a screen to open, but we want to open a pop up. Okay. So now we can use our navigation composable inside of the content of our scaffold. So this is inside of this scaffold right here. So with the draw content. And then we have the actual content of the scaffold. And that's where I don't want to just display a text, I want to use the navigation for it. So here I'm going to pass our navigation controller as the controller, the ViewModel as the ViewModel and it as the PD. Okay. So it is going to be this padding values stuff that we get here. So if we now test our application, we will see that ad account is set by default, even though it's not really a page. We can go to the subscription page you see here. But when we try to go to the account page, it still crashes. So that's because we're not fully done with everything yet. We haven't set up the different pages yet that we're going to need, but what I'm going to do in the next video is going to add this ad account dialog, okay. So once we click on that I want the dialog to pop up. That's what we're going to do in the next video. So see you there.