Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 5 - Our First App - Unit Converter
Lecture: Day 5 - OutlinedTextField and Anonymous Functions
----------------------------------------

Welcome back. In this video we are going to look at how we can use text fields to enter text into our application. And there are different types of text input fields that we can use three different types actually. So let's get rid of this grid or this greeting stuff. And we can actually get rid of this greeting function composable in general. So the only thing that we are going to need from it is going to be this text composable. But we can set one up ourselves. So let's go ahead and get rid of this composable. We're not going to greet anyone with it. So we will just use our unit converter composable as our parent, composable, so to speak. So where we're going to put all of our user interface. So how can we actually make text visible? Well, the thing is I'm going to use my column to first of all make a text composable. And now the minimum requirement that a text composable has is the content of that text composable. So the text property, so to speak, and we need to pass the text in there. So I'm going to call this text unit converter okay. So what else do I want to have. Well I want to have that field where I can actually enter some text in. And that is something that where we can use either text fields. So there are three different composables that allow us to enter text fields. And all of them have slightly different properties and are just slightly different as to how they behave and how they look. There's the text field that you can see here. Then there is the basic text field that you can see here. And finally there is the outlined text field. And I'm going to use the outlined text field which you can see down here. And there are even two different versions of the outlined text field. So one needs just a string. The other one needs a text field value as the main value. And then there is a difference as to what needs to be passed when you want to execute an on value change. So what is an on value change? That is something that basically is something that should be executed the moment that we change the value. So what do we want to happen when the value inside of our outlined text field is changing? So once we enter something in there. So what? What do we want to happen when that happens when there is new text coming into this text field. So I'm going to start with a text here saying. Well, what did we have in our user interface. Let's look. So we say enter value should be the value of. It's not really the value of our text. It's really just a help as to what the user should enter. And we can even see what kind of help this is. So we have the UN value change the text styles here placeholder. That's what I was talking about or searching for. So it's basically the placeholder which just says okay, this is what we want to show to the user what they should enter okay. So visual transformation is supporting text. So boom boom boom okay. So what else do we need. Well as I said I created this function or I used this outline text field. And it needs a value and it needs UN value change. We get an error. Otherwise you see it just isn't happy with us using the outline text field without passing an on value change. So it needs to know what should happen when a value changed. And you know, we can just say nothing. So we just add the two brackets here. And this is where we could implement the logic of what should happen once the value changed okay. So once this changed we could execute something. But we can also just say all right you know what. At this point we don't want to implement it yet. So here goes. What should happen when the value of our outlined text field changes? Okay, that's pretty much what is inside of this these two curly brackets. And let me actually put that into a separate line so you can see it better okay. But now it is complaining because my outlined text field is not closing because I have that in here. So it became part of the comment because I cut it out this way. But basically it's the same thing as me just having two empty, well, an empty code execution piece. So what is this code execution piece? It's actually an anonymous function. What what is an anonymous function? Well, an anonymous function is basically a function that doesn't have a name, so we cannot call it, but we can still execute it. So it's a local final fun anonymous. And we pass a string to it and it returns a unit. So that's that's going to be a little bit too much. But that's what the onvalue change does. So we pass a string to it and then it returns a unit. What that means we will see once we actually use it a little more and execute stuff with it. So for now we are happy with it just doing nothing later on. We're not going to be happy with that. We want to actually make changes to it. Okay, so I'd say let me quickly just summarize what we learned in this video and basically run this code while I do so. We have this column, and inside of this column we have a row. But on top of that we have a text. And then we have an outline text view or text field. So let's look at what this looks like. So we have unit converter. And then we have our outlined text field. And let me actually go back to my settings and change it back to being white because I want it to be the default theme. So you're not confused. So you see unit converter is the text that we have here this one. And then we have this outlined text field that is this one here. And once I enter something here nothing changes. The user interface is not updating. Nothing happens. And that's what we are going to cover in the next video. But the cool thing is how we can actually execute some code when that happens is by using this on value change. So each time we change the value inside of our edit text here, inside of this text field, this outlined one which has this beautiful outline surrounding it. Then we can execute this anonymous function which doesn't have a name but still will behave like a function. All right, that's it for this video. See you in the next one.