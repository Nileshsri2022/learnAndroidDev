Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 5 - Our First App - Unit Converter
Lecture: Day 5 - Spacer vs Padding
----------------------------------------

Welcome back. In this video we're going to look at padding versus spacer elements in order to space our user interface accordingly. Because you can see it doesn't look too great I'd say that's because everything is so clamped together very close together. So we're going to look at the patter padding modifier and the spacer element. So the elements in our case don't have any spaces between each other and Jetpack Compose. When you want to add spaces between elements there are two primary ways. One is padding or the padding modifier, or introducing a spacer element. So an element that just spaces the thing. So what are the pros and cons of each? Well, the pros of using a padding modifier is the simplicity. So you just add a padding around your element with the padding modifier. And it's straightforward and quick, and you can apply it directly to the composable you want to space. So in example two our buttons we want to add some some padding. We can directly do that. The cons are that there is a limited reusability. So if you want to consistent spacing between multiple elements you need to apply the modifier individually every single time. So each button needs to have this padding. Each unit converter like each element would need this padding manually okay. Then there is a spacer element. So the beauty about the spacer element is the fact that it or the pro is the reusability. So the spacer element can be used to create consistent spacing between multiple elements without applying the modifier to each one separately. And it's clear intent. So using a spacer explicitly communicates your design intention, making your layout more self-explanatory. The cons, however, are. It's slightly more complex. It involves adding an additional composable to your layout, which may slightly increase code verbosity. So yeah, which one to choose? Well, the choice between using the padding modifier and the spacer element depends on your specific use case and design preferences. If you need fine grained control or only want spacing around a particular element, the padding modifier is convenient. If you want consistent spacing between multiple elements. The spacer element offers a cleaner, more reusable solution. So that being said, let's take the spacer option. We will add one underneath our text here. So we have our column unit converter. Then we have the text. And before the outlined text field that's where I'm going to use the spacer okay. So there's this composable that's called the spacer. And you need to pass a modifier. So we can just use the default modifier here that is over here the modifier dot. And then we can just say what height we want to have. So I want for example 16 dpi. So 16 dot dpi. And in order to use it we need to import the extension property called int dp. Okay, so a density pixel is just a pixel, but depends on the density of pixels of your screen. With this basically require well generates is the fact that you have different sizes of screens, right? And also different amounts of pixel on each screen. So some have a DPI of 440. Others have a DPI of 300. Others have like even less DPI. So older devices for example, usually are not that pixel dense. And that means that the same amount of pixels span over a bigger distance. So that means that UI items wouldn't be as aligned if you would just use pixels. But if you use density pixels, then suddenly items can be aligned okay, because they basically say, okay, you have this much space on your screen and take away or use this much space. So it's like it's like 16, 16 pixels, but. Really on the screen if you have a very high density pixel screen. So a lot of pixels, which means let's say like 1000 pixels in the wide area, then that would be more like 30 or 40 or 50 pixels. Okay. So it's not a fixed pixel value, but it is a relative pixel value. --, was that hard for me to explain. I need to find a better way to do that. So there we have spacer number one, and you can see that there is a little bit of space now between the unit converter and our text. So if I comment this out. You see, there is no space in between. So this spacer basically now has created this a little bit of distance between one element and the other. So between the text and the outline text field that's exactly what I wanted to achieve. Now we can use this spacer and put it underneath our outlined text field. And suddenly you can see we also have a distance between the outline text field and our buttons. It's a little difficult to see here, but let's run our application to see what this is going to look like. So we run our app here and we see that there is now a little distance that we didn't have before. And that's what our spacers allow us to do. Now we need one more spacer. And I would say that goes before the result. So here at the bottom before we get the result underneath those buttons. So that's a very easy way to just get the space in between. I really like that. Otherwise you could obviously also modify the. Here, for example the padding. So you could have a modifier equals modifier dot padding. And then you can add padding towards all directions. So let's say 6100 let's do 100 uh density pixel. So it's a whole lot. You can see now we have 100 density pixels distance here because we created a padding around our text okay. So that would just be another way to add distance. But we are using the spacer in this case. All right. That's it for this video. In the next one we are going to look at our drop down menu a little more.