Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 10 - Navigation in Android
Lecture: Day 10 - Implementing the Navigation and Passing Objects
----------------------------------------

Welcome back. In the last video we set up the screen and yeah, the sealed class, even with objects and stuff like that. So this might be a little confusing at first, but you will see how we are going to use it, and that's going to save you a lot of headaches later on. I just wanted to state that once again. Now let's go ahead and create an app that will take care of basically the app. So so far we just have this main activity that takes care of the app. But the main activity should be the entry point. But then it should directly run our app which then runs our screens. So the app is basically taking care of the navigation as we have seen before. So let's go ahead and create a new file which I'm going to call Recipe App. And. Let's do so. All right. So the recipe app will be a composable. And this composable will be called recipe app. It will require a Nav controller of type nav controller. And then inside of it I'm going to get the recipe recipe view model of type Mainviewmodel. And I'm going to default that with ViewModel. All right. And without initializer just the default ViewModel. And then we need the state. So the view state. Which I'm going to use by recipe ViewModel dot category state. Okay. So let's import that. Now what is this category state? If you look at our main view model, we had set up the main view model quite some time ago and it had taken care of this category state, which is a state of type recipe state. And it just takes care of the recipe that, well, the list of all of the recipes, basically the list in our case of the recipe categories. So now we want to use that in order to know what we need to pass. So which one the user clicked on for example to go from one screen to another. So our recipe app needs that information. And we'll use that view state to not only go over to the other screen, but also well to display the recipe screen. I'm going to use the set up the nav host now, which requires a nav controller. So we're going to pass the nav controller from here. And it requires a start location. So I'm going to get rid of this graph here I'm going to say start destination. And now we need to pass the start destination which I'm going to use screen dot recipe screen dot route. So what is this. The thing is I use this screen really just for this route property so that this one global static variable that will not be changed and so that I can access it and never run into problems. So basically here instead I could have just said the route should be whatever route name you want to give it. So for example a recipe screen, write recipe screen. But instead of doing that I'm using this here. To avoid running into errors. Okay, so this Nav host now also needs to execute some code. And by the way, I realized that my recipe app should not have a nav controller, but a nav host controller. So nav host controller. Okay, because that's what this nav host requires. It needs a nav host controller type so we can get rid of this nav controller. And now this nav host controller can take care of the two different composables. So we have the composable for our recipe screen and we have the one for our detail screen. So here we can just use the route to be the same thing as we had here. So screen recipe screen route. So it's just our way to making sure that we don't mistype or anything. And then now that we have the route we can say what the composable is that should be displayed in there. And in our case, it's going to be the recipe screen. Now the recipe screen needs to have the navigate to detail and I'm also going to make sure that it requires a state. Let me see here. So it needs the navigate to detail. But we're also going to require the view state. So instead of creating it in here we're going to pass it from our. Ray's recipe app. So let's get rid of this here. The view state and actually set it up in here where we're going to have view state of type main view model dot recipe state. Okay. So the recipe screen will now need the view state as well. And that will then be the same view state that we use here. So you see nothing else changed this. We don't need to set up the view model in here. We're going to take that care of that inside of our recipe app. So the screen should not take care of that. It's going to be the app itself. So here we have the view state. Now we can pass the view state as well as the navigation as well as the navigate to detail. So what I'm going to do here is I'm going to implement the navigate to detail code like so. But then we also need to have the view state in here. So I'm just going to pass the view state. So view state will be view state okay. This one here. So that's what our recipe screen needs because that's what we set up here right. We said hey we need the view state. Now you get the view state. And you need to now implement what navigate to detail should do. Because that's what our navigate to detail needs here. It needs the category. And then it will execute some code. Or we need to implement what it should execute. Now what should it execute. Well actually we want to move from one screen to another right. And we get the category here. You see we get it category. Well we're going to require this category and we get the category. The thing is now this will be a little more complicated what we're going to do here okay. So I'm going to say nav controller dot current Backstack entry dot saved state handle dot set. So these are all nobles. So that's why I'm asking setting the question mark in there. And I'm going to call this cat. And I'm going to pass it. Okay. And then I'm going to use this nav controller to navigate and navigate. Now I need to say to which screen or to which route I want to navigate. I want to navigate the screen dot detail screen dot route. Okay. Let's go over what just happened because a lot happens. So we let me actually paste that in here. I have a really good explanation of what happens here. This will be a comment. Okay, so this part is responsible for passing data from the current screen to the details screen. So this part here retrieves the current navigation Backstack entry which represents the current screen state with in the navigation graph. So we have this navigation. Basically we have a starting point. So the screen let's say the recipe screen. Then let's say we go to the detail category detail screen. On the category detail screen there might be another screen we click in there. So all of this flow is stored inside of this Backstack entry. Now the save state handle is a mechanism for passing data between different screens using the Jetpack Compose navigation. It provides a way to store and retrieve data that needs to be passed to the next screen. So then we have this set cat it. So this method sets a key value pair in the save state handle inside of here. The key of this key value pair is key cat and value it. So which is the category that we get from here okay. And which is the category that we get from the recipe screen if you recall. So here we pass this navigate to detail in there okay. And the key is used as a reference to retrieve the data on the detail screen. So basically here we're saying hey this is where we're storing the category. Now on the detail screen we need to retrieve this information. And here we're initiating the navigation to the detail screen. And it uses the nav controller to navigate the screen represented by screen dot details dot route which is inside of screens. It's this one here. So it takes this detail screen route. Now we can set up the detail screen. So here I'm going to have another composable and not in here but outside of here. So let me make sure that I'm at the right spot. So here I'm going to use composable. I'm going to set the route to be screen dot recipes. Well this is the detail screen dot route okay. So we get the detail screen. Now that will be the route. And then we will. And then we get the animated content scope but also the navigation backstrap backstack entry. So what we're going to do is we're going to use that Backstack entry to get the category. So here I'm going to say val category will be nav controller dot previous Backstack entry, which is another step added the current Backstack entry which is a nullable. And then we get the saved state handle which is again another label. And then we get the key. And the key that we want to get is going to be of type category. Or like the value will be of type category. So we just say get of type category. And then the key itself and the key is cat. And if that is null then we're just going to set the category. To be category with the details of an empty category ID, then an empty string. Category and an empty thumbnail, as well as an empty description. So what is this entire line? Let me put that into a separate line so it's more readable. So basically we're saying and this gets should be here okay. Like so. So this is just one line okay. This is still one line. We're saying the category we want to get that from our save state handle which was this thing here. So we saved it in there. And that was the current Backstack. But once we click and go to the next screen, we navigate to the next screen. We need to get the previous Backstack Backstack entry. So basically now that we moved to another screen, there is another component on that Backstack. But we want to have the old version of the Backstack, which is the previous Backstack entry. And from there we get this save state handle, which is basically like a container that we can set our variables in. And I'm just saying at the key pair or the at the key cat, I'm setting the category that I'm getting. Now I'm retrieving the category, and I'm saying the type of that thing that I'm retrieving is going to be of type category and the position where it is. So the key is cat. So this will be it basically and stored inside of category. And it is whatever we had here passed inside of the recipe screen, which when we create the recipe screen we will pass a category. So then. If cat is empty. We are creating an empty category, basically saying no ID, no string, no image, no description. All right. So that's basically what we're doing in here. And now we actually need to pass that category to the category detail screen. So let's just do that. And this will then be the actual composable which is our category detail screen. This one here. Which needed the category to be displayed on the detail screen. Okay, so a lot has gone on now. There are only two more things left to do. And then we're done with this, this navigation. So instead of the main activity I want to go to the recipe app, not the recipe screen, but I want to go to the recipe app, which needs a nav controller so we can create a nav controller inside of our set content here. So vile nav controller which will be of type. Remember nav controller okay, so we're setting up a nav controller and we can pass this nav controller to our recipe app. And if you recall our recipe app just needed the nav controller. Now from there it could take care of the navigation. Okay, so there's one last thing. As I said, I want to do and that will be serialization and deserialization. I'm going to implement it in this video and test the app, but I'm going to explain it in the next video. Now inside of our Build.gradle this one here, the module one, we have the application and Jetpack Kotlin Android. Now here we are going to add another plugin and it's called Kotlin Dash per parcel lies like so. And this should be inside of a parentheses like so. Okay let me sync that. So I synced it. And now this will allow us to parcel these objects okay. We're going to see what that means in the next video. But for now let's make sure that our category. Is going to be realizable at Parsley's. Like so far. And it's this one here. And then we need to make this a parcel label. Parcel label like so. And now let's run the app. And as I said, I'm going to explain it in depth in the next video. This video is long enough already. Okay so there's our app. Let's go to beef. And we see we get the details about beef. Now let's swipe back at chicken. We get the chicken details. Now let's swipe back again. And I believe what was it that had a very long details pork. Right. So here you can see we have this scroll view where we can actually scroll inside of texts. Okay. So our application works. And there was a lot of work involved for the navigation in here. But I'm going to go over one last thing in the next video. And that is this partial lies concept that I just implemented but didn't explain. So let's do that in the next video.