Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 10 - Navigation in Android
Lecture: Day 10 - Setting up the Routes with a Sealed Class
----------------------------------------

Welcome back. In this video we're going to take care of the navigation. And therefore I'm going to for now comment this recipe screen out because otherwise we constantly have this error in our main activity. And I don't want that. But let's go over to our model module app and make sure that we add the dependency for our navigation. Okay. And the dependency that we need to add is going to be this one here. So implementation Android X navigation navigation dash compose 2.7.4. That's the version that I'm going to use. And I would recommend you do the same. So then you need to sync your Gradle. And then once that is done we can keep going. Setting up the basically screen class that will take care of the different screens that we have in our application. For now, we just have two screens, right? We have the recipe screen and and the detail screen. But in the future, if you want to extend the application, it's really good to have all of your screens in one place. So what I'm going to do is I'm going to create a new Kotlin class, and I'm going to call this class screen. So what should this class screen do? Well it should take care of individual routes. So we have seen how routes work in our other project where we just would use route keywords and we would potentially run into errors. But what I want to have is I want to access my screens myself inside of the composable that will take care of navigation. So what I can do here is I can use the sealed keyword, so it is used when it is known in advance that a type will conform to one of the subclass types. So sealed classes ensure type safety by restricting types to be matched at compile time rather than runtime, so that means they will be matched already in the IDE. So inside of Android Studio, and not just after once we actually run the application. So this can make sure that we don't run into the problem where for example, we accidentally write the route name incorrectly when we want to call the route or pass the route or something like that. And that's what we make this sealed class screen for, because what I want to have is I want to have the different routes directly in here. So we pass the route of type string in here. And what I'm going to do is I'm going to create two objects. So one for the recipe screen which will be of type screen. And it will have a recipe screen as the root. Okay, now I'm going to do the same thing for my detail screen. So object detail screen screen. And here again the route will be detail screen. So this is just good practice to make sure that you're basically creating constant variables for each of those different screens that you have. But in our case we're going a step further and putting it as objects in using this screen class, the screen class. And we create directly objects inside of this screen class of screen class types, where we pass the recipe screen route name. And now we can use that route name later on again and again. Or basically this object again and again, which makes sure that we are using the right location. So to speak. Okay. Now let's go ahead and create a new file. And actually let's do that in the next video where we're going to set up the new file that will take care of the navigation itself.