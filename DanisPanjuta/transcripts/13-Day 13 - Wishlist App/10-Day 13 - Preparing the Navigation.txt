Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 13 - Wishlist App
Lecture: Day 13 - Preparing the Navigation
----------------------------------------

Welcome back. Next we want to navigate from one screen to another. Now we can obviously create the other screen straight away. But I'm going to focus on the navigation part first okay. So we need to have another class that will contain our different screens okay. So I'm going to create a new file class. And this time it's actually going to be a class which I'm going to call screen. And this class screen is going to contain just our route. So we've seen that before where we just had multiple different routes. And we just made sure that we have them directly in a clean format. So this class will be sealed so nobody can actually inherit from it. And then it's just going to have two objects for now, which is the home screen, which will be the screen with the home screen route. And then finally the add screen, which I will use for adding as well as for editing as well as for the detail. So we're going to simplify everything here. And this will be screen add underscore. Well as a string obviously add underscore screen. So these are going to be the two screens that I want to be able to use for my navigation. So as you recall that was step number one. And then the step number two was to set up a navigation composable okay. So I'm going to do just that. Let me add a new file class here as well. This one will just be a file. And I'm going to call this one navigation. Now this navigation is going to be a composable which contains our Nav host and the nav controller. So here fun navigation which will require a ViewModel. And I'm going to use our wish view model which for now will be just a view model. So we will need to set up the ViewModel as it always is. And we also need to pass the nav controller which will be of type nav controller. And I'm going to pass the Remember nav controller here by default. So now the wish view model. Let's actually set that up real quick. We will need that either way. So I'm going to create a new class here. Kotlin file class. Class wish. View model. Like so. And for now, our class that doesn't need to do much except for inheriting from ViewModel. Like. So okay, now going back to our navigation here and here I'm just going to use the view model. Let me let me import it manually. So I'm going to use the one from Life Cycle. Okay. Android life cycle ViewModel compose view model okay. That's the one that I want to use here for my navigation. And I'm passing that here. And the view model is far from being ready. Right. Because as you know, the view model is taking care of basically the communication between the data and the UI. So that's pretty nice. But our navigation now, as we recall, needed a nav host. And that nav host needs a nav controller. So I'm going to pass the nav controller as the nav controller. And I'm going to say that the start destination start. That's the let me actually import nav host makes it a little easier. And it's going to be this composable Nav host. Then the start destination can be our. Screen. Dot. Home screen dot. Route. And this nav controller also or nav host needs a content. So it's not happy with my nav controller here. And let me put that into a separate line so I can read it better. So let me see. It's this nav controller, which is yeah, that's because it's not a nav controller, but it should be a nav host controller. Okay, so our nav controller is of type nav host controller, which we can pass to our nav host as the nav controller. Good. So now inside of here we can have our composables which are going to be the different screens. So screen number one and screen number two. So in my case I only have screen number one. So I'm going to say screen dot home screen dot route should be the route for that composable. And the composable that it should actually display is going to be our home view. So our home view, if you recall, is the one that we have set up here. Let me see this one here. So currently our home view doesn't require any parameters. We're going to change that later on. But let's just see if our application still works if we didn't break anything before we go further. So we just prepared the navigation. We're obviously far from being done. But you see our app is still working. It's still loading the right navigation. Well, actually isn't loading the right navigation at all, because we're not saying that it should load navigation. So inside of our home main activity, instead of loading the home view, let's call our navigation. And let's rerun our application to see if the navigation is working. Because once we know that, we can add additional screens to our navigation and we see it still working. So that's great. So we have our navigation ready. We prepared our add screen as a well the route of the add screen. But we haven't prepared the add screen itself as a composable, which will be quite a big one. So I would definitely say we do that in another video. We're also going to take care of the ViewModel still, because currently we just have this empty ViewModel, which we obviously need to extend as well. So still quite a long way ahead of us, but we're really building a very powerful application here. So see you in the next video.