Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 16 - Navigation and Menus with a Music App Part 2
Lecture: Day 16 - Setting up the BottomBar
----------------------------------------

Welcome back. So in this video we're going to take care of the bottom scroll bar. This one here which says home browser library. And this we're going to start with it. So we have this current screen. And underneath we're going to set up a bottom screen. Actually we can do it underneath the title just well above the scaffold. So I'm going to create this bottom bar because that's what we need inside of the scaffold if we want to have it. So you see I have the scaffold here and it has this bottom bar property okay. So now I can define what the bottom bar should look like. I will however create a separate variable which will take care of that aspect. So here what we can do is we can just create a val bottom bar which will be a composable. And I need to add the add keyword add composable, which will take in no value and will return nothing. However, I'm going to define it straight away in here, where I'm going to check what the current screen is. So you see, we have this current screen which takes our ViewModel and gives us the value of the current screen, because we basically want to know which screen we're at in order to then update later on here. This aspect you see, first of all, it moves to the screen. Second of all, it also updates the bottom bar here to change the color to change which of the screens is selected. So. Current screen. I'm going to check that and then I'm going to check. Is it a draw screen. So is screen dot draw. Screen or current screen is equal to. Screen dot. And now that's what we are going to require to set up. Set up. So inside of our screens. We will need to set up the other screens that we don't have yet. So we will need the bottom screens, right? Which if you look at it, are similar to the draw screens. Yet they need to be slightly different. So we need to set up another sealed class in here inside of screen, inside of this sealed class screen. So I'm going to create a sealed class called Bottom Screen. And that will have a title. So same as we had here. So I'm going to call it be title instead. And it will also have a drawable resource. So be title and be route. So for bottom not for draw. So title route and the resource the icon that we want to display. But then it well it will also inherit from screen with that title and that route as a parameter. And here I'm going to set up new objects. So basically we could have done it on the draw screen. But I really wanted to have two separate ones because they are the same pretty much. Right. They inherit from screen, they pass the same information, but I wanted to have them separately so that it's easier, like easier to read on the next screen. So one of them will be the home screen, because if we look at it, we have this home screen, we have this browse screen and then we have the library screen. So let's set all of those up. So this one will be the bottom screen or will be a bottom screen with home as the title and home as the root. So I'm going to use home without a capital H in this case. And it's going to be using a drawable. And here. I'm going to use this music video one. We can obviously change that, but for now I'm just going to use that. Then still inside of this bottom screens, I'm going to have another object, which will be a library, which will also be a bottom screen where the title will be library, the root will be library with a smaller lower capital. And then I'm going to use r dot drawable dot I see baseline. Well actually I think we don't have the image for it yet. Don't have the icon for it yet. It should be the play button. So let's set up a new resource under Tools Resource Manager. Yeah there it was. I just didn't see it. Right. And then we want to set up a vector asset. We're going to select a button. So. Smart display we could use display here. Or what would be a library like library. Library. Play this one would be fine. So video library, music library, something like that. Okay, so you can choose whatever you want. I'm going to select that. This will be the name I'm going to click yes or next and finish. And this will be the resource that I want to use for it okay. Now we need the final third entry which will be our browse which will also be a bottom screen. And then it will have the name of browse and the route of browse as well. Now finally we need to set up the drawable. So our dot drawable dot. And yeah so I'm going to use another drawable here. No new vector. Let's create it. And I'm going to search for browse. Let me see if I find something like that. And you know what? I'm just going to use this one. Now, you could obviously use whatever you want. Whatever fits article would also fit, I believe, because that's what the screen looks like if you look at it. Right. So here. Well I cannot select it right now. I'm not going to use the same icon that we used before, but I'm just going to use this icon. Now let's use that. In here. And this one was what did we call it. Baseline apps. So I'm going to use this one here Baseline apps which is this icon okay. So now we have our objects for the screen. We can now go back to our. Main view where we have set up those screens. So we're checking which screen we're at. And if we're at home. So screen dot bottom screen dot home then. We're going to set up a bottom navigation. So there's this thing called bottom navigation which I can pass a modifier to. I'm going to say that it should wrap content. So modifier dot wrap content size. So I will now create another variable which will take care of my screens that I want to have in the bottom area, which is just going to be inside of screen. We had this list before. You see screens and draw. Now we're going to create another one, screens in bottom. So while screens in bottom which will just be a list of. Screen, dot bottom screen, dot home and screen, dot bottom screen, dot browse and screen dot bottom screen, dot library. So we're basically just setting all of the three screens that we just set up here. Right. All of those three, we're setting them up into the lists so that we can now use that list inside of our main views, bottom navigation to display those three icons there, or basically the screens, so to speak, so that we can access them very easily later on via the items. So here we can just say screens in bottom dot for each. And here we're going to use this for each loop to go through each screen inside of the bottom screen. So I'm going to call this one item. And we're going to create a bottom navigation item for each of them. So you see we need to know is it selected. And therefore we can use current root equal equal item dot be root. So that's what this root says here. All right then we need to define the onclick. So I would say well basically it should navigate. To the right page. So here we can use controller dot navigate and item dot b route. So currently it will crash because we have not set up those routes in the navigation yet. But let's do that later on. And now we need to define which icon we want to use. Now the beauty is we have a well we can define it like this icon is going to be icon with a painter. And let me see if I have I can import it. Well it seems like it's fine. It just needs the content description which I'm going to say item dot be title. And we need the painter which will be the painter resource with the ID of item dot icon because the icon is of type painter resource. Okay. If you look at it here, it's of drawable resource which can be converted into painter resource or which is what the painter resource wants. As the type you see here. Drawables rest. So that's my icon. And now let me just make sure that this is the painter variable. Like so. Then the icon should also be happy. There we are. So that's the icon. Then we can have the label as well here label which will be a text inside of curly brackets which will just have the text variable. So text equal item dot be title. And on top of that and still inside of the bracket of our bottom navigation item, I'm going to have the selected. Let me actually select it content color to be color dot white. And the unselected color color dot black. Okay so that's our bottom bar. Let's now set that bottom bar up inside of our scaffold like so. So now we use this bottom bar that we've just set up inside of our scaffold. And now you see once we start the application we have our bottom navigation bar with our home, our browse and our library. So if I click on one it will crash my app. So it looks like it's working. But that's just my other app as you see here. Because basically we have not implemented those navigation items yet. And let's just do that in the next video. So for now you see that's how you can add such a bottom bar. You just need to prepare the bottom bar as a composable. You could have also put all of this code inside of the scaffold here. That would have been fine, but I just have it as a separate variable up here and use the bottom navigation with a few navigation items, and you just need to define which items you want to have. And that's what we created this list for with the screens. So now we can use the screens as well for our titles as well as for their description, or like the route where they should be in terms of the navigation as well as which icon we want to use. And then you can say which color should be used for selected and unselected. So that's it for this video. In the next one we're going to fix the problem with the navigation.