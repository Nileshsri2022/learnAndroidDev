Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 16 - Navigation and Menus with a Music App Part 2
Lecture: Day 16 - Setting up the Home Screen - StickyText
----------------------------------------

Welcome back. In this video we're going to set up the home screen which should look something like this. Where you see we have these different options actually this one where we have these different options where we can scroll through them on the side or via scrolling to the side and also to the bottom. So you see we have basically this very interesting kind of view. So let's set all of that up. Therefore I'm going to create a new view or a new file which I'm going to call Home View. Now this will be a composable fun home which is going to be using the Experimental Foundation API. But we're going to take care of that later. So I first need the categories. So categories we're just going to be a list of items. So here I'm just going to be hits. Then happy and so forth. Right. So basically you see all of these categories hits happy workout yoga okay. So I want to have that. So let me just paste those in here. So workout running yoga. And then I'm going to have another list which will be called group which will be a list of strings. So here I'm going to define it being strings. And it will contain new release top rated and so forth. Okay so here new release favorites top rated and they will be grouped. There is this method called group by by index zero. So it which will be the group. So you can see here like so it is going to be the string itself. And I'm going to say it should be grouped by the first letter so to speak. Because if you take a string it's basically like an array of characters. And if you take the first entry of a string, then it's the first character. Okay, so now we can create a lazy column here which will have content. But I'm going to put that like so directly. Okay. And I'm going to group it again with a for each. So you see I get a lazy list scope which is a list. So I can group this list with a for each loop and then say what I want to do with each of those items. So for each and here what I'm going to do is I'm going to have a sticky header which will take the text. And I need to import text here. Okay. And this will be composable text. The text will have a value which will be its dot value at position zero. And then a modifier which I'm going to set to modifier dot padding with 16 density pixels. Let's import density pixels. Let me see import modifier as well. Android compose. And padding as well as it seems. So that's going to be the text that I want to have in my sticky header. So let's look at what this means. So you see here the headers. They are pretty sticky right. Those ones. And I want to use those headers. And then I want to be able to scroll around. So this is my sticky header. That then also needs to have a lazy row in it. In our case, so that I can swipe also to the side. So the lazy column is basically the part that allows us to scroll up and down like so. And the lazy row, each of those will have a lazy row which allows us to scroll to the sides. Okay. And you see, I'm using always the same. I'm using always hits happy workout. It's always the same thing. So here I'm using the lazy row and I'm going to say items categories. And here I'm going to get the category which I'm going to use as a browse user item. Now we will need to define what such a browser item is because basically it's this block, right. So if you look at it, this is what the block looks like. Basically having a little icon in it and above it you have a little text. And then basically it's surrounded by this box. So again I'm not sure whether I explained it well. So if you go to the home screen you see that this favorites text is sticky, even though I'm swiping to the side. That's what the sticky header does inside of my column. So we have this lazy column which will still push it towards the top and bottom, but then on the level where it is, it's going to be sticky, and the lazy row that is inside of it will still be able to move. So the text doesn't move with the lazy row that we're creating. So now. The items will be our categories. Now, this doesn't work yet because we still have a problem inside of our code as we have not defined the browser items yet. So let's define what a browser item is going to be looking like. Now you could also call it like home item or whatever, but I'm just going to use it like so at composable fun browser item, which will need a category and a drawable that it's supposed to display. So the drawable will be an int. So just a resource right. Resources you can use as drawables. So now what we can do is we can take the cat as a category. So cat and the drawable will be drawable dot or is equal to r dot drawable dot. And what do we call it. Browse screen. Let me see. So now I need to import R. Dot. And. Baseline apps I think I use their. And now at this point I will need to add the Experimental Foundation API up there. Okay, now let me just see why is the categories complaining here. I need to use. Well I need to import the extension function. Lazy scope items. All right. There we are. So now we're loading in those categories. And for each item like hits happy workout running yoga. We're going to have a separate browser item now which currently is just empty. So let's design it real quick. So I'm just going to use a card here. This card will let me import card. So import function card and I'm going to use the. Top one, not material three. I'm going to set the modifier to be modifier dot padding 16 dpi as well as size. So I'm going to define the size as let's say 200 dpi. And I'm going to add a border which will have a border stroke of a width of three density pixels, and the color of black or dark gray I think we used. So color is going to be color dot dark dark gray. Okay. And then let me put that into a separate file line. And then the cart needs content. Okay. So basically if you look at it this is a cart. This thing here. So it has an text in the middle and an icon underneath. But then surrounded it is with a big black box. It is 200 pixels density pixels tall and wide. So that's what this size does. And the border stroke, the thickness of the black border is three density pixels and the color is this dark gray. Now the content of it should be a column. So column we need to import column here. So now the content of that column should be a text, which I'm going to say is going to be the category and the image, which I'm going to say is going to be painter painter resource with our drawable and a content description. Content description. Why is it? Well, I need to use the right image here. So let's import. And the one with a content description, which is the one coming from composable or the composable one. So this will be content description. And here we can use the category as well because it is a string. Right. So let's just use cat. So that's the column. Now I can define the column a little bit. I want the items as you see I want them to be in the center. I want them to be bold in both directions centered. So we need to use the vertical arrangement for that to be arrangement center and the horizontal alignment to be alignment center horizontally. Now we just need to use this home page. So in our main view we are going to add this home screen. Let's import our home screen from composable home from UI theme. Well it should be in the UI folder, but it's specifically in the UI theme folder, which it shouldn't be. We're going to look at that later. But now you see we have the home screen. Once we click on home there we are. So now we have new release hits, happy workout, running yoga favorites and so forth. Okay, so you see we have home browse well browse not but home. And now you can go ahead and set up browse which is very similar. So if you look at browse it's pretty much the same thing right. Only that you cannot even scroll here. Well it has a specified six options available. So this is not something you shouldn't be able to do. You should be able to set that up yourself as well. So that would be a great exercise for you for the next point. So try to build the browse screen yourself. And then that will be the exercise that you that will come up after this video. So in the next video we're going to set up the library. So see you there.