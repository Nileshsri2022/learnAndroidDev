Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Trello Clone - Firebase - User Management - Drag And Drop - Online Database AN10
Lecture: Adding The Token To The DB
----------------------------------------

Welcome back. Whenever you register somewhere and you're logged in, then a token is sent to your device. And we need to work with that token because we want to use that token for the notification functionality. So what we will do in this video, we are going to focus on the token functionality before we then go back to the notification and finish that functionality. So therefore, first of all, I'm going to go to my constants and I'm going to create a new constant because I'm going to use shared preferences to store that token on the device. So here, let's go ahead and create a new entry, which I'm going to call. KONST Well. Pro DJ manage preferences. And that will be pro-gay marriage. Preferences. Or maybe something like pro-German Press. Actually, it doesn't really matter. It's just important that you always use this constant value. If you want to work with pro-German preferences or share preferences. Okay. And the main activity, that's where we are logging in. Right. So let's go over there. And this main activity here, we are going to create a new global variable. So here, private. Var and actually let's make it the late night var late in it var m shared preferences which will be of type shared preferences. Now let's initialize the shared preferences in our on create method so we can just do that down here. Or actually, let's do it before we. Load the data or anything like that. Even though it shouldn't make that much of a difference. But still, just to be sure. Let's go ahead and. Initialize it this dot get shared preferences at the position constants dot proxy manage preferences. So this is the shared preferences object we need to add. The mode that we want to use. So here, context out mode private. Okay. So the shared preferences should only be available inside of our application itself. Then in our Firestorm class inside of our update, user profile function update, user profile data in there, we need to make some changes. We're just at the point where we call activity that profile update success. So this will not be only a profile of my profile activity, but it will be an activity in general. So this, uh, this activity that is passed to us, but then accordingly, we need to make changes to this line here so we can check. Is it a main activity? If that's the case, then. Call activity. Dot token. Update success. So that's going to be a function that we all need to create in a minute. And then if it is a my profile activity, then do as you did before. So just call a activity profile. After its success. Okay. And here we still get this error, so we need to make the same distinction here. So when the activity is a main activity, call activity, hide progress dialog and when we are in the my profile activity do the same thing. Okay. Now let's go ahead and create this function token update success in our main activity. So let's go ahead and add that. Fun token update success here. We first of all should hide the progress dialog. Then we will go ahead and create an editor for our shared preferences. So Shared Preferences Editor which will be our shared preferences that we prepared. And here we need to edit or use the edit function. And then we can use that editor to put information into it. So. Editor dot put boolean. And it should be constants dot fc m token. Updated set. True. Now you might wonder if same token updated while we don't have that yet, well, let's add that in here as well. So const Val. AFC token updated. Which I'm going to set to FCM token updated. And then we need another constant here while FCM. Token. Which I'm just gonna call FCM Token. Okay, so these are the variables that we're going to use. CM Token update it should be fine. So at the position of FC token updated, we set the value to true. And then we. Set the ED to apply function or we call it, so that this change will be applied to our shared preferences. So this is called if we have an updated token and we get the token once the user is registered. Right. Well see registers oh. Once he logs in. And then here we also want to show the progress dialog. Which says please wait. And then we use the fire store class that. Load user data with this. Activity and the value true for the read port list. So it should re open the screen or reroll load the screen, so to speak. Okay. Now we will also need a function that takes care of updating the token inside of our. Our. Database, so to speak. All right, so here. Fun. And this one can be private. So private fun. Update FCM token. Here we pass in the token which will be of type string and then. Here we use the user hash map. Which will be a hash map of string and any. And then we use this user hash map at the position constants. Dot. FCM. Underscore. Token. So if you look at the user. Here at the model user, you can see that we had this value called FCM token and you might have wondered what the heck this was for. So we never used it, but now we do. So now we want to store the FCM token. So the token of the user that is locked in so that he can re log in very easily and that we can send notifications to him and so forth. So we set that value to be the token that is passed to us when we call the update. FCM token function. And then we also want to. Show the progress dialog. Accordingly and. Update so far store class that update user profile data with this activity and the user hash map that will just be paired. So this function will take care of updating the FCM token. And now we can. Get the FCM token. From the shared preferences and that will be in our own create here. So that is where we get the shared preferences. And once we have the shared preferences, we can now of course use the. Well. Token updated. Equals m shared preferences. Dot get boolean at the position constants thought. FCM token updated. And the default value will be false. So this variable is used to get the value, whether either the token is updated in the database or not. And then we check if. The token is updated. Then we will show the progress dialog. With our Please Wait statement. So we use the string once again here. And then. We use fire store class. DOD load user data with this activity. And read board details set to true. And otherwise. So we've talked an update that is set to false. Then we will use Firebase. For instance. I'd get instance. Thought instance I'd. Thought Add on success. Listener. With. And here, by the way, we need to import firebase. Instance. Or write it correctly and then import it. So here. Instance, I'd. With the add on success listener. And this should be the main activity. And then. What do we want to do here? Well, we want to update our FCM token. With instance id dot or ID result dot token. Okay. So. By using this. And by the way, it result. For instance, result is what we get. From. Our function here. So we get the instant result from this call here. So basically what's happening here is if we have an updated token, we just say, okay, load the user data and if we don't have an update token, then we want to get an updated token. So we get it from the instance and then we call the update FCM token. We pass in the user hash map, we update the user profile with the updated token. And then there's one thing left to do, and that is when we click on NAV sign out. So when we sign out of the application, what we also want to do is we want to make sure that. Our. Should preferences are reset so that the token is not stored anymore. So edit dot clear. So here clear that apply. What that will do is it will reset the whole shared preferences and set that back to empty. So let's test the application and see if the data is actually updated in the database, or at least the FCM token is stored in the database. So here if we look at the users. If the user gets an FCM token or not. So not part, but then it's at gmail.com. Okay. So that we are. Profile updated successfully. Good to hear. And as it seems, we are not closing the dialogue yet. So at some point, we're still not closing our dialogue there. But if we look at the database, we can see that I have a token here now. So updating the token worked, but we still need to close the progress dialog. And as it seems, I was missing a high progress dialog inside of our update navigation user details function. So here you need to add that line as well as inside of the main activity. So I checked the flow. By the way, if you wanted to know how to figure that out, I check the flow of our application. So each time that we show the progress dialog, we need to make sure that whatever calls we do afterwards. So for example, get points list, this function needs to close the dialog as well. So here. Add on success listener needs to at one point close the dialog so as it doesn't do it directly in here, it needs to do it in one of those calls. So for example, in the On Failure Listener, it needs to hide the progress dialog but also in the populate port list UI. I need to make sure that the height progress dialog here is also called OC. So that's it for this video. And in the next video we're going to do some more notification stuff. So finally getting the notification running. Yeah, the next one.