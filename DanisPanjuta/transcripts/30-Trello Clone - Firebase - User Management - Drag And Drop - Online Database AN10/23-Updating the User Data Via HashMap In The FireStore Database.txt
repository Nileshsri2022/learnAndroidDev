Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Trello Clone - Firebase - User Management - Drag And Drop - Online Database AN10
Lecture: Updating the User Data Via HashMap In The FireStore Database
----------------------------------------

Welcome back. Now that we have created the functionality to upload an image, let's go ahead and pretty much update our user info as well because just uploading the image is not going to be enough. We also need to make sure that the image is stored in the user database as well. All right. So let's go ahead and first of all, create a little function here, which I'm going to call profile update success, which will be called each time that our profile update is successful. And for now, we're just going to do the following. We're going to hide the progress dialog and we're going to finish the current screen. Or activity. So we're closing the current activity with the finished statement here. So basically when the user has updated, what we want to do is we want to close this activity and send the user back so he doesn't have to do it manually. Next. What I want to do is and that will be the code that will be done in our firestorm functionality. So let's go over to our far store class. So here we will have another function which will take care of updating the user profile data. So you can see this class is responsible for all of the database stuff. The other functions, they just do the code that has to be done inside of the class itself and which is not directly linked to the fire store or Firebase functionality. So this is very useful when you want to make your app maintainable. So fun update user profile data should be responsible of updating the user profile. And here what I'm going to require is an activity of type my profile activity. And a hash map. So let's look at this. I'm going to call this one user hash map and it will be of type hash map. And now we need to define what variable types it will have. So as the key, it will have a string and as a value it will have any OC. So this is what I was talking about in the last video. So basically the idea is that we will have the functionality that allows us to pass in the user as a hash map. And I will show you why that is required in a second. So let's just start this file store collection stuff as we have done before. So we get to constant users, then we get the document for the current user ID and then now we don't want to get the data, but we want to update the data. Now the thing is, if you want to update, so here, let's have a look at this update. It will offer you multiple options. So data field and field property or whatever. And the one that I want to use is the one that requires a user or a hash map. So it requires a type of a hash map. If you look at this update function, you will see non Nullarbor or a non null map with a string and object. So object is the class name and Java and in Kotlin it's called any and that's exactly the type that we have here. So instead of object, you have to use any in. Cotton. You can see the object itself is not accepted. We need to use any here. It's the cotton class. The base class. Everything is built upon. So that's why we need to pass in a hash map instead of a user object. Because it would have been easier as we have done before, right, where we get the whole thing as the user object. And then we make changes to the user object and then we just send it over and update it. But the thing is, it requires something of type HASHMI So we need to transform our user object into this specific hash map type. So whenever you call this updated user profile data, you need to pass on the activity and user hash map and that will then update the collection. So then the next thing we want to do is we want to add an on success listener. So we want to know if things went right. So what do we want to do there? Well, first of all, let's write a little log entry here where we just say profile data updated successfully. Very basic log entry. Then we can make a little toast so that the user also knows that things have been updated, profile updated successfully. And then we want to call activity that profile. Update success. So basically it is function that we just prepared before. Profile. Update success. So what shall happen if there is a failure? So add on failure. Listener. Well, what I'm going to do is I'm going to use that exception and. Use activity dot height progress dialog. Have a little log entry again. So. Error while creating a board. And yeah, that's pretty much it. We can also, of course add a little toast here saying. Profile updates. Error or error? When updating the profile. All right. And now we can go back to our high profile activity. And we will need to. New variables here. Actually we have one already, which is the profile image URL, but we will need another one which will be private late in it. Variable end user. Details of type user. And we will initialize it. In said user data. UI updates function, whereas it set user data in UI. So that's what we're gonna set up the user because that's where we get the user, right? So we get the user from the other class, from our fire store class, and now we can set it. So WM user details will be our user object that is passed to us. So what we're going to change is the image, the mobile and the name. And it makes sense to have these names, so to speak, as Constance. Because they are constant names inside of our database and we want to change values behind those keys. So keys should always be used as constants, at least in this context. Right? So let's go ahead and go to our constants again. So here, utilize Constance. So far, we only have the user's table as a constants. Actually, it's not the table, it's the whole document. Right. But what we want to have is we want to also have the image, the name and the mobile as constants. All right. So these three variables here. All right. And now we can actually update the user profile data. So we have this function called update user profile data in our fire store class. Now let's create a function with the same name and or my profile activity. So. It's also going to be called update user profile. Data. And this time it doesn't need any specific data because the profile data is inside of this class already. So what we will do here is we'll create a user hash map. All right. You probably already guessed that we will need it one at one point. Right? So user hash map will be of type. Hash map. With a string and any as the. Key value pair. And now we can add values to that hash map. So how do we add a value to a hash map and what are the values that we want to add to the hash map? Well, it's the image URL. It's the name of the user and it's the mobile number. It's the same three values that we have here as constants, right? Image, name and mobile. Okay. So let's do that. Let's add the image. First of all, we can check if AMP profile image URL is not empty because if it's empty, we shouldn't use it. And image profile URL. Is not the same as the image that we already use as the user details image. So m user details dot image. So this is the one that is stored in the database, right? If it's exactly the same URL, then we don't need to make any changes. But if it's not the same, then we want to make changes. So then we will just say user hash map at the position. So at the key. Constance thought image. It's going to be equal to him. Profile image URL. So here we're just saying user hash map at the position. That's the same as if we had user hash map. And here we pass an image because if we look at this, Constance is just image. So it's just the key. And if you want to get the value from something or from a hash map, you can just use the key like this. So this will then return whatever is behind image. In our case, it will be this URL here, this value. So this upload and Wikimedia work and so forth. And if you want to assign something to it, you just use the equals sign. Okay. So this is how we can update a hash map, how we can add values to the hash map. Now, the next thing we want to see if the user has changed the username. So if itI underscore name. To text to string is going to be not equal. The same as MX user. Details that name. Because if it's the same, there is no use in updating, right? So the idea is that we first check if the user has made any updates before he pressed the update button because maybe he didn't make any changes and he still presses the update button. But if that's the case, we don't want to make another call to the database. It's not necessary. We don't want to make any changes in the database if there are no changes to be made. And that's why we check first if there are any changes, because we already know the data from the database and we can compare it to the data that the user has entered. And if there is no change, then don't do anything. Okay, so here we're just changing the same thing for the name. So we're just saying hash map constants name is going to be. Equal to itI underscore name. Text to string. And then finally we do the same thing with the mobile number. So e t mobile text to string. Is it the same as the mobile value? Then we check. Is that mobile? And here mobile to string, of course. And then we say t mobile. Text to. String to. Long. Because the user hash map expects a long value here. Because, of course, our phone number here is not a string. It's a long value. Okay. And at that point, we have prepared everything and we can just say fire store. Class. But update user. Profile and hear. It's this fire class like this first class update, user profile data with this activity and the user hash map as the hash map that we need to pass. Now, of course, as I said, if there are no changes, we wouldn't need to call this function. So you could create a new variable here. So var any changes made is false. And then if that is the case, you could say any changes made equals true. And here the same. Any changes made. Equals true. And then finally here as well. Any changes made. Equals true. And then check if any changes made. Equals true. No. Not equals. Well, actually, just like this. If it's true, then just run the update profile data. Okay. So this is how you could check it if the user presses the button. Either we want to update anything or we don't. But the thing is in our update user profile. Do we do anything about hiding the progress dialogue, where we are hiding the progress dialogue here? So in that case, what you would then, of course, also have to do is to hide the progress dialogue accordingly. All right. So I'm just going to get rid of this. If any changes are made and so forth, you can use it. Now you know how to do it, but I'm just going to keep it simple for now. Okay. So we have the same name, right? We use the same name for both functions. Update User Profile Data. One is inside of my profile activity and the other one is in our file store class. But the fire store class needs the activity in which it should perform it and it needs the user hash map as well. All right. And at that point, we can modify our on update button click listener hear button update set on click listener. So now we can make changes to it because otherwise what we want to do is be one to. Show the progress dialog. I'm not the image chooser but the progress dialog with. Please wait. And then what we want to do is we want to update our profile data. So update user profile data. Now this user profile data we able to do still we added a little to do somewhere in our code. And actually, it's easier to find that like this. So here. We have this to do hard progress dialogue. So now instead of doing this, we can call this update user profile data function and that is inside of our upload user image. So the thing is when we upload the user image now what we will also do is we will call the update user profile data function, which will then. Trigger making changes. All right. Now, we can, of course, make this function private. And at this point, we can test our application because we never use up that user profile data somewhere else. So it makes sense to. Make it private. All right, so let's test it. Let's see if any changes are going to be made. So let's go to my profile. Let's select the homer image here. Okay. Now it's uploaded. We can see Homer. Well, let's change it to Homer Simpson. And the mobile phone number will have seven, eight, nine as well. Okay. At that point, let's press update just to see if anything's going to happen. So the Progress Dialog opens profile updated successfully and we're sent back to our main activity. And now let's look at the database. Let's see. So the link has been changed. The link now is this URL here. Which is this homelessness an image perfect. And other than that, what else was changed? Well, the mobile number has been changed at the name also has been changed. So now it works perfectly. You can see how you can update data in the database, not just create data, but also update data. Of course, also how to load data, because if you want to update something, you need to know what you want to update. So now you pretty much know everything you need to know to build the whole application, I'd say. Of course there are functionalities where we need third party libraries still and where we need to look into it a little more. But overall, I think we're at a very good spot now. Okay. So let's say let's go over to the next video.