Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Trello Clone - Firebase - User Management - Drag And Drop - Online Database AN10
Lecture: Preparing The Cards Color Dialog And Adapter
----------------------------------------

Next we are going to implement the functionality to select a color. So you can see we have a bunch of colors to choose from and then we can use that for the color of that particular card. All right. We can then update that. And for now it will not store it. But basically the idea is that the card itself will then have a top layer here, which will then say that this is a blue card, for example. All right. But this is for now what we're going to implement. So this a little additional dialog here. All right. To get started, I think it makes sense for you to just download the latest Strings and Diamonds file. So here dimensions that we're going to add are going to be the following. So. Item label colour height, label colour margin, colour, selected image margin and so forth dialog list, card view, elevation and corner radius and so forth. So just download the latest Diamonds file and also the latest strings file because we are going to use one string here as well. Of course, you can also just type in this string because it's not that much effort. So it's the string select label colour. Okay. So what we're going to need is a new layout. So I'm going to create a new layout resource file, which I'm going to call item label. Color. And it will be a frame layout as the root element. So let's create that one. And inside of this frame layout, I would like to use a new vector asset. So let's use the tick box. Actually, it's a check. This one here. I want to use the check and I'm going to call this one. I see. Vector White. Done 24 Jeep. Ad finish. So inside of this frame layout, I would like to use it. So let me show what this code is going to look like. So basically what we have is a frame layout which matches parent and width and reps content and height. Then the foreground is set to attribute selectable image or item background. Then we have a view here which I call view main, which has a width of match parent and the height is item label color height, which is 40 density pixels. Then we have a layout margin which I'm going to set to five density pixels and color X and is used as the background color for now. Of course, we want to adjust the color accordingly. And then we have this image view which is basically this tick box. It has an ID, I've selected color, it reps its content and height and width and its center vertically end gravitated. So this means that it will basically look like what you can see here. So we have this little icon here on the right hand side. This is the selected check, which means that this is the color that we have selected so the user can select multiple different or from multiple different colors. Pretty much. Right. So we have this blue color, for example, and we of course want to adjust that for the different colors that we want to use. The visibility is set to gone for this image view and in the tools we see it, of course. Okay. So that is the individual item for the colors that we want to be able to use, but then we will need another XML file. Which I'm just going to call dialogue list. So here. Dialogue underscore list. Or XML. And what that will do is it will be a frame layout also. And at will contain the following code. So let's look at it. You can, of course also download this file if you don't want to type everything in here. So we use this frame layout which matches the parent. Then it's a card view which has a linear layout inside of it, in which we have a text view view and a recycler view. So basically if we look at it, we have select and then we have a separator and underneath we have a recycler view which we cannot see much of yet. This recycler view should be full of item label colour XML files, so full of these kind of colours and then the user can use the one that he wants to use for the card. So basically the dialogue list is this card here. With this select label color text and the separator. And underneath we have this recycler view with a bunch of item label colors, which are the colors here that we can then select from. So this is how they are linked together. And of course, as we are working with a recycled view, we also need to have an adapter. So let's go ahead and create the new adapter inside of our adapters folder. So here new. Scotland file. It's going to be a class called label. Color list. Items. Adapter. Pretty long name, but that's basically what it is. And. The information that we need to pass in here is the context. So private, vital context, which would be of type context. Then. And I think it makes sense to put that into multiple lines. So we need to import context here. Android content. Context. Then we have the list, private var list, which is an array, a list of strings which are basically the color codes. And then the private vul m selected color, which will be a string. Now as this is an adapter we inherited from recycler view dot adapter. Of type recycle of you. Thought view holder. All right. So this is how it's going to look like overall. They will call the items adapter, which is a recycled view adapter. And then of course, we need to implement the functions that are missing. So. He wants me to make this abstract. What I want. But I want to implement the members. So I have over class implement members and then let's implement those members here. So we have on create view holder, so get item count and so forth. So let's first start with the easiest one get item count. Let's get rid of that one or of the To-Do and let's just say return list the size. Then on create view. HOLDER Well, the thing is, as we are going to work with an item click listener and with our class view holder, let's just implement them real quick. So private class, my view holder view view which is going to be a recycler view. I thought. You hold her and we pass in the view. All right. So we need to put import view here, import Android view and then as we want to be able to click on the individual elements. So let's add an on click listener, which is going to be an interface. Called On Item Click Listener where we have the on click function, which will need the position and the color which is a string. So we need to know which color we pressed on. All right. So now let's implement this on create view holder here. Which will return my view. HOLDER That's the one that we have. Added at the bottom. And it should return my view holder so there. What I want to return. His layout inflator from. Context. But inflate. And then our dot layout dot item. Label color. Parent and false. So we need to import our here and then we can use item label color, which is this XML file. So we inflate this XML file inside of our view holder for every single last element. Okay. So that's the idea behind this, a line of code. We've done that multiple times already. Now unbind view holders. So what do we want to bind inside of the view holder? Well, first of all, we will get the individual item that we look at, which is the item at the position of our list that we get passed. So the list of colors, so to speak. And then we can check if the holder is of type. My view holder. If that's the case, then we can go ahead and design it. So. Item view. Dot view. Main. Set background. Color, spectrum color. Which will be. Color dot parse color. Out of the item. Okay. So what's happening here? You mean, is this part here of our item label color? So if we look at it, it's this view. Then. We set the background color of that view based on the color parse color item. So. Item what is item item is the individual item of our list. And then we parse it into a color. So if it's something like hash five, so six FS, then that will be, for example, white. If it's a bunch of zeros, then it will be, for example, black. So this year is passed to us as a string. And then. It makes a color out of it. So this part is color makes then the color out of it. All right. On top of that, what I want to do is I want to make this item view. Or this image. You better set this one here. This tick box. I want this to make. Or make this visible or invisible, depending on. Whether the item is the selected colour or not. All right, so let's go back to our adapter. We can here check if. Item is equal to m selected color. Then we want to make this. I've selected color visible. His view thought visible and otherwise. I want to make it invisible. So basically this part here. Is equal to gone. And then, of course, every single individual element should be clickable. So if we click on green, we want to have green, if we click on blue and so forth. All right. So. Let's make them clickable as well. So holder item view dot set on click listener if. On item click. Listener is not equal null. Then I want to set it or set an on click for it. So we need to create a variable for this, of course. So let's create a global variable here on top of the on create view holder. So on item click listener will be a variable. Which will be of type on item like listener Nullarbor, which I'm gonna assign null to. Okay. And now we can check. Is it now? If it's not now. Then go ahead and. Set the on item click listener dot on click with the current position and then we also need to pass in the item. So this item, which is part of this position, which is. What position is an integer but list? That position will give us a string. So here. A string of the ArrayList. Because if you look at it we can see on click expects an integer and the color and that's what we see we pass the position and the item which is a color for a string, which then will be a color. All right. So next, we need to create this dialogue because you can see once we click on it, it opens up this dialogue. Right. It's not a new activity that we are populating or anything. It's just this little dialogue. So just pop up. And if you want to use dialogues, well, you need to create them. So let's create a new class here. Which will be. Inside of a new. Directory of package and I'm going to call this one dialogs. All right, so let's create a new Kotlin file or class, and I'm going to call this one label color list dialog. And this one will be an abstract class. And abstract classes cannot be used to create objects from them. So classes cannot be instantiated, so you cannot create objects of an abstract class. However, you can inherit subclasses from them. So I don't want to be able to create objects from them, but I want to directly access the. Elements of it. So here I'm going to need the context. Then the private var list of array list. Strings. And then. I want to know the selected color. So private. Vul. Em selected. Color, which should be a string, but a way. We also need the title. So private var or maybe vol title should be a string of. An empty string. By the way, this one should be a vase as well so that we can change it. So it will be an empty string. In each cases. And we need to import context so that we can use it. Now, this whole class should inherit from the dialogue class because well, it should be a dialogue, right? And if you want to use the dialogue class to inherit from it, you need to pass in the context and you need to import it. So now we have dialogue and context. All right. So at this point, we can now implement this class. So private bar. So first of all, I want to create an object of my adapter which for now will be empty. So color list items, adapter multiple, which I'm going to assign null to for now. All right. And then. I need an on create here. So on create. What do I want to do here? Well, I want to create a view which will be using the layout inflator dot from context, dot inflate. Ah, Dot. And here we need to import ah as well. But this will allow us to inflate our own layout that we created. So it's inside of the layout folder and it's called dialog list. Dialogue list. So let's import our here so that we can use our dialogue list. And we also need to pass in the root what we call inflate and we're just gonna pass null here. So we call a layout inflator. We create a new view out of this, right? So based on this dialog list, XML file that we created, okay. So now we have this new view. And we can now set the content set content view to be the view that we just created there. I'm going to set the cancel label on touch outside to True. So once we click next to it, you can see the dialog just disappears. So this is done by this set. Cancel on touch outside and I'm going to set cancel label to true so that it can be cancelled. And I'm going to set up a recycler view which we need to create the function for. So let's create a function which will take care of the recycler view. Private fun set up recycler view. Which we need to pass in a view. And let's import views so that we can use it and rotate view. And here we can then design the view so we can just say view TV, underscore title or how did I call it TV title? I think that's that was it. Text view title. This one here. The text. Should be the title. And by this I mean the title that is passed to me. So to the label colored list dialogue class. So here currently it's empty, but we can override it, of course. Then. View. Dot rv. List. So the. Let's hear. If we look at it, it's this recycler view list or V list. Let's set that layout manager to be a linear layout manager. Which also needs a context. And then we can set the adapter. So let's set the adapter to be label color items adapter which needs to, needs a context. And what else does it need? The list and the selected color. So let's pass in the list. Enter selected color. And then we can set the adapter of our RV list. So here you are. We list thought adapter. Should be the adapter that we prepared. All right, now, every single adapter. Needs to have an on click listener. Right. So adapter dot on item click listener. Should be a new object off label color list items adapter dot on item click listener and here we can implement it. So we're creating a new object off label color items adapter on item click listener which is this on item click listener interface. And now we need to override its events, so implement its members, so override on click. And here once we click on the button, what we want to do. Well, we want to dismiss this dialog and we want to say that we have selected an item. So we need to create this additional function protected abstract fun on item selected. So here we can then implement what should happen once we selected an item and we need to pass in the color of the item that we selected. So here we just say it's abstract so that we can later on implement it. You know, our class is abstract as well, so we can now create abstract functions in here and it's protected so it can only be used inside of this project or yeah, that's pretty much it. Right? So here I can just say on item select, it should be called with the color that is passed to us as a string once we click on the item on the individual item of our adapter. So here this is our recycle view. We use the adapter for it, we click on one item and that is pretty much what is then called. So the item selected and the dialog itself, as you can see, is also dismissed once I click on a color. And now there's only this function to be left to call or be called here. And we need to pass in the view. And luckily we prepared a view already, which is our layout inflator dialog list. So at this point I think we're going to make a pause because it's still a little bit to set up. So we still need to do all of the setting up in the cart details activity itself, which will then of course use our label color list dialog. So you can see we created the adapter to be then displayed inside of the dialog. So we create a new dialog in a way that you can see here. So we pass in the context, the list that we want to display in the dialog that's not necessarily required if you will ever want to create a dialog. But in our case we need that because our dialog needs to display this list. Then I want to have a title for the list and I want to know which color is selected so that I can then make this color specifically different so I can make a tick box here, for example, which we don't have at that point yet. Even after finishing the next video, we will not have that yet. But overall, you can see we first of all need to have an adapter, which is our label color items adapter that we created before. And then in the on create, we can inflate the view, which is the dialog list view which we prepared before this XML file. Then we can set that as the content view. We make it to touch outside. True. So we can cancel it once we touch outside. Then we set up the recycle view and now we need to do a lot of coding inside of card details activity to actually add the functionality. It's not that much, but still we need to do that in the next video. All right. So see you there.