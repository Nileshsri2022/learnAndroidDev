Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Trello Clone - Firebase - User Management - Drag And Drop - Online Database AN10
Lecture: Using The FireStore Database to Store The User Details Part 2
----------------------------------------

Now that we have the user class, we can go ahead and use user. So let's import user here, which is our models. So inside of models. And not the user that we get from file store. It's important that we use the right one here. So now what I want to do is I want to register the user right in this function. But what we need there for is our MX five store collection. So I'm going to create a new collection which is going to be called users. So it's going to be something like this or something like this. But the thing is, I'm using a magic string here. That's something that I shouldn't do. I think it's better if I go ahead and create a new. Class, which will be our new file, which will be constants. This will just be a constant object. Which will for now just have one constant const vul users string which will be uses. All right. So now we can use this constant that we prepared. So constant start user or users. So this is just a code way to do the same thing as if we just created a new collection here, which is called Users. Okay. So here creating a collection or start collection and we call this one for example, users, and then we click next. And now it becomes interesting because the document ID and all of the different fields, that's what we do next in the code. So we now go ahead and create a document. Right. So dot document. And here we need to get a current user ID, so we want to create a new document for every single user that we have because we want to store details about every single user. Right? So it would be good if we could get the user ID, which is this unique user ID. So how do we get that? Well, we need a function for that. So let's create a new function which is just going to give us the current user ID, get current user ID, which will return a string, which then will simply return firebase off. Firebase auth get instance current user. You ID? Okay. So that will give us as the current users unique ID which is. This. You-You ID this one here. So it's going to give us this one, and we want to use that as the document inside of our collection. So we create a new collection which we call users. Then we have a new document which is going to be called this weird number or this weird value that we just solve for every single user. So every user will have his own collection or collection, but every user will have its own document. So we have collections in collections with documents, and then we have all of those fields, and the fields will be the one that we have in our user class, which is ID, name, email, image for mobile and so forth. So we don't want to create all of that stuff manually, but we want to do it in a code. That's exactly what we do now. So now let's go ahead and pass in to user the current user. So get current user ID. Then we want to set the user info to be set options merge. So it's going to merge the user info, whatever user info is passed to us. And then we need to finally add an en success listener So did it work? Add on success listener And if it did work, well, then we want to execute some code. So we want to run the activity that is passed to this function, this registered user function, which is the sign up activity, and we dare want to call the user registered. Success. So user registered success. So it doesn't exist. So let's go over to the sign up activity and create it. For now. It's just going to be a new function. User registered. Success, which will show a toast. So let's see if we find a toast somewhere. Just gonna. He used his toast here. And actually, I want to do the same thing as I did here. So where is it? It's up here. So let's write this little toast without the name and the email. You have successfully registered. Something like that. And here. We should also hide the progress bar. So hide. Progress. Dialogue. So here Mick text something is wrong with make text. It still needs to length here. So that's because I forgot to add the little quotation mark here. So high progress dialog at that point and then lock the user out. By the way, if we now use the hard progress dialogue function here, we won't need it anymore in our register user because we also did that somewhere in here. And that is right here. So we won't need it here anymore. And now at that point, we need to make sure that when we registering a user that we call this first class function, which is called registered user. So the thing is we're registering the user on two levels, right? So one is we actually create a new authentication entry for him. And the other one is that we create a new entry in the collection of users, which is a new documentation or a document. Right. And this will is a little abstract still, but it will make a lot more sense once we actually run the code and we see what it does with our entry in the fire store database. Okay. So at this point, there's one little thing left to do. And that is to get rid of this toast here. We won't need it anymore in here. And we also don't need to sign out here anymore. But what we want to do is we want to create a new user, right? So here we just go ahead and say all user is going to be a new user, which is going to be a Firebase user, dot, you ID, that's what we're going to pass there. We pass the name and we pass the registered email. Okay. So let's go ahead and import user this time. It's also our models here. So we'll create this new user object and now we can pass that user object to our file store class function, which is this registered user. So we need to pass an activity which is going to be this and then we pass in the user, which is the user that we just created in our sign up activity. So now we can go ahead and save our store class dot register user and here we pass and this and we pass in our register or our user that we prepared. And then of course, I need the brackets here because I'm not using a fire store class object, I'm using the actual class here. So I need the brackets in order to call its function. All right. So at this point, let's test our application and see if it's going to do what we need from it. And by the way, while it's loading, let's go to authentication and get rid of those two users because those two day would cause problems later on. Because otherwise, well, we will try to access their information as users, but they don't exist, right? Because we changed everything. So they just didn't exist in the database. They exist in the authentication, but they don't exist in the database. So if we want to access anything from the database, that would cause problems. All right. And at that point, we can test it. There is only one little thing that I noticed when I ran into an issue testing it. And it's not really the code itself. The code is fine as it is now. There's just one duplicate that I have here, which is activity finish, which I run here once again in the store class, even though it's not necessary because in user registered success we already run finish. So it's not really necessary also when testing it and by the way now it works. So the code is fine and I had to test it on another machine in order to make sure that the code is fine. But it still didn't work for me. So registering the user did work for me, but creating the collection for some reason didn't work. So what I needed to do is I needed to restart my computer and then run it again and then it worked. So if you have the problem that it doesn't work for you, then just try to do these kind of things, such as restarting your computer or closing Android studio and opening it up again, or even deleting the different folders that are inside of this project folder. So the build cradle an idea and also the build folder in here. You could delete them and then run the application again. I'm telling you this because this issue, which is super funny, it didn't happen on other machines, but for me it did. That's why I wanted to talk about it real quick so that, you know, if something like that happens that sometimes it's not the code, sometimes it's some unknown force. That's how I'm going to call it, because I have no idea where it comes from sometimes. All right. So let's sign a sign up now. Therefore I go here, I create a new user, by the way, I delete all the other ones, as you have seen. And now let's add this user here. And sign up. Please wait. And you've successfully registered. So now let's go back to the authentication. And just refresh the page just to see if we have new users. And we can see we have them. Of course, I could have pressed the reload button. That probably would have been faster. And then in the database. Let's see if we have something in Project Montage. We have this collection called Users. Then we have the user with this unique ID and we have all of this information. So we have this email FCM token nothing yet. ID Yes, image nothing, mobile zero and name Dennis. All right, so it worked. We have passed an object into our database as fields. So we have this object. Is this the object? The user object? And these are all of the fields of the user object. So all of the variables that the user class has and all of the details that we have with it. All right. So now you know how to create a collection, how to add a value in there and store data inside of the database. And we will, of course, have to check how to retrieve the data, and we will work with all of those things in the next few videos. So see you there.