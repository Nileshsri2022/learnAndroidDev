Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Trello Clone - Firebase - User Management - Drag And Drop - Online Database AN10
Lecture: Preparing The Boards Recyclerview Adapter And UI Elements
----------------------------------------

We'll come back. Now it's time to display the list of boards that we have. And in order to do so, we, of course, need to add some more code. So let's get started. Therefore I'm going to go to the main activity. And there we need to create a new function which will take care of populating our board. So let's go ahead and create it here. Fun populate board's list to UI. So I'm going to call it. And we first need the board's list, which is going to be an array list of boards. So ArrayList of board. And then we need to import port here. I know we can start with a code for dysfunction. Okay, so what do we want to do here? Well, first of all, we want to hide the progress part. If it was loading before, so let's hide it. Then we want to check if the boards list is empty or not or is zero or not. So both. This size is greater. Zero if it's more than zero. What we know is that we can make our RV board's list visible. So let's set the visibility to view dot visible and then we can go ahead and make the TV no boards available, text invisible. So here visibility set to view, dot gone because invisible is not going to be enough. It actually has to get out of our whole layout. Then I'm going to tell my RV boards list what kind of layout manager I want to use. So here, layout manager will be a linear layout manager where I then pass this activity to and then I can set has fixed size to true so that our board's list has a fixed size. All right so that's what this function does so recycler view function. And that the changes cannot affect the size of the recycler view. Then as always with recycler views, they need an adapter, right? So let's prepare an adapter here, which will be our board items adapter that we created in the last video here. We needed to pass a context and the list that we want to use that is that I want to use is the board's list. So if you recall, ports item adapter is the class that we created in the last video we needed to pass in a context and then a list of boards. Okay. So that's what we do here. And now we can assign that adapter to our boards list. So the board list adapter should be the adapter that we just created. And then if the word list is in fact empty, then we don't want to display our board list, but we want to do it the other way around. Pretty much so here then our boards, this should be gone. And the text that says that there are no boards available, this one here, this text view, no boards are available, should be set to visible. So this function will just take care of creating the boards in the UI. It's not just going to actually download it, right? Because we will download it somewhere else. Then we will pass it to this function and this function will then go ahead and display everything in the UI. Okay. Now I want to make a little change in my boards because the board doesn't know about his ID, so I'm going to create a new variable here, vul document ID, which is a string. And then we of course need to add that here as well. So Passel read string. Then further down here parcel write string with the document ID. Okay. Then in the constants. I will also use this assigned to. So I'm going to create a assigned to. Variable. And my utils constant. So here we have the mobile with the name, the image, the boards, everything. Now let's create one for our assigned to OC assigned to which will be a string which will just have this text called assigned to. Because now we can use this constant instead of using the string, which will be a magical string. And it's always difficult to then find the right spot where we use that string and how we call it. And if we made a mistake, then it's going to be difficult to find that mistake. So it's really a lot easier when you just work with constants that you can then quite easily adjust. All right, so now we can go to our file store class because now it's actually time to get the board's list from our file store database. So let's go ahead and create a new function here. So here we created the boards that we update the user profile. Well, let's create a function to get the boards a list and we need to get the main activity as the activity that calls this function. Then I'm going to use this collection stuff as well. So M fire store collection with the constant thought boards. Then now we're using queries. So that is something that is pretty powerful and that is a feature that fires two offers that unfortunately the real time database of fire base doesn't offer. And that's really where the power of firestorm lies. That's why we use it. I thought about using a database or a real time database, and I even try to build it there. But man, oh man, that was a tough one. It really made things so much more difficult when you wanted to create a maintainable and hugely flexible database structure. That was a pain. So I decided to go with the firestorm approach, and this is why pretty much so the stuff that we're going to do now is really a big part of why I decided to use the firestorm. So we are checking whether array contains constants dot assigned to. And what is it designed to? Well, our current user ID. So current user actually we need to get it right. Get current user ID this one. Okay. So what we're doing is we are checking. The Board's collection where the value four assigned to is the current user ID. So if you look at our boards here, for example, this board was created by this user and that's the user ID of the current user ID or of the current user that is locked in. That's the current user ID. So we just checked through all of the boards that we have where assigned to is equal to whatever this ID is that we have. And if it's the same, then we want to display this board because then we want to use this board and it was assigned to us. So either we created it as ourselves, so it's assigned to us or somebody else created it and assigned it to us. Then we also want to see this board because then we need to make adjustments on the board maybe, or we need to finish tasks. And this project is also assigned to us and so forth. So what do we want to do with that? Well, we want to get it. So get and then let's add an on success listener. So if that was successful, then we will get and you can see there are multiple different options what we will get and I'm just going to get a document. So I'm just going to say, okay, now that you give me this document, let's use it. And I'm just going to write a little lock entry here. So simple document documents to string. So I'm just going to display the whole document as well as I will create a boards list, which will be an array list of boards. Which will be for now an empty array list. And of course, I want to add all of the words from that document to it. So this document is a snapshot, and the snapshot only contains the stuff that is assigned to us. So only the boards that are assigned to us. So what I can now do is I can go through the whole document. And then add every single board to my board list. So what I'm going to do is I'm going to say create a board, which is I to object. And here is our board, colon, colon class or Java. So we're basically saying whatever object you have there. Now create a board object from it and store it in this board variable, which we then can use. To, first of all, get the idea of that board. So now we don't only get the board list, but we're also getting the document ID of the board. And we can now go ahead and add that board's list or that board to the board's list. Now, as it seems, the document ID. String. Let's have a have a look what the problem is. Well it has to do with two objects returning a Nullarbor. These are the tricky ones. Right. So it returns a noble. That's why we need to. Use double exclamation marks here and here. I'm still getting a little error. That's because my document ID is a var or should be a var instead of a vowel because I want to be able to change the document ID. As you can see, that's what we do here. That's why I needed to change it real quick. Okay. So at this point we get the whole list, but what should we do now? Well, we should, of course, also populate our activity. So activity dot populate boards list to UI with the board's list that which is prepared. Okay. So that's what the whole pause list to UI function was for. Right. It needed a board list to then display the board's list in our recycle view. So that was the whole point of this function. And then we created this function to get the board's list to then populate our board with it. So that's the add on success listener. Now let's add in failure listener as well, and which I'm just going to hide the progress dialog. So here add on failure listener with an exception here called E and I'm just going to say log E simple name error while creating a board. All right. The next step that I'm going to do is also a little optimization step, so to speak. So let's go to the load user data function. So we have this function here, load user data. This one here. What I'm going to do is I'm going to add a parameter here. So that's going to be called read boards list, which will be a boolean. All right. So I want to know if I should read the board's list or I shouldn't. And this will be relevant here in the updated navigation user detail, because I want to make sure that I only update the navigation user details and only read the boards in there if necessary. Okay. So let's go to our update navigation user details. And actually, before we do that, real quick, I'm going to assign false to this value. So not here, but here. I'm going to assign default false. So this will be by default false. So you don't always have to add a value, but if you add one, then you can make changes to it. Okay. So now let's go over to our update navigation user detail. And here we need to accept a boolean and I'm going to give it the same name. That will be a boolean. The idea behind this is that we don't always load everything from scratch. But yeah, sometimes we just have to clean up things, right? Okay. So before we update this function, I want to make clear why we even need that. So inside of our main activity in the on create, we load the user data, right? So here, file store, class load user data. At this point, we want to also load the ports. So the user comes to the main activity for the first time and then we want to load all of the boards of that user. So here we need to pass in true because load user data is the function where we load the user data, right. So it reads the board list or should it read the boards list boolean true or false. Well I say yes. Please read it. So then. It should read it when we load it the first time. But now let's say the user has this, uh, this flow. So he comes to the app and he sees all of his elements here. Then he goes to the profile, he changes something in the profile, and he comes back to this activity. Then I don't want him to load the whole board once again. The word can stay the same as it was before. The page will load, but it will not load all of the details of the boards. It will just keep the boards as they were before. All right. So this is just a design decision. You could have left that out, but it wouldn't be that optimized then. Okay. And now we can go back to our update. Navigation. User details. And here. I want to load the boards list if the read boards list is set to true. So if read boards list a set to true, then go ahead and show the progress dialog. That we have prepared so far. Which is this? Please wait. Progress dialog. So here you can use resources, kid. String or string. Please wait. And then our far store class get boards list with this activity. And at this point, we can finally test our application. All right. So. Pro-regime Monash. No boats are available. And then it loads, loads them. And as you can see here, board with image and board without image. Okay. So these are our boards were created earlier on. Now, there is one thing that I'm not so happy about, and that is the fact that I cannot distinguish the two boards from each other or where one ends and the other starts and so forth. So we should go back to our resources and here we have the layout item board. And at the very bottom I want to add something. So it's just inside of the last linear layout, which is just a little separator. All right. So width should be match parent and height will be. The item border or board divider height. All right, then. The margin top. So here margin top should be divider margin top. And then finally, I want to have a little background which will be at colors divider color. So it's this divider color that we prepare this grayish, whitish color. So slightly different than what. Okay. So let's close this and run this again. All right, A.B. So there's this little line here, barely visible, but it's there, and that is this divider color. Of course, you could make it more gray, so it's a little more visible, but it is all design choices that you can make yourself. Okay. So that is it for this video. See you in the next one.