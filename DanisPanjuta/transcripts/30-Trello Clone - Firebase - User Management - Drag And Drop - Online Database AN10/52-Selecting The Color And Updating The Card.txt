Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Trello Clone - Firebase - User Management - Drag And Drop - Online Database AN10
Lecture: Selecting The Color And Updating The Card
----------------------------------------

All right. So now let's take care of all of that coming together to finally be available in the card details activity so that we can then actually select the color and see it here under label color view. All right. So in order to do so, we will need to have a function which will return a colors list. So I'm going to create a new function inside of card details activity. So here fun. And actually we can make it private as well. So private fun colors list which will return an array list of. Strings. All right. So first of all, I'm going to create a new list of colors. So colors a list which will be an array list of strings. And I'm going to say why I'm going to create an object of ArrayList here. Now I can go ahead and add to the list, and I'm just going to show you the different colors that I want to use here. So it's those colors that you can see here. So I just add a bunch of colors here. You can. Select different colors if you want to. It's free for you to decide. And then I can return the colors list. By the way, you can also return a different amount of colors. You are completely free to choose whatever you want here. All right, then we will need a function which will set the colour so private fun set colour and that one will basically set the TV selected colour. This one here TV selected label colour text to be empty because in our case currently if we look at it, it says select color. So this is the text that we currently have and we want this to be an empty string, so no text at all and instead we want to change the background of it. So here. Set background color, which will be. Color thought pass. And colors, but color pass. Color. With selected color. Now, by the way, we, of course, need to create this M selected color variable. So this global variable and also let's import color here from Google Graphics and here let's create this global variable at the very top. So here. Private var em selected colour. Which will for now be an empty string. All right? And now we need a function to actually trigger our dialogue. All right, so let's create one private fun label. Colors list dialog. All right. So what are the details that we need from label colored list dialogue? Well, we need to context the list, the title and the selected color. So let's prepare all of those so well colors list should be an array list of strings. Which will be a colors list or but we'll just use the colors list function to get all of the colors. All right. And then now that we have those colors, we can now go ahead and prepare the. This dialogue. So. Vol listed dialogue will be. Object. Label. Color. This dialogue, which now needs, of course. The context, then the colors list. Then the text that we want to display and here I want to use the resources that we prepared, which means it will be the string string select label color, which is going to be this string here. Okay. So that's the one that I want to use here. And now. We can define the code of it. So we can now go ahead and say, for example, by the way, I miss a bracket here, so here we need to implement it on item selected because if we look at it. We have this abstract function on item selected. So if you use label color list dialog, you need to implement its members. So let's do that, implement the member on item selected. Now we have a little to do here. So what do we want to do once we selected an item? Well then we. Get the color right. And we want to set that color to be the selected color. And also we want to then call select color or set color, which is this function which will then set the current color as the new background of the set label. So if this select color label here. Now, of course, we need to trigger all of that. And in order to trigger it, we will add an on click listener for this text view. So in our on create function. Here, we can give our TV selected label color and click listener. On click listnr will be. Calling label colors, lists, dialogue, which is dysfunction that we just created. So it should go ahead and use that. Now, of course, once we created this list dialogue here, we need to show it some way as well. Otherwise it will be not worth it created. Right. So that's the whole idea behind this label color list dialog. You could even call this one show labeled list color dialog. So this function, you could call it differently, but I think it does what it should do. So it should now display the list dialog. Okay. That by itself is already pretty cool. But what I want to do is, of course, I want to not only display the color, but I also want to pass it to my card. So the card later on, when we reload the page or this card, it should know that we actually want to. Display a certain color because otherwise it won't. No. So our card currently doesn't know anything about colors. That's what we need to change. So let's go over to this card and we can see now it works. So now we can select the color. And you can also see that it even goes into the. Field right here. But of course, the cart, as I said, does not know anything about that. So let's go to our cart. Model. And let's adjust it a little bit, because if we look at it, here's the card. What it knows is the name who created it and who is assigned to it, but it knows nothing about its color. So I'm just going to add a field here which will be evolve label color, which will be a string that is empty by default. And now we can, of course, update the card details. All right. So we have this function here, update card details here. We, of course, also now need to pass on the color. And luckily enough, we have the color here. As we can. Just go ahead and add a new entry called M Selected Color. So overall, it's going to look like this. So we'll create a card now. And this card also has a selected color. So now we will store that color in the database as well. Of course, we're not loading it yet. Or do we? Let's look at the on create. We're not actually loading it yet, but at least we're passing it to the database. So let's see if that's actually the case. Because once we click update. And the create function. We update the car details. All right, so let's see. Let's go to card one and list to that. Select the color. For example, this green. Let's click on Update. All right. Now let's go to the back end. Let's check out list two here. So it says card one. And that's actually the wrong list. So here list to. As the card here, which has this color. So let's see if that is actually the right color. And you can see that the screen here. And that looks a lot like the green that I have selected here. All right. So that seems to work. Okay. And that's it for this video. And the next one, we will, of course, also want to read color to display it and so forth. So see you in the next one.