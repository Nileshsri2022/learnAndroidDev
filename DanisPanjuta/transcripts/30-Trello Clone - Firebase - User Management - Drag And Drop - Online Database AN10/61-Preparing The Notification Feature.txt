Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Trello Clone - Firebase - User Management - Drag And Drop - Online Database AN10
Lecture: Preparing The Notification Feature
----------------------------------------

Welcome back. In this video, we are going to start creating the notification functionality in our application and therefore. I can recommend to check out cloud messaging slash Android slash client to understand better how to set up a Firebase Cloud Messaging Client app on Android. Even though we're not using a messaging app. But it's still useful when it comes to using notifications. So whenever you want to use that, the first thing you need to do is to add the gradle settings or entry here. So inside build gradle. For the app Cradle Module app here. We need to add a new implementation here which is going to be the following. So implementation combat Google Dot, Firebase Colon. Firebase Midas Messaging Colon 20 .1.1 is the current version in my case and we need to sync it once we add it that. Even though, of course, the version might change. So here, if you check out this documentation here, you will always find the latest version. And then you will need to edit your manifest. So you need to add that to your manifest, even though in our case we will need to make slight adjustments. So let's go over to the manifest file. And let's add the service here inside of the application. And for some reason I did copy the starting service part. So let me just give you the final version directly. So here, this is how it's going to look like. I'm going to call this FCM, my Firebase Messaging Service. Android exported this set to falls and then this is the action that we're using. So Android call a name, contact Google dot firebase dot messaging underscore event. Okay, so now. We will also need to add some meter text to our Android manifest here. And by the way, this error will disappear as soon as we create this class here, this my Firebase Messaging Service class, and we'll do that in a second. But first, let's add some meta text and metal text. We can add them at the very top. So here. Matter. Data should be, for example. The one that you can find here. So basically, we can also just copy that. And paste it to our manifest here. And then this one is optional. Let's put that one in there as well, so you can just click the copy button and then let's add this metadata and here as well. Okay. So then you will have these three metadata text with their information in there. And at that point, we can create the class that is still missing and why we get this error. So here let's create this class inside a folder which we will also create, because you can see already it says here that it should be the FCM folder. So let's create a new folder or a new directory in our project here. Right click. New package. FCM. And. Instead of that, we want to create a new content class, which I'm going to call an firebase messaging service. All right, now let's look at the Android manifest. The error seems to disappear. Even though we need to make sure that this class is inheriting from. The Firebase Messaging Service. So basically everything here except to my. And that's important as well. Now we need to add brackets here towards the end and implement our Firebase Messaging Service class. Now, there are, of course, multiple methods that we can use here and override. So let's start with on message received. So on message received. Here. I'm going to rename this one to a remote message because I don't like the name. So here. So it's a little more consistent. Just zero. It's not very useful. So when we get the message, this function will be called. So there are two types of messages. There are data messages and notification messages, and the data messages are handled here. And the message received, whether the app is in the foreground or background data messages are the type traditionally used with GCM. Whereby GCM stands for Google Cloud messaging. Notification messages, however, are only received here and on message received when the app is in the foreground. When the app is in the background, automatically generated notifications are displayed when the user taps on the notification, then he's used returned to the application and messages containing both notification and data payloads are treated as notification messages. All right. The Firebase console always sends notification messages. If you want to know more, then check out the Firebase documentation for that. So thing is, if you want to handle such a message, you can add a log entry here. So log D with a tag. Messages from Colon. Other. Remote message from. So if you want to know where the message comes from, let's import a log here. It's import tag as well. So here is the way where we can now use the or read the remote messages. Then we can also check if the remote message. The data is not empty. Then. We can also have another log entry. So for example, if it's not empty, we can just say something like message data, payload. And then we display their remote message data. So remote message data. Then we can check if the message contains a notification. So remote message notification. Thought let. And here the notification is a Nullarbor and here we can also have another log statement. So let's copy that. Message notification body. And here we can get that by using. I'd. Taut body. All right. So it is our. Value that is given to us. So the remote message notification. So you can see that here this grayed out area. All righty. So that is the function on message received then. We can add another new token override. So here this 1p0 will be the token that we need to pass. So let's replace the stupid name here with token. So called. If the instance ID Token is updated and this may occur if the security of the previous token had been compromised. Note that this is called when the instance ID token is initially generated. So this is where you would retrieve the token. All right. So if you get a new token, well, that's the function that would be called. And then here we can run a little log entry. So let's say we want to know about the new token. Then we would just set refresh token and here is where we would send the registration to the server with the new token. So if we have or should create a new token, well, that's where we would then also want to send the following function. So private fun, send registration. This is one that I create myself. So this is not an override here. Registration. To server and here we need to pass in the token, which is a string motherboard. So in this case, well, we need to implement the functionality to send the registration to the server and so forth. But in the new token, that's where we would call this function and we need to pass into token. Like that. All right, then. At this point, I realized that I imported the tag, but I would like to use my own tag, so I don't want to use this content values tag. I want to have my very own tag. So if you want to use your own tag, but you can do this, you can of course create a variable which is called like that. So you can go ahead and create a companion object which you call tag. And I'm going to call my tag, my Firebase Message Service. So then if I do that, of course, I want to get rid of this content value tag so that I don't have any interference here. Okay. So the tag for the log should be the tag that I have here and my companion object. Okay. And then. There is one last function that I want to show you in this video, but as you could see, we will really need to dig deeper into it later on. So I will create a new function here. Private fun sent notification with the message body which is of type string. So if you want to send a notification, this function should then be called. So I'm going to do that is with an intent. I'm going to say, okay, let's create an intent where I need to pass in the context, of course. So that's important to end here real quick, then I will pass and the activity to which this intent should be sent, so main activity. So the user should be sent to domain activity once it clicks on the notification cluster Java. And then. I will add a flag to this or add flags which will be intent dot flag activity. Clear top. So activity. Clear top this one. As you might remember, this is how we made sure that a specific activity is set to a specific position inside of the stack. So, for example, if we have two activities and we want to put one at the top, then we would give it this. Intent here. Okay. And then as the notification is something that is sent to the user and he might be in another application and we cannot just open an intent from the other application to our application. We need to use a pending intent. So here what we can do is we can say pending intent, which should be pending. Intent dot get activity and let's import pending intent therefore. So here. Get activity and hear the singular. We need to pass in the context. Which is going to be this. Then the request code. And the intent and finally the flex. And here I'm going to use pending intent thought flag. One shot. And this flag says that this activity should or this intent should only be usable once. All right. And by the way, if you want to know more about panning and tense, you can check out here. There's a good description. So basically the idea is that you should or by giving a pending intent to another application, you are granting it the right to perform the operation you have specified, as if the other application was yourself with the same permissions and identity. So you should be careful about how you build your pending application or pending tense and depending intents itself is simply a reference to a token maintained by the system describing the original data used to retrieve it. So we are using this pending intent and then we will create a channel ID for which I'm going to create a string. So let me import our here. And let's create the string. So let's go over to our strings here. Resources, values, strings. And let's create this new, new string here. All right, then we can come back to our channel. So now we have this channel idea and we gave it the name, right? So it's this default notification channel name, whatever we entered here, which is program manage on the score notification channel ID. So I called it and then. Now that we have the channel, we can add the default sound. Uri. Which is the ring tone. Manager dot get default, you or I? Which should be done notification. Ring tone manager dot type notification. So you can see here there are type, alarm type all time ring tones. So the user has specified what type of notifications he wants to have, for example, for a notification such as an SMS. And that's the one that we want to use. So we don't want to use a notification that should be used for alarms only. So the user would be confused why we are using that feature. And then we can create the actual notification. So here, if we want to create a notification, we need to use the notification builder and. Therefore we can use notification compat dot builder. And now here for the buildup, we need to pass a lot of information. Or we can pass in a lot of steps, so to speak. So add to the builder. So first we need to add the context and then the channel ID. Which is this idea that we have created here. And then we can set an icon for this notification. So set a small icon. So this is the icon that is displayed in your notification bar. So you see the little icon and here I'm just going to use our dot rubble. Dot. I see. That icy notification. At least that's how I'm going to call it. So let's create it. So I'll go to new. Vector asset. And here we can select the. And try it. This one here. So let's just use the default android. I'm going gonna use this one as as that is icy notification. Here you can of course, play around with the color, so let's use any color. So it's a little different than the default, the green one. All right. So it's going to be this one here in my case. Then you can set. The content title. So the title of the. Notification. I'm just going to say title for now. Of course, you could create a string for this, but I'm going to keep it simple this time. Then the context text. So the text that you want to display and I'm just going to call this one message then set auto cancel to true. So. Setting this flag will make it so the notification is automatically cancelled when the user clicks it. And the panel. And yeah, that's pretty much it. Then we can set the sound and that will be the default sound, URI, that we just prepared. So this one up there. And then we can set the content intent. So if the user clicks on it, then it should open up our. Main activity. So here pending intent is using our intent, which then uses our main activity. So basically the user money clicks on the notification should be sent to our application. Now we can go ahead. And create a notification manager by using get system service with the context. Thought notification underscore service and as the. Notification service. So here as a notification manager, to be precise. So notification. Manager. And then since Android, Oreo, not a notification channel is needed as well so we can check if built. Thought version thought SDK end is greater equal build. The short version codes thought, oh, so whatever the Android O version code is, we need to import build here. Let's do that. And then we can set the channel to be notification channel. So we had the channel ID, but we didn't have the channel. So now we need to pass in the channel. I'd. Then to name. So I'm going to say channel. Pro Day, Monash. Title. And then the notification manager importance. So here notification manager importance default. So now let's import this notification channel here class so that we can use it, and then we can go ahead and use the notification manager to create the notification channel based on our channel that we prepared. All right, then at that point, we can use the notification manager to finally notify. All right, so notify here. We need to pass an ID. I'm just going to use zero here. And then our notification builder dot built. Okay. So this is a very long function. There is a lot of stuff going on and things that we have not touched on yet. But basically this is how you can prepare a notification and when we call this function now, it will send a notification, so to speak. All right. So this is just the build up. And in the next video, we're then going to actually use this. But first we will update the token itself. So see you in the next video.