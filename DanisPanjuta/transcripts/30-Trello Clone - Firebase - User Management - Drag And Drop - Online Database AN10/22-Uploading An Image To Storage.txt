Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Trello Clone - Firebase - User Management - Drag And Drop - Online Database AN10
Lecture: Uploading An Image To Storage
----------------------------------------

Welcome back. And this video, we are going to use the storage and we are also going to update the user. And before we do that, I would like to talk about two things. One of them is the concept of a hash map. The concept of a hash map is also something that is called dictionary and other programming languages such as Swift. And the idea is that you have a pair of a key and a value, but this whole thing is stored in a hash map instead of in an object. So what we have done is we stored stuff in an object, so we created an extra user class for the user information. But in some cases you don't need to pass in the whole object, but it makes sense to pass in a hash map and it is even required in some cases. So we need to look into hash maps. So the idea is that you, for example, have a string as the key and then a value of the type any as the value itself. So in our case, we will have any as the type that is stored within the hash map and that will be the value type, so to speak. So we have the key email and the value, then it's a gmail.com, then we have the key FCM token, the value is an empty string, then the ID image, and then here the mobile is a different again. So we have the string as the key type and then the value type is the long value here that we can see here, which is a number. We will see later on when that makes sense. And I think I will show you a little more than that. It will be a lot easier to understand. I think the other thing that we're going to use is the storage. All right. So so far we have used authentication database and now it's time to use storage and you can simply get started. So here store and retrieve user generated files like images, audio and video without server side code. So this is super powerful, very helpful. We can just go ahead and store the images that we have in the storage. All right. So let's get started with it. You can see you have to have the security rules accordingly. And at one point. So after you define your data structure, you will need to write rules to secure your data. You should definitely check this out. But for now we can just press next and we can now use it. And here the cloud storage location is going to be the same as we have selected before. So now I'm going to press down and it's creating a default bucket for us. So now we can go ahead and start storing stuff in our Firebase Storage. You can upload files from here directly if you want to. Then you can see that there are certain rules that you can define. So if you want to know more about the rules, view the documents and check those out in a lot more depth. But what is interesting for us is going to be to upload files. By the way, we also have here usage where we can see how much bytes are stored, how many objects there are, the bandwidth that was used so far and everything. All right. So let's go ahead and upload files, but of course, not by using it manually, but via code. Therefore we need to go to our gradle and add an implementation here. So. Let me add this implementation which is com that Google Firebase Colon Firebase Money Storage Colon 19 .1.1. All right. So once you have added that, let's think it. And once it sinks, we can use it. So now let's go back over to our my profile activity. And here I will create a new function which will help me to understand the file extension of a file that we have downloaded. Because if I could have multiple different extension types. Right. And it's interesting for us because if it's an image type, then we can use it as an image. If it's another type, then we cannot use it as an image and for example, not as a profile image. So we're just going to go ahead and create a function which will get the file extension based on the URI. So we get a URI, which is a link, right? And we can see is it an image or whatever type is it. All right. So we can return a string or we can return nothing. All right. It really depends. Now, you see, we, of course, need to return something. And what we will use is we will use the mime type map class. So return mime type map. Get singleton. Thought, Well, here, get. Extension and why we need to import mime type map. Let's do that real quick and then get extension from mime type. And here we can pass in the you or I. So content resolver get type with the URL that we pass in and its our you or I. All right. So let's look over what's happening here. So mime type map. The two way map that maps mime types to file extensions and vice versa. So it's basically a class which allows us to find out the type of a URI then we use Get Singleton in order to get an instance of this mime type map class so that we can use its functions. Then we use the get extension from a mime type. So we basically find out the extension for the given mime type. So we just go ahead and say, okay, you use this URI, get to type of the URI. We used a content resolver here from the context wrapper. So basically we just can use this URI here and then return the. Extension. So the type of it or find it out based on the extension. So if we have a file which is let's just use the Homer Simpson example again. So it's stored right here. Let's use this value. So you can see it has this dot, P and G. So this is the extension. This is the ending of the file. It starts with a dot. So we know, okay, it's of type P and G. So this will be an image. If it's of type MP three, then it will be an audio file. If it's a type MP four, then it will be a video file. All right. So basically we are passing in the whole link. This https and so forth dot png and we will get a return type which will be of type string and it will basically give us the extension type. Okay. All right. Now it's time to upload something to our storage. So private fun upload user image. So I'm going to call this function. And what it should do is it should first of all show the progress dialog. So show progress dialog with our please wait text. So please wait. Actually we have to get it from the resources. So here we sources get string. Ah, dot, string, dot. And here it was, something like, please wait. Yep. There it is. So we have this string, right? Which is called Please wait, this one. So of course, we want to make our application easily extendable and adjustable. So that's why we use this approach. Okay, so we show the progress dialog and then. If the selected image file URI is not empty. So if m selected image file URI, that's the variable that will create it in the last video. If that is not null, then do something with it. So basically we want to then store it in our storage and in order to do that, we need to have a storage reference first of all. All right, so we use VUL as REF, which stands for storage reference will be our firebase. And it is, by the way, the type it's not equal sign here to colon storage reference. We need to import that. It's also from Firebase. Right. So this is a class which is given by Firebase and we can go ahead and use Firebase Storage now to get the instance of it. And here import firebase storage as well. Or write it correctly like this. Okay, then you don't need to. Or actually, we need to import it. Let's do that. Then you enter reference. Thought child. So this is basically the structure that you will need every time if you want to store something. So you need a storage reference first, then you get the child of that Firebase Storage instance. And here you can pass in the name of the image that you want to then use. So in our case, what I want to do is I want to call this one user image. Plus system thought current time in milliseconds, plus a dot and then the file extension. So get file extension based on our M and how did we call it M selected store image URI. Okay, so let's look at it in one line. So it's a little more readable. So basically what we want to have is an image which has a very unique name. So it's going to be called user image. Then it's going to take the current time in milliseconds and then it will add the file extension. Okay. So this will generate a rather unique value for us so that each image has a unique name. So it's going to be whatever this will return. So it's going to return that time in milliseconds, right? So it's going to be a very long, long value, plus a dot and then the file extension that we get from our selected image file URI that we have stored on our device. So that's what we use to get file extension function for so that we get the extension that we can add that to the location where we want to put it. So this is the storage reference, but we want to put our image and now let's put the image there. So we can go ahead and say as ref dot put file and which file do you want to put there? Well, our m selected image file, Uri. Now, the thing is, this is ineligible. So what we do is we force it here because we check that it's not null. So we only do the code if it's not now. Right. And then when we put a file onto our storage, we can check if it was successful or not. So here on or add on cancel listener, add on complete listener, adult success listener. So all of those are available again. And I want to use the Add on Success listener. And there I'm going to get a task snapshot. And as a result, I want to then use this test snapshot to log it for now. So what I'm going to do is I'm just going to take the snapshot, get the metadata from it, get a reference from it, get to download URL from it and make a string from it. So basically with this line here, I make a string out of where the file is stored, and I'm just going to log that as an error into my log saying Firebase Image URL. You could of course use info here as well instead. So we're going to get an info saying Firebase Image URL and then the image URL where it is stored and we can later on check this value out in our logcat. All right. So what is happening here is that once we put the file, we get an on success listener, right? And when it's once it was successful, we get a snapshot of that task. And that task will give us the download URL. And we need that download URL in order to store it in the user data as well. So in order to store it here, right, if we want to store it here, we need to get the link the link of the file in the storage. All right. So in the storage it gets a unique link. And now we want to also store it in the database. So let's prepare that. Therefore, we take this task snapshot, we get its metadata and so forth. So we pretty much do the same thing as here. Except for this two string part. And we add another unsuccessful listener here. So here this time we get a wry. Which is the actual link. Now we can go ahead and store that URI somewhere. So what we can do is we can have another log entry. Which is going to be the you or I as a string so downloadable imagery and that will now be the URI to string. And now what we can do is we can go ahead and create the variable in which we store that value. So let's go ahead and create a new variable here. So we have the selected image file, URI, which is on the device, but now we need to have the downloadable uri. So private var m pro file. Immature. I'm going to call it simple string. Because that's gonna be fine as well. An empty string for now. So the profile image URL. And now we can go ahead and set that. So in the upload user image function. Here. We can now go ahead and set it. So let's say m profile image, your URL is going to be a URI to string. So the URI that we get as a string will be stored in EIM profile image URL. Now at this point, what we want to do is we want to update the user profile data with this profile image. So here to do update user profile data. So we will need to create a function which will update the user profile data, which includes the name and all of the changes that the user could have made. Right. He could have changed the email. Well, not that, but the phone number, his name, and then whatever settings you want to add later on as well. Okay. But of course, when something can go right, there's also something that can go wrong. So let's add. For this part here. Ford is putting file something into our database or into our storage and on failure. Listener So what shall happen if we get an error here? So then we will get an exception, right? And what do we want to do with this exception? Well. What we're going to do is just write a little toast with the message. So we're just gonna take this exception and then display the exception. Message. And of course, what we should also do is hide our progress dialogue, because otherwise it will be constantly displaying if we had the error and will not disappear. All right. So at this point, let's just test it real quick. And in order to be able to test it, we need to give our update button a functionality. So let's go ahead and use the button update. Set on click listener. And here we're just going to say if selected image URL is not empty so it's not null, then run our update or upload user image. Of course it's not done yet. We still need to do a lot more here. So what if the image URL is empty? What do you want to do then? Well, then we just want to update the other information. For example, if the user changed a name or changed the phone number, then we still want to update those. Right. All right. Well, now let's test it with what we got. Okay. So let's go over to our profile. Now let's select the image of former. And let's see if we can upload anything. Okay. So let's press update. Please wait. And of course, we don't have the functionality that will close this because currently if we look at our code. When we upload it, upload image URL, we start a progress dialog and we only close it in the on failure listener. So here this hide progress dialog. Of course, we will later on do that in the updated user profile data. So this hide progress dialog. But for now we can do it in here as well. But now let's just see the logcat to check out if we get something. And what we should get is a downloadable. Image URL. So let's search for downloadable and we can see downloadable image URL is Firebase Storage, Google APIs dot com slash V and so forth. So the name of our project here. App spot dot com slash user image and then this. Value and then dot P&G. So it's 158255. Search for these values here in our storage. And let's see if we got it there. We can see 158255. All right. So the image is here. And if we check out this image, we can see it's this Homer image. It has roughly 75 kilobytes. But overall, you can see it works. So now you know how you can store something on your Firebase Storage. But of course, what we want to do is also store it in our database here as well. So where we just update the image here with the new URL and that's what we're going to look at in the next video. So see you there.