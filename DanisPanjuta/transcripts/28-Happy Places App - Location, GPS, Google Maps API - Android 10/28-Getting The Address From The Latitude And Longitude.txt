Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Happy Places App - Location, GPS, Google Maps API - Android 10
Lecture: Getting The Address From The Latitude And Longitude
----------------------------------------

Welcome back. Now it's finally time to make use of the feature that we have implemented, which is this set current location feature. So when we click on it, you can see now it's going to give me this location here, which is the location that is by default set up and emulator and that is the Google office. All right. Google head office. And you can change the location if you want to. So here you can play around with the location. For example, you can just assign this value here unnamed road, so maybe we should use another one. Let's go here and select this road here. And set the location. So now let's press this button again. And you can see now it changed it to the location where we are. So we can test this quite a bit, or you can play around with this as you want. But generally what I want is this feature here. So the location as it is staged here. How do we get there? Well, there are many ways, as always, but I personally prefer the way where we create a helper class which will do the job for us. So I'm going to create a new Kotlin class. Which will be in class here and this will be called get address. From, let alone so from the latitude and longitude. And this class should be an async task, so it should run asynchronously because I don't want to block the main thread, so I'm just going to say inherit from async task. Now when we inherit from async task, we need to pass PN three values here void string and string as we've seen before. It's at least that's what I'm going to use here. And then I, of course, need to adjust my constructor here. So the constructor will be that I pass in the context so in which I wanted to run this async task. So let me input context here then the latitude. So private vol latitude, which will be a double and then private vol longitude, which will also be a double. All right. So that is going to be our. I think task class here. And of course, if I create a class which is async task, it should implement its members, which is due in background. So we have this function doing background and that is what we want to execute in the background. But as you also maybe remember is that we had some other functions here as well, which I also want to use, which is on post execute. So that's going to be the other method that I want to use in here. And then what I would like to have is an interface. So I'm going to set those things up before I implement the actual methods here, as you can see. So I want to use an interface here and that will be an address listener. So we're creating our own listener here, which will have two functions on address found. So what do we want to execute when the address is found and we pass an address here and then another function on error. So what shall happen if we get an error? All right. So this is going to be our interface that we're going to require for this. All right. So the next thing that I need inside of my class here, inside of this get address from latitude, longitude is going to be a geo coder. So geo coder is going to be the magical class which will take care of actually making a location, a readable location from latitude and longitude. All right, so let's create this variable here, private. Well, geo coder, which will be of type geo coder. And I'm going to create a geo coder object here. So geo code dir and here I need to pass in the context. So I'm going to use the context that is passed to this class. So here at the top you can see when we create an object of this class, we will need to pass in a context. And that's going to be the context that I use for this geo coder. And then I'm going to or I have to pass the local dot and here I'm just going to use the default of the device. So you could of course use local and an English, German, Chinese, whatever, but I'm just going to get the default of the device. So it will be different for every setting, so to speak. Next, I create another variable here which will be pilot initialized. Which would be a private. Late in it. Var M address listener. Which will be of our address listener, of course, which is this interface that we created earlier. All right, now comes the actual magic, and that is going to happen in the background. So we're going to use this decoder. And I told you earlier that the studio coder is taking care of encoding the geolocation to something that we can read or the latitude and longitude into readable text. So that's what we want to use inside of our dune background. So that's basically what we want to have the result of, right? So I'm just going to create a new list of results that I get from it, which is going to be a list of addresses. So here you can see there is this address class in Android available and we're going to use our geo coder object to get something from the location. So you can see there are two options. You can either get it from the name or you can get it from the location and we want to get it from the location because that's the latitude and longitude. So here we of course pass in the latitude and longitude which are passed to the class when we create an object of it. So here at the top of the construct, you can see whenever we create an object of get cross from latitude and longitude, we need latitude and longitude and then get from location requires one more variable here and that is the amount of results that you want to have the maximum results. So when you have the latitude and longitude, it can happen that there are multiple addresses at that point and I'm just going to take the most likely one or just one of them. So I'm just going to assign max results one. Now, the next thing that I want to do is if the list is not empty and if there is something in there. So if address list is not equal, null and address list is not an empty list, so is not empty. Then I want to use that address and break it into parts so well address which will be the address that we get from our address list at position zero. So from this list here, which gets the value from the location using the geo coder, I take the very first entry and I store that into this variable called address and then I need a string builder because this address is not actually a string, right? And I need to get the data in form of a string because as you can see, doing background is going to return a string here. That has to do with the string up here. All right. So it's going to return a string. So I'm just going to say, let's use this string builder object in order to go through all of the addresses that we have. So I n 0.. address. So go through all of the address space available and it's here with double D address, dot Mex address, align index. And then this is of course going to be the largest index currently in use of the specified address line. So an address can have multiple lines, it can be a very complex address and we just want to have all of them. So the maximum line index, so give me the whole address, so to speak. And the address consists, as I said, of multiple elements. So. Admin area. Sub admin area locality, sub sub locality and so forth. You can see postal code, country code letter. So it's a super long thing. It's a super long, detailed information. And what I want to do is I want to get all of it. So just say, okay, go through all of it and then append that. So use SDB or String Builder to append this address. Get address line at the position I and append. And empty space in between because otherwise it's not readable for us if it's all connected to each other. It doesn't look good. So that's what this line will do. And then. I will use the following line where delete a character at the end. So just delete the very last character because I don't want to have an empty space which is not required at the end. So this is what this line is going to do. So delete char at SB dot length minus one. So whatever the length of this string builder string is going to be. So string builder, by the way. Is this? Plus here construct a string bill with no characters in it and the initial capacity of 16 characters. But it's going to be more than that in our case. And then I finally can return that string builder. But the thing is, the string builder itself is not of type string, but we're going to make it a string and beautifully enough, it's going to give us the right type anyways. Okay, so this whole code here can go wrong. So I'm going to put all of that into I try and catch block. So let's do that. Try. All of that. Catch it if something goes wrong with a generic exception ee which is of type exception. And then just either print stack trace or just print it onto our console and otherwise return. An empty string. So what we're saying is if. Try and catch for whatever reason didn't work out. It's just going to return this empty string. And if the try block worked and we are inside of this if block where we have an address list which is not empty and not null, then we return the string that was created from it. So basically the address. So now this method is fine, but we still have this on post execute which we're not using and we have this address listener which we're not really using. So how do we properly use it? Well, inside of this on post execute, what I want to do is I want to check if the result string. Which is this string here. I'm gonna recall this one or rename it. Result string is more to my liking. So if this result string is null, then just call the address. Listener on error. And otherwise call em. Address listener on address found. So if we have a result string which we get on input on post execute, then we want to use it. And of course this should be result string. Now there are two more things that we still need to do. And one of them is to set this address. Listener. So fun. It's a public function that we're going to create here. Set address listener which will require an address listener. And it should basically just assign. The address listener that is passed to our variable called HM Address Listener. So when we call this function, we need to pass an address listener and that one will be assigned to the address listener of this class, which is this late in it variable, which we of course need inside of our on post execute because otherwise it will not work. So we need an on an address listener that we of course create ourselves. And here, by the way, on address phone needs, of course, a result string because it's still complaining here. Right. So it needs to have a string because look at this function. You can see that requires a string here. And then finally we need one more function and that one was going to take care of getting the address. So I'm just calling it like that. But what it really just does is it executes this. Whole async task. So and async task doesn't do anything until it is executed. And I'm just going to say, okay, in order to execute, just call this public function called get address. All right. And at this point, we can use our beautiful class that we prepared, because now you can re-use this class whenever you want, whenever you need the address from the latitude and longitude. So you can just paste this class in or copy this class into your other projects that you will use in the future and use it. It's perfect. So let's go back to our. Spot where we actually need it. And that's the ad happy place activity. And here what would be a good spot where we can put this? Well, it's inside our location callback here. All right. So when we get the location result, currently, it's the latitude and longitude which we just log, but we don't do anything else with it. And now it's time to actually transfer it into the right format, because now we have the latitude and longitude and we can use that in order to send that data to our address from latitude and longitude class. So let's go ahead and do that. Vol address task is going to be an object of my get address from latitude and longitude and here I need to pass in the context and the latitude and which is going to be our hm latitude and longitude which is our MX longitude. All right. So this task. Has the function where we can set the address listener and it's going to be important that we do so. So by the way, this is complaining because we're not in the right context. So I'm going to have to add the add happy place activity in here because that's where we want this to be executed. All right. So now we can use this address task, which is an object of our get address from latitude and longitude to call its function, which is the set address listener. But if we do that, you can see we need to assign the whole address listener which is a new object. So I'm just going to create a new get address or actually within Java here. So we need to use object here, get address from latitude and longitude dot address. Listener So I'm actually passing the address listener from the get address from latitude and longitude here as a new object, I'm creating a new object from that and then I need to implement a according or the right functions here, of course. And which are those? Well, on address, found and on error. So if we go back here, you can see we had these two functions on address, found that on error and we need to implement them here. So let's overwrite an address found. So here on address font. Which needs an address of type string Nullarbor. And of course one to override fun. So I was hoping that the idea would pick up on that, but it doesn't. So that's the one function. And then we had the other function overwrite fun and that was on error. So what do we want to do if we get an error here? All right. And there. What I'm just going to do is I'm going to write a little lock entry log E. Kit address. And the message should be something went wrong. Super useful error message. But I think that's going to be fine. And in my case, still complaining. That's because I'm closing the bracket here and it's not so happy about that. So now I need to close the bracket down here. So this whole thing here is actually just one line of code, so to speak. And of course, on address found there, I still want to execute some code. And that is going to be the underscore location. And that's the final step. Set text address. Actually, it's the second last. Of course, we need to call get address at one point. So address task. Third get address, which is executing the whole async task. Okay. So what we just did here is we created an object of our address from Latitude and Longitude class, and then we set its listener to be a new object of an address listener, and which we override the two functions on address found and on error, which are required as we have defined it in the interface. And now the moment of truth is here. Let's run this application once again. So let's go ahead and create a new location here. And this one will be Google. Head office. And. That's where the Google Magic happens. And then let's set the current location and of course, the new location that I assigned earlier. And it's not the actual Google location. I don't know how to reset that here, but let's just take any value. Let's set the location. Let's test it again and we can see it works perfectly. All right. So at this point, we can, of course, go ahead and add an image here, select one from Gallery. And it's going to be. The beautiful image here from the camera, you can see that's how I think it looks like in the Google head office. And then when we save the location. We can find the location here. Now let's click on it. We can see the location is down there. And that's where the Google Magic happens. Let's view it on the map. And to concede zooms all the way in. Well, not that far. It zooms in up to ten. So the settings I had set last was ten. But of course we could go back to 15 to zoom in even further. All right. But that's it. So now you see how you can create such an application, which uses a bunch of new features that we have not seen before. And the cool thing is because we wrote the code so cleanly, you can just use a bunch of it for your own project. So this whole database handler, the adapter, well with some minor changes, you can use that as well. The Happy Place model is a great example of how a model would look like and all of those utils, for example, the swiping to delete and to add it. They are amazing as well as this get address from latitude and longitude. So I think that these whole classes that you see here will make your life as a developer so much easier in the future once you have projects which require any of those capabilities. Okay. So thanks a lot for being part of this project and following along and coding along. And yeah, I'd say see you in the next one.