Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Happy Places App - Location, GPS, Google Maps API - Android 10
Lecture: Storing The Images On The Device
----------------------------------------

Welcome back. In this video, we are going to add the functionality that allows us to store the image on our device because currently it is not stored on the device. It's, of course, in the gallery. But the gallery could be changed by the user any time. So he could go ahead and delete the image and then, well, we cannot get the image anymore and we want to have the image, of course, in our Happy Place app here in this screen where we will then later on have all of our happy places in a list view. All right, so how do we get that? How do we fix that issue and store the image permanently? Well, we can just go ahead and take that image and then, well, just add the code to store it permanently. And therefore, we are just going to go ahead and go back down here to our companion objects. And I'm going to create a new. Constant here, private cost value. And I'm going to call this one image directory. Because that's going to be the folder on the phone where I'm going to store the image. All right. And the text that I want to. Use here is happy places images. And then I'm going to create a new method here, which I'm going to call save image two internal storage, because that's what it should do. And what should it store? Well, it should store in bitmap. And what shall this return? It shall return a yuri. So the location of the image that we're storing. All right, so that's the idea of this method. And then what I'm going to use is the wrapper. And the wrapper should be our context wrapper. And that is going to be the application context. All right. So context proper, I'm going to pass the application context to it. So context repr as you know, extends context. We saw that in the last video where we also used the. Context wrapper before the content resolver. Okay. So now we can use the application context this way. And then we need a file. And this one can be a variable because we're going to override it. It should be wrapper, dot get directory and we need to context wrapper by the way, in order to get the directory of our application because it has a specific place on your phone in which you can store images, for example, or store files in general. And we're going to use that. So we're using this context wrapper that we created here to get the directory, and I'm going to use our image directory here. And then you can use context and different modes here. And I'm going to use mode private. Mode private is the mode that allows me to make this file only accessible from the calling application or all applications that share the same user ID. So basically, other apps will not be able to access this directory, so they will not be able to access our images that we store in this application. All right. And then we can go ahead and use file path in a file and the name of the file. All right. So here we need to import file. So here we got the directory. Now we can use this directory to create a file. So we say, okay, the file should be at this directory and then it should have a random unique identifier name. So here we can use this code here. Juju id dot random. Use your ID. Dot. JPEG. Okay. So it's going to store this image as a JPEG and it's going to give it a random, unique user ID as the name, so to speak. So this one will be random each time. And by this we make sure that each image is unique. And this is, by the way, the unique user ID you ID. Okay. Now that we have the file, we can store it. And you probably remember how we did that. Or maybe remember it. We used the file output stream for this. So we can go ahead and use file output streams and stuff because things can go wrong. We should use try here. All right. And then catch as well. So I'm gonna catch an input output exception here. And I'm just going to say, if something goes wrong, then. Right print stack trace. So this is just going to be for us for testing purposes. If something goes wrong, we will see that in our logcat. All right. So then the next thing is that we need to create a stream which will be our output stream, because we're trying to output an image to our phone and it's going to be of type file output stream. All right, then. This file output stream needs to have a file that we need to pass in, and the file is the one that we created here. So we're just saying, okay, use this file. So the directory with this name and then go ahead and compress our bitmap. So here, bitmap dot compress with bitmap. So compress. Former. And the compressed format. I'm gonna use JPEG here, the quality I'm gonna use 100% here. And then we need to pass in the stream. All right. So this dream that we created here, so that is part of our compressed method. If you want to know more about it here, compressed method needs the compressed format, the quality and the output stream. All right. And that's exactly what we prepared here. So we prepared the output stream. We have the quality set as 100. And the bitmap that we want to use is going to be the bitmap that is passed to this function that we just created. Okay. That's not it. We still need to add some more here. For example, we should flush the stream once we are done compressing. So flush it and then close the screen. So our stream dot close. Now the method itself is, of course, still not happy because as you can see here, it's expecting a URI to be returned. So now we of course need to return a URI dot parse file dot absolute path. So what I do here is I'm saying, okay, our file has a path, right? So we created a path here, the directory and the name. So this is going to be the whole path, so to speak, so that the whole directory, the whole name, how you can find the file and we use that and then we pass that into the format of a URI. And we'll return that. So we're just creating pretty much whatever the format is that we can later on use and that can be readable later on. So the right format here is a class again. Of course, it's a. An abstract class, as you can see here. So if you want to know more about this, your class, you can check that out. All right. So now what is left to do is we need to pretty much store this or use this method that we just created. And a good spot would be in our own activity for result method. So it's all the way up here on activity result. So here we expecting a result and currently what we're doing is we just set the image in our. I've placed image. So this is the selected image and actually. We should do that here in our gallery, but also when taking an image. Okay. So here we're going to call this method, but it, of course, needs to have a bitmap. And guess what? We have this bitmap here, which is the selected image bitmap. Now, the thing is, I cannot test it this way because we currently don't have any way of reading this image from the storage. So what I'm going to do is I'm just going to create a new variable and I'm going to call this one save image to internal storage. So the same name as the method that we created. And I'm going to create a little log here, log E So that is nicely red. So it's going to be an error log here. We can of course use verbose or D or I here as well, but E is definitely visible. So I'm going to call this one saved image. And then the message should be a path. And our saved image to internal storage. All right. So that's number one. And then we need to do it one more time. So I'm just going to copy those lines here. I'm going to do one more time when the image is our thumbnail here. So when we get it from the camera, I'm going to put it in here. And the image, of course, will not be our selected image bitmap, but it will be the thumbnail. All right. And now we can go ahead and test it to see if the image is actually stored and where it is stored. All right. So let's select an image from the gallery here. And I'm just gonna use this image here. There we are. So now let's see in our logcat if we get an error. Somewhere. So here error. And couldn't load Amtrak module. So there are a bunch of errors. Maybe it's easy if we just search specifically for saved image. All right. So that we are saved image and the path is going to be data user zero. The name of our directory or of our project namespace. Then app happy places images. And then that is the usual ID that we used. All right, now we can test this once again. But this time using the camera. So let's add the image capture photo from camera. It's just like any image here. And we are. So now we have a new unique image ID that was used. So these two images, they are stored now. And when we later on want to read them, we will be able to read them. So that is one of the first steps that we have here in terms of storing the images and then also reusing them and displaying them. But of course, there is still a lot to do in the next few videos. So I'd say, see you in the next one.