Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Happy Places App - Location, GPS, Google Maps API - Android 10
Lecture: Fixing The Updating Duplication Bug
----------------------------------------

Welcome back. In the last video, we had this little book where we recreate it or duplicated entries when we edited them. So we kept the old entry, but we created a new entry with the updated values. So let's fix that problem. First of all, we have to go to our database handler because that's where we handle all of our database stuff. And here we had to create an update and then we had this fun add happy place function. And we're going to create a similar one, which I'm going to call update happy place. Now we could, of course, go ahead and create functions which are depending on a entry that you have here or a parameter. But I think in this case it really makes sense to have two functions one for adding, the other one for updating, even though they are so super similar to each other. All right. So first of all, we need a database that is writable. So that stays the same then. Everything else should also stay the same except for this part where we insert something. So instead of inserting something to the database, what we want to do is of course do the update something. So let's create this variable called success and call DB update. So database which is this writable database, dot update. And then we need to say what we want to update. So I want to update the table called Happy Place with the content values. And then we also need to add another value which is our work clause. So we want to update it on a specific entry. So here. I'm going to say at the point where the key ID. And here key on the score ID. Is equal to. Our happy place. Thought I'd. And of course, a plus is missing. So I think it makes sense to put it in a new line here. So we have this update method and there we pass in the table the content values and then we say where we want to change that and that will be changed where the ID is the happy place ID and we need to call the where arguments pass in the where arguments here as well I'm going to pass and know. So in this case we are updating something but only at one point and that is going to be where the ID is the same as the happy place ID that was passed when we call the update happy place. So we pass in an object of type happy place model which contains of course all of the information such as the title, the image and so forth. And then we just go ahead and update all of that. And now I'm just going to return success. Now, the thing is, when updating and calling DB update, you can see it returns an integer. But our update happy place currently returns it long. So we need to change that because when adding something you can see here when inserting insert returns a long and that's why it was totally fine to use a long here as the return type for our add happy place method. But now when working with the update happy place method, we're working with integers, so we need to return an integer because the success here, which is the result of DB update is an integer. So when is that even relevant? So when do we call either of those methods? So either add or update. Well, it's this safe button here. So once we. Press on this button here at the bottom. So it's inside of adding this happy place. So let's go to add happy place and we have this. Button. Function so it's inside of our on click so here. When we press button save, we do all of this stuff and then we have a while happy place model. So we call DB handler at Happy Place. So this is the spot where we should make changes because now we don't only want to add happy places all the time, but we really just want to do that. If we create a new entry, otherwise we want to update a happy place. All right, so the changes start right here where the ID will either be zero or it will be the ID of happy place details. So here it's going to be if m happy. Place details is null. Then use zero else use em. Happy place. Details. ID. Okay. So what is this rm happy place these days? Well, it's a happy place. Model object that we created here at the highest level, and then we set that 1 to 1 point. So here, if we the intent has extra, that's where we get information about the happy place model that we need to change. So if that exists, then we want to update if it doesn't exist and we want to add a new entry. So we need to go back to. That part here. All right. So this is the if statement. And now finally, we need to check when calling this function. So here we have the DB handler and now either we call ADD happy place or update happy place. All right, so here, let's create another if statement. If M happy place details is null. Then just go ahead. And through this code here. So run the ad happy place and also check this one here. So if statement two as you did before. But otherwise, if it's not null. Then go ahead and do the update. Happy place. So it's pretty much the same thing, only with the difference that it's now called update happy place. And of course, the function is also called update happy place. And now we just need to pass in the model. And of course, here, check if the result code. Is greater zero, which means there is no error. All right. So going back to our update, happy place. You can see it returns an integer. So if that integer that comes from the success. So from the attempts to update something, if that is greater than zero, it means that things worked out. And then we can go ahead and use it. And the rest can stay the same. So this is pretty much it. What we need to check here. So let's now test it again and see if the bug is fixed. All right, so there we are. Let's change test without anything. Let's call this one test. Number one. And press update. And you can see now the first entry is test number one. And if I scroll, you see no changes were made or no additions were made, no new entry was created. Now let's see if we create a new entry. So test. New entry. Test new entry description. And now the location. Somewhere. Far away. Add an image here. And now press safe and we can see test new entry test new entry desk. So that worked perfectly. All right. So that's already a pretty good step. You could see you could go ahead and create a to do list now with all the knowledge that you have. So you could use pretty much everything that you've learned so far and create a beautiful to do list app, which would really do well, I think. But then of course, we want to add a bunch of new features which include the location. So we will see how to use Google Maps and everything else and the next few videos. So see you in the next one.