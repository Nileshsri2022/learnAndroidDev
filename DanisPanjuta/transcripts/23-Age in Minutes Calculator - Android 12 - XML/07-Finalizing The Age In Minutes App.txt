Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Age in Minutes Calculator - Android 12 - XML
Lecture: Finalizing The Age In Minutes App
----------------------------------------

Welcome back. So we can select the date now and that works. But now let's actually calculate how much time has passed ever since. So let's go back to coding. So we have the date now and this is just using this simple date format that we have prepared, and it's parsing that into the data variable. But I want to know the selected date in minutes. So to date that we're getting there is not giving us so the selected date in minutes. So how much time has passed ever since? In minutes. So what I want to use for that is going to be a variable that will be called selected date in minutes. And in order to get that, I can use the date variable here and get its time property and then divide that by 60,000. So why would I divide it by 60,000? Because a date. So the date property, if we look at it here, gives us the details and the time that has passed. Ever since a given moment. So here does get time method for example gives us the number of milliseconds. So January the first, 1970 at midnight. So when you use get time, you get a super long value. This is a long data type, it's a very long value. And if you want to get minutes instead of milliseconds, you first of all have to divide by 1000 to get to seconds and then another time by 60 in order to get from seconds to minutes. So if you do both in one step, you divide by 60,000 and then you get the time that has passed since 1970 in minutes. At this time. Property is the same thing as if we were asking for get time. So it's the same as if I would use get time here for example, like so but this is this get time is the old fashioned way. We can just get this time by using the property access syntax dot time as you can see here. So now we have the selected date and we need to calculate how much time has passed between the selected date and the current date. So let's get the current date. Current date is going to use the SDF pass. And here we need to use the format so we could, of course create the format object here and so forth. But I'm just going to do it in one step as the F format where I use system dot current millis. So what this will do is it will give me the time that has passed since the 1st of January 1970 in milliseconds. That's how I can basically use this approach here. Here we get how much time has passed since the war between the selected date and 1970 1st of January, and here we get the current time. So how much time has passed between now and 1970? So that's the current date that we get. And now we need to calculate the difference in minutes. So let's get the current date. And we need to calculate that in minutes actually. So let's create a valid current date in minutes. Which will be the current date time divided by 60,000. And now we can use the current date and minutes minus the selected date and minutes. And this will give us how much time is between those two time points. So between the selected and the current date. So now we can use this difference in minutes and actually assign it to our text view that wants to know about it. And it's this text view here which currently doesn't have an ID, so let's give it an ID, you probably know how to do it. You just use this android ID add plus ID slash, or you could have used a designer to define the ID here or here directly. Okay, but I'm going to show you how to do it in the code. Let's call it TV Age in minutes. Okay. So now let's access this text view in our code and therefore we need to create another variable up here. So private var. And I'm going to call it TV in minutes, which will also be a text view optional. I'm going to set it to NULL and then I can initialize it here in my on create. Method and I will find it by its ID which is our dot id dot tbh in minutes. So here we are assigning the text views and this agent minutes is this text view. Here, by the way, you can get there by holding the control key, clicking into it, and you see which text view it is and the split view you can click into here and you will see that it's this one here where it currently says 40 million. So that's the one that I want to override. So in the main activity now we have access to this TV page in minutes and we can actually assign it. So here TV age in minutes, dot text should now be the difference in minutes. Now, unfortunately, the difference in minutes is of type long. So we need to convert it into a string for this to work. And TV age in minutes here because it's a notable we need to add the question mark here for type safety. Okay. So at this point, let's actually test this. So let's test it once again. And that selected date. Let's select between. Yesterday and today. It's not yesterday, but it's a little longer. So you see here, 1440 minutes have passed ever since that day that I've selected. And now so let me select the 1st of September. Ever since 20 minutes have passed. Let me select a date in the future, because today is the 21st. So let me select the 22nd. And you see, it's some time in the past, even though for some reason my phone thinks it's a different date. I believe it thinks that it's September the 15th. So definitely something wrong with my phone here. It's still stuck at the 15th and thinks the current date is 15th, even though it's the 21st. But if I were to test this on a real phone, this would work. So how can I limit the selection to dates in the past? Because I cannot be born in the future, right? Well, that is something that we can do with a neat little trick. So instead of showing this date picture dialog here directly, what we can do is we can put this entire date picture dialog, which is all of this code, right? This is all of the code that is part of our date picker dialog. So we can put that into a variable. So I can call it dpkg for example, and paste all of that in there. So this DPI value more variable will now be our date picker dialog. So let me shrink it down. And now well in here with this minus I can collapse the code. You see, all of this code now is going to be reduced at least from a visible point of view. And now we can show the date picker dialog. Let's just test if the date picker dialog will still appear as we wanted it so we can see it's the works. Let me select the date here. That's the works. And now instead of just showing it, I want to add another property to it. And here I can set the date picker property. To be set to a specific max date. So here there is this max date and I can assign a max date to it. And here, in order to set the max date to be yesterday, I can just go ahead and set it to system the current time in milliseconds -80 6400. So 86,400,000 milliseconds. So there are 3.6 million milliseconds in one hour. So multiply that by 24 and you get 86.4 million milliseconds. So let's test that once again and I'll select the date in the future. While you can see I cannot select dates in the future anymore, I can only select dates in the past. So for example, one day in the past and you can see now it says 1440. That's how many minutes I'm old if I'm one day old. Exactly. Okay. So now we can make a couple of changes here to our code. Because we are using the date here. And now if you look at our code, you will see that there are some parts of the code marked. So this dead date, for example, is highlighted or has a background color here, and that is because we're not using type safety. So what you can do here instead is you can make it null safe by using the following approach, by basically creating this selected date in minutes if the date is not empty. So here the date question mark let. And now do all of this part here and side of it. Okay. So only if the date is not empty. Then run this code. The same goes for the current date. So here we can use current date. Question mark. Do not let and then execute the rest. Only if current date is not empty. So this dot let is just an approach to make sure that we only execute this code if the date is in fact not empty. If the date is empty for some reason, then we will not execute the code. So this way we are making sure that our code is not crashing. And this is just null safety that we are implementing here. And then another thing that we can optimize is that we can make sure that this click data picker is private as well, so we can make it private so that it will only be accessible from this class and from no other class, which may totally make sense for our particular case. So if we ever want to use this method from somewhere else, we would just have to make it public again and then we can access it from the other classes. But in our case, as this is never going to be required for now, we can replace that. And another thing you see here, this view is never used. So we can basically just replace it with an underscore and the code will work exactly as it did before. Okay. So that's the highly optimized code that we have here and now. We can feel confident when we publish this application. So in the next lecture, I have a little challenge for you where I would like you to rebuild this application, not displaying the age in minutes, but actually the age in hours. So good luck with that and see you in the next.