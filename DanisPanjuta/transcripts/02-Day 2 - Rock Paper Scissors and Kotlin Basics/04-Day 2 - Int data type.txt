Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 2 - Rock Paper Scissors and Kotlin Basics
Lecture: Day 2 - Int data type
----------------------------------------

All right. Now let's look at data types. And Kotlin has a very great documentation. So I would highly recommend to check it out every now and then. Obviously you can use ChatGPT as well along the way if you want something explained in like for example, in a way that a five year old would understand or something like that, but I want to just quickly show you the documentation while going over the basic types in Kotlin, and read through it a little bit and give a little bit of additional information. So let's look at the basic types. All right. So I'm at Kotlin lang.org/docs/basics types HTML. And you can see it's under concepts types. And then there are basic types. But there are also type type checks and casts. But we're really going to focus on the basic types for now. So in Kotlin everything is an object in the sense that you can call member functions and properties on any variable. Now that is already going way over my head. If I'm a beginner, which you are. So I'm not a beginner anymore, obviously. But um, as a beginner this might already be too complicated. So. But still, let's go over it a little bit and then we can come back to it. And also I will show you how to use ChatGPT to make it even easier to understand something. If you want to understand what's going on on this page a little more. All right. So, um, let's go over the next sentence. So some types can have a special internal representation. For example, numbers characters and booleans can be represented as primitive values at runtime. But to the user they look like ordinary classes. Okay. Again we don't know what classes are. We don't know what objects are. So it's basically trying to explain us the very core concept, the very basic thing with things that are a lot later on in the knowledge base, like in the flow of knowledge that you're going to acquire. So I think that this is a very poor explanation for a beginner, but still the overview is good. All right. So let's look at the overview. So we have numbers and their unsigned counterparts. All right let's look at what numbers are. So Kotlin provides a set of built in types that represent numbers for integer numbers. There are four types with different sizes and hence value ranges okay. So integer numbers are basically whole numbers. So they don't have a decimal point in them. They're full numbers right. But now depending on the amount of digits that number has or like the length of the number, there are different types. So why are there these different types? Well, the idea behind this is that if you want to build a super performant application, you would always use the type that uses the least data or storage, so to speak. So for example, you can see that here type. Then you can see size in bits and the min value as well as the max value. Okay. So let's look at the integer types for now. And we can see that a byte has eight bits. So a byte that's where basically the byte came from. So if you look at a hard drive that you have it might have for example one terabyte. But this one terabyte is roughly eight tera bit okay. So that's where this bit and byte thing comes from more or less okay. So it has a minimum value of -128 and a maximum value of 127. So how does that come about. Well the thing is what you can basically do is you can take two and then take the exponent of eight. Two by the power of eight is 256. So if you take 256 and divide it by two, then you have 128 for the negative numbers and you have 128 not quite for the positive numbers. So in this case zero also has to obviously be somewhere. Right. And that's why it doesn't go all the way to 128 here. Because there is the zero in between. So you have minus one on 28 all the way to minus one. And then you have zero all the way to 127. And that's how this byte comes along. And you would use that for example, for super performant graphical applications where if you only need to store a information that is super small, like a number between 1 and 100 or minus one and 101 hundred, then you would use a byte so it wouldn't make sense for you to use a for example, integer that requires 32 bits. So four times as much storage on your device on your on your Ram, so to speak. Because this is taking up Ram. So the read access memory I believe is the what what it's short for. So what's basically happening here is with each next type you double the power. So by the power of eight now you have by the power of 16 that's the next thing. And that's where you have the short data type and that's going. All the way from -32,768 to plus 32,167. So there's zero in between again. And that is two by the power of 16 is 64,000 65,556 I believe. So that would be for bigger numbers. But then if you want to have even bigger numbers, you would use an integer which is the default. So nowadays storage isn't that much of a problem anymore. Or Ram also isn't that much of a problem anymore. We have loads of it, right? So that's why most of the variables will just by default be integers, because, well, it gives you enough freedom. So it's basically minus two by the power of 31 all the way to two by the power of 31 minus this one number, which is zero that you have to put in there as well, which gives us numbers between 2 billion or -2 billion and plus 2 billion. And then if you want to store super long numbers, then you would use a long data type. And you can see it goes like all the way to minus two by the power of 63, which is this number of like what is it? This is a billion, uh, this is trillion and quadrillion, I believe. And then quintillion or something like that. I don't know how the American, uh, the English uses the zillion thingies in German. That would be million, million, billion, billion. And that would be trillion. So 9 trillion, which is something crazy in English. Crazy numbers. So a very long number that you can store, you can store phone numbers using long, for example. And that's quite, quite an efficient way. So. If you want to store a data, you see what we can use is this Val thing. Okay. We create a variable and we give it a name. So the variable short keywords that you need to use in Kotlin is val. So val one equals one. This will automatically be stored as an integer. Even though we did not specifically state that we want to store this one as an integer, it automatically does that. So if we want to store something as a byte or as a short, we need to specifically state that, because otherwise Kotlin just interprets any number that fits into the integer easily, so to speak, or into the integer will automatically be considered an integer. Now, if you want to do something like 3 billion, suddenly it jumps from integer to long because 3 billion, as you can see, does not fit into 2 billion anymore. Which makes sure that it stores the data as a long. So in other programming languages, you always have to define what data type you want to use for your variable. But in Kotlin, you don't have to do that. You just have to assign a value to that variable. And then the variable will know what data type it has by inferring the data type. So it just knows it so to speak okay. So if you put something like 3 billion in there it just knows okay. Yeah I'm going to store that as a long value. All right. And then you if you want to store the one as a long data type because later on you want to have a very big number inside of that variable, or you want to have the freedom of having very big numbers inside of that variable later on, then you can specifically state that by adding the L to that one. So suddenly that variable is going to have the flexibility of storing numbers that can go all the way to 9,000,000,000,000,009 quintillion. I think we said, right, uh, -9,000,000,000,000,000,009 quintillion. And then if you want to specifically store that one as a byte, you can say so by using the val keyword, then the name of the variable, then colon empty space and the data type. So in this case you see the data type is byte. So that's the name of the data type. And then you can store one in it. Okay. So that's just a little example. Let's go ahead and see how that works in code before we go over the other data types. And then I will also show you a little trick as to how to let ChatGPT explain stuff easier than you can see in the documentation that you might not understand.