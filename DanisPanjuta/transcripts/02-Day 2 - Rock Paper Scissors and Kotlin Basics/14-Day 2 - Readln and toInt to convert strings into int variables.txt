Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 2 - Rock Paper Scissors and Kotlin Basics
Lecture: Day 2 - Readln and toInt to convert strings into int variables
----------------------------------------

All right. Welcome back. In this video we're going to extend our if else statement with some user input. So we're going to look at how we can get user input inside of our console. So therefore we can just go ahead. Instead of using println we can use a function called read ln. So we have this age defined as 35 by default. But let's just say somebody isn't 35. Somebody is zero. But actually what they are is something we need to enter in here. So we're going to store age read ln. So what that means is we're going to read whatever the user has entered into our console. So now we run into this problem where the data types don't match. So what's on the right hand side of the equal sign doesn't match with what's on the left hand side of the equal sign. And that is a problem. So how can we fix that? We can make this an integer two int. So we can read whatever the user enters and make an integer out of it. So we use this dot and then we use this function called two int. What it does is it takes a string and parses. That's the name of it parses that string into an integer number and then returns the result. Which means whatever we enter will be converted. So we enter a string. Even though it's a number, right? A number can also be a string. So if I enter 35 like so, this is still a string, even though what it contains is a number. So what we're doing here is we're parsing that string 35 into a number integer of type integer. And then we can store it inside of age. And suddenly this problem doesn't exist anymore. Now the thing is we need to do that because otherwise we cannot make a comparison because a string comparison of a number doesn't work. We need to do a comparison of an integer number. All right. So now we can read out whatever the user has entered and then we convert it. So if we were to write it let's say a little easier. We could store it like this. So entered value. So this would be a string. So entered value would be read ln. So read line whatever the user has written. And actually this is not going to be like this var entered value will be whatever the user has entered. And then what we can do is we can say age is going to be entered value to int. So we convert that entered value into an integer. So that's basically what we have in one line here broken down into two lines here. So this can also be a vol because we're not going to overwrite this value later on. So let me just comment this out and give you a better understanding. So here read what the user has entered into the console and then convert the string variable into an int variable. Now you might see a few problems here. The first problem being what if we don't enter a number. Well let's actually try it out. So let's run our code. And what we'll see is it will ask us to enter something. It doesn't really ask us but we can enter something. Let me enter 33 in here. So it says you can go into the club now for you to understand better what you need to do. You can add something like println here. So we tell on in the console that the user, in this case us should enter something into the console. So please enter your age as a full number. Okay, so let's rerun our code and see what it's going to look like. So this will be a little more visible. So it says please enter your age as a full number or a whole number I think is like the right word right whole number. So I'm just going to enter 17 now. So once I enter 17 and press the enter key, what it will do is it will read that string of 17. It will convert that 17 into an integer and store it inside of age. And then it will compare if my age is greater 18 or not, if it is greater, it will say you can go into the club. If it isn't, it's going to say you are too young to go into the club. So let me press enter and it says you are too young to go into the club. So that's what our code here did now. Let's look at this in a little more. Structured manner. So here you see everything on one screen. So the first thing is we define the zero. Then we tell the user to enter a whole number as what the age as a whole number. Then we read that value that the user has entered. So basically our console is waiting. At this point. It just waits for us to enter something and then it converts what we have entered and does something with it with this if else statement. So now let's run this again. But this time we are going to. See that we don't enter a number. So let's enter a Asdf. Now you see we get an error. So this is how you can actually break your application. We'll get a number format exception which is basically an error saying hey the format that you have entered is not a number or not the type that I have expected. And what it did expect is actually an integer. All right. So that's how you can break your app. So be very careful with what you do. You can rerun your app and then you can enter another number let's say 25. And it will say you can go into the club. So that's what an if and else does for you. And that's what you can do with a read line. So how you can read the user's input. So read ln is how you read the user's input inside of the console with two. And we convert into an integer if it is a string. And that's pretty much what we have learned in this video. So I hope you enjoyed it in the next video, we're going to look at another little aspect of if and else. And that is the else if. So, if you want to have more than just two conditions where it's just not just the age above 60 or 18, but there is another one where, for example, somebody is over 40 and you want to make sure that only young people go into the club because it's a club for younger people, and people over 40 don't belong there or something like that. So we're going to build that in the next video. So let's do that.