Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 2 - Rock Paper Scissors and Kotlin Basics
Lecture: Day 2 - Float and Double
----------------------------------------

All right, now that you understand how integers work with the four different types that Kotlin has to offer for integers, let's look over two floating types. So just scroll down a little bit on this page if you want to read through it. And again, it just uses a bunch of numbers and stuff and explains it this way. But I'm just going to do it my way. So basically you can see there is a float which allows us to store 32 bits of data, and there is a double which allows us 64 bits of data. So depending on the amount of digits that you have inside of your floating point value, you could use either a float data type or the double data type. All right. By default, however, Kotlin just uses double. So as you see we just have too much memory available. That's why it's not a problem to just go always with the bigger type or not the biggest type, but the bigger type. In this case it is the biggest because there are just two. But basically you can see if you want to store Pi with 3.14, then this will automatically be stored as a double. So you see here Val one that we had before this number. If we want to store it as a double we get a type mismatch. So let's try these things in a bit. Before that however I want to just show you how to set up floats. You can see if you want to specifically say that a variable should be of type float without having to define it. After the variable name assignment, you can use the F at the end of your number, and that will then automatically also make this a float, even if it cuts off. So you can see here the actual value is not the same as the value here. So this, while being a double from its length, is stored as a float because we override it with this f. Okay. So let's just quickly test this inside of our little program. So we had my age in the last video. So let's get rid of that and test it with floats now. So I'm deleting all of this code. I'm going to create a new val. Let's call it py. And this will be 313.14. All right. So now let me print it out println and print out py. So let's rerun this code and see what it says. It says 3.14. Now let's also hover over py just to see what data type it is. And we can see it's of the data type double. So automatically it has been stored as a double. Now if we add the f to it suddenly it is a float data type. All right. So that's how you can change the data type by just adding an F. Or you can also obviously do it by using the old fashioned way that we saw earlier. But then you also still have to add the F because otherwise this number is automatically considered a double. Okay. As you see, this is not necessary because it infers it and it infers it because of the F as being a float. Now, if you want to override py with a very long number and this doesn't work because it's a val, so we need to make it a var if we want to mute it or change it. So let's say we started off with 3.14 and py is a constant right? It's always the same, but the amount of data that we store about py is not constant. So we could say we store 14159I believe that's like if you want to go deeper. So that's the new py. So I want to have more precision. So I'm going to use that. Now let me see what is the uh py in Stellan. Okay. Let me see that. So ten numbers. So it goes 59145926. So two six. 535 okay, so you see, I get an underlined grayish color or brownish color or something like that, and that just says the floating point literal cannot be represented with the required precision. So the precision so to speak. So the amount of digits that I assign to pi is now bigger than what a floating point or a float can store. All right. So you see it cuts it off here after the six five like it says 265 should be represented as seven. So it just takes the closest next value that would fit. So now let's print Pi again just to see what this does. And you will see that now the new pi will be this imprecise number. So it doesn't just cut it off at that point where it doesn't know what to do with it, but it's also at the same time rounds the number up, which could be a problem because if you use Pi, we want to make sure that pi is what it is, right. So that's what we can achieve by getting rid of the F in both cases and making this a double. And suddenly we have the precision again. So doubles are more precise as floats. You can see we get all of the precision that we needed. So that's just for you to know there are different types of floating point values. And that's pretty much it for this video. So see you in the next one. And obviously also don't forget to do the exercises please. All right. So see you in the next one.