Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 6 - Unit Converter Part 2 and Understanding State
Lecture: Day 6 - Converting and Finishing the DropDownMenu Input
----------------------------------------

Welcome back. In this video we are going to implement a function called Convert units, which allows us to convert from one unit to another, which is exactly what our unit converter should do. And it's basically going to maintain the logic or contain the logic of our application, which is quite simple because it really is just multiplying two values with each other. So I'm going to put this function inside of our unit converter composable. And I'm going to put it underneath where we have all of our variables here our underneath our input value because we need the input value there and the output value as well because we want to override it. So let's go ahead and create this function. And you can see you can create functions inside of Composables. Pretty interesting right. So in other programming languages you wouldn't put functions into functions too much. But yeah in this one you can. So we have convert units. And what do we want to do. Well we want to get the input value as a double. So therefore we need to take this input value. So that means that whatever we have entered in here and we need to convert that into a double. So how can we do that. There is this function called to double or null. And what that will do is it will parse the string as a double number and returns the result or null if the string is not a valid representation of a number. So that's actually pretty cool. So that means that even if we don't enter a number that is a double into our. Text fields here. Our application won't crash because it will just return null. It will not return a well. It will not crash the application. It's basically what I'm saying. So what do we want to do here? Well we're going to use something that is very powerful, very cool. I'm going to say question mark colon 0.0. Okay. So let's look at what is going on in this line. So I'm using this question mark colon. And then 0.0. So what this does is or what this is called. So question Mark colon Elvis. Operator. Okay. This is the Elvis operator, which is like a smart, very short if statement. So what we're getting back from our to double or null is going to be either the result of whatever input value we have entered as a string into a double, or it will return null. So what I'm saying with this is if you return a double value, then use that double value as the value that you store inside of input value double. However, if you don't, then use 0.0. So that's what this Elvis operator does for us and just says, if this thing goes to null, then use the default of 0.0. Okay, so if we enter something that is not a number, let's say we enter here, this one here with q w e r, then this would not be a number. And our entered value would default to 0.0 is what this is supposed to do for us. All right. Now let's keep going. What else do we need. We want to have the result. So how do we get the result? Well, we take the input value and we multiply it with our conversion factor. And then and specifically the value of the conversion factor because the conversion factor does use equal. Remember. So coming back to this example of having the gold coins inside of a chest. So this here is a chest. And if we want to get the coins we need to use dot value. Okay. So this multiplies it with that. And then we multiply it with 100 and we rounded up. Then. And then round. To int and then we divide by 100 again. So what is going on here. Well basically we're just saying take the input value. Take the conversion factor value, multiply them with each other. Then multiply the result with 100. Then round it into an integer and then divide by 100 again. So this will give us a result that we can work with instead of having these very weird, super long results. Now you see that I'm getting an error, and that is because I'm not using the input value double like so. Now it should be fine. Okay, so I accidentally used the input value, but we needed to convert it into a double first right. Which we just did. But now you need to use the double here. So now round two needs to be imported because this is a extension function that isn't automatically implemented. So now we added it to our imports up here. Okay. So now it will be available under our. Imports C. Kotlin math round two. INT has been added. All right. So now that we have the result we can use the output value and say that result should be the output value. But the result unfortunately is a number. So we need to convert that into a string by using the two string function or method. So now our output value will be whatever the result was which is a double as a string. And we're storing it inside of this. Remember variable of the state variable. And we can now use it inside of our user interface. Now we need to change every single input value. So here inside of our drop down menu we need to change every single onclick. So we have I expand. Set to false. The conversion value for our for our centimetres one should be 0.01. For the input unit, it should be centimetres. And then we want to call the convert units function. And now we need to do the same trick for every single one of our dropdown menu items. So I'm going to put that here. Now however I need to change the meters and I need to change the value. So for our meters it should be one because this is going to be our base so to speak. The meter is the base. Unit that we're using. And then let's do that again for feet. And there I have to look it up, because feet is not something that I have in mind when working with numbers. So feet should be 0.3048. So a foot is 0.30848. Centimeter meters. So 30cm and 48mm. Okay. So I'll wait 480mm. So that's what our feet does. And then finally we have millimeters. So let's add that in there as well. Now here we need to copy this in there. The millimeters. And the value that we're going to use is going to be 0.00. One. Okay. I accidentally also copied a closing bracket. So that's for our input. And I would say let's make a quick break here or take a break here, because in the next video we're going to then take care of the output side of things and finally execute our application. So see you in the next video.