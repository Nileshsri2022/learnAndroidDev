Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 11 - Location App
Lecture: Day 11 - Reading the Latitude and Longitude and displaying it
----------------------------------------

Welcome back. In this video, we're going to finally set everything up so that we can get the actual location and display the location. So we can already get the location, but we cannot display it yet. And that's what we want to change. So I'm inside of this location utils class that we created a little earlier. And in here I will load a private val fused location client which will be of type fused location provider client. So you see I automatically get this import and it's from Android GM's location. Okay. So if you recall that was the Gradle implementation that we added. So GM's play service location. So now that we have that, let me actually go back to my location utils. Now I can use this fuse location provider client, which then will allow me to get the longitude and latitude of the user. So we need to say what should it what it should be. Okay. Let me actually say equal. So we need to initialize it. Location services dot get fused location provider with the context. So get fused location provider client where we need to pass the context in. So let me put that into a separate line like so. Now you see why we needed the context. We didn't only need it here, but we will also need it for our fused location client. So this is the main entry point for interacting with the fused location provider. In order to use most location APIs, clients are required to hold either this or define location. We saw that then. You can also use background location if we need that. Yeah. So basically this fuse location provider client will fuse the location together so we can work with it. All right. So now that we have that let's create a function that will give us the actual location. So I'm going to use fun request location updates. And now. The location updates function will require the ViewModel, our location ViewModel. And now in here I'm going to call the location or use something called a location callback, which is basically a callback is you use something where you request something, and then you wait for it to finalize that thing, and then it returns something to you. And in this case, we want to get the location, okay. And this can take a little while, which is why we're using a callback. So I will create a object here directly of the location callback that we also get from GM's. So here location callback. This import. And now, what should this object do for us? Well, the thing is, we're creating this object of location callback. And in here we can override things. So that's really crazy. We can basically just say, okay, instead of inheriting your from your class and do our own thing with it and overriding, we can directly create an object and override instantly inside of that object. So we're overriding a function. And the function that we want to override is the on location result. So this on location result will give us a zero which I don't like as a name. So I'm going to replace that with location result. And here I'm going to pass the location result. So there's the super function as we've seen whenever we do an override. And then I want to use that location result and get the last location. So this will give us the last location now. I want to basically create a location object with our location data, so where I get the latitude and longitude in order to do so, what I want to. Well, I will basically unpack this last location with our let method. We've seen that before where I will get the location. So I'm going to say val location will be location data and the latitude of our location data, which is our own data class that we created will be eight dot latitude. So it is this location right here. It is it thing here. That's what we get from this location. Result last location. So the latitude and the longitude will be it dot longitude. So this thing inherits or holds those two pieces of information. And now at this point that I have my latitude and longitude I can use my view model to update. The location with my location. So what is going on? Well, inside of our location view model, we had this location mutable state that we created, and we created a function called Update Location, which now allows us to update the location with a new location data object. New location data object, which just holds the latitude and longitude. And we're basically overriding this or overwriting this location with whatever the location is. Once we get the. Location results. So basically once we call this function call request location updates. Now this is just a callback that is never used. So it's never actually going to do anything unless we tell it to do something. So telling how to do that is in our next line. So location request is going to be location request. And I need to manually write it. Okay. Location request dot. And here we need to use its builder. Okay. So I'm going to use the builder. And I need to pass in the priority. Priority dot. And let me just see that. Let's import the location request here. And I'm going to use the version. Let me just check. Location request. Yep, that's the right one. So here I can now go for the priority which I also need to import. Import class priority. And I'm going to use the version. Now this class has a few priorities that we can use. So I'm going to use priority high accuracy. So you can see there are different ways of how you can actually request the priority. The location of the user and high accuracy will use a lot of battery. Low power will use low battery balance power accuracy will try to balance it. And then there's this priority passive which will, from what I understand, do require a little only a little battery so to speak. So the builder needs additional information. So it needs to get the priority as well as the interval in which I want to. Ask for location or get the location, then I need to build it. So let me put that into a separate line. So this will be my location request. However, this location request is not executed yet. You see it's grayed out. So we're just building it basically. And now the next step I can use my fused location client, which is this one here, which is now going to put everything together because it has a function called request location updates. Now here I need to pass in the location request, which is this one here that we just set up. And then I need to pass the location callback which we had set up earlier. And on top of that. There is another parameter that is of type type looper. And I'm just going to use the get main looper here. So whenever you want to use that feature you need to ask for permissions. So that's what this arrow is that we get here. We're basically getting the error saying hey you need to have permission to use this thing course location as well as access file location. So we now need to make sure that we only call this function if we actually have the permission or not. Like if we actually have the permission so we can get rid of this problem or this error by using a suppress lint. Okay. Let me show you what that is. We use this suppress lint, which is an Android annotation. And here we're going to suppress the missing permission. So now this ignores the error that we have here at the bottom. Okay. So what is this lint thing. Let me quickly, quickly show you because I really like the explanation that I got here. The lint tool checks your Android project source file for potential bugs and optimization improvements for correctness, security, performance, usability, accessibility, and internationalization when using Android Studio, configured lint and inspections run when you build your app. So basically this thing is just taking care of us, not screwing up our application and making sure the code is good and secure and safe and all of that. That's why we got this error. And we're just saying, hey, just ignore this error. We are taking care of it ourselves. So don't worry about the missing permission. We are going to take care of this ourselves. So now this looper. I just rushed over it real quick. Basically, the looper is used to handle the threading and message processing for location updates. It allows the location provider to communicate with your app's main thread or another specified thread by providing a specific looper, you can control on which thread location updates will be delivered. So this will go a little too far for for discourse. This is quite advanced stuff, but if you really want to make sure, hey, it runs on a specific thread, then you can modify that piece of code. Okay, I know this video will be long, but I really want to finish it in this video. So let's keep going. And therefore we're going to go over to our location display. Let me see. That's inside of our main activity. So location display needs a view model, right. And here when we do have the permission, this is where we actually have access to the location. That's where we're going to use that location. Utils dot request location updates. And we're going to pass in the view model. So here our view model this one up here. So we're passing our location view model to request location updates. Now this request location updates will give us the. Now this request location updates will update our view model which contains, if we look at it, the location. So if we want to access that location now, we can go ahead and create a variable in here inside of our composable because we do have access to the view model itself. So we're going to say val location will be ViewModel dot location dot value, which now is this location here, which is the state of location data which basically contains our latitude and longitude. Okay. So now we have this location variable and we can go ahead and use it inside of our composable text. So location not available is what it said by default. But now we're going to check if the location is not null. So if location. Is not equal null. So we know we have a location, then on let's print text that says address. And currently it's just going to display the location dot latitude as well as the location dot. Longitude okay. And now finally if we don't have the location so it is null then we're saying location not available. All right so that's it. Let's quickly run it because basically we prepared everything. And now we can finally execute it and see the latitude and longitude of our phone. And. Let me get the location. And actually let me make sure that I do have access. So allow when using the app. And yeah, I missed one thing. And that's when we actually has location location permission. So when we do have access to the location permission that's when we actually want to get the location. So here I'm going to request location updates with our view model. Okay. So once we click on the button that's when we want to get access to the location. And as you see we're just passing the view model. And the view model always takes care of basically the data. All right. So we don't need to manually update the data all the time. So let's get the location. And there we are. So we have our latitude and longitude. And by the way if you don't have access to the location make sure that you have your permission activated. So we see the latitude and longitude. And in the next video we're going to look a little more into what latitude and longitude means in this context. So see you in the next video.