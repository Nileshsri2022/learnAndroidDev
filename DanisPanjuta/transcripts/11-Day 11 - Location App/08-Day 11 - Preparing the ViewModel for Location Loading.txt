Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: Day 11 - Location App
Lecture: Day 11 - Preparing the ViewModel for Location Loading
----------------------------------------

A welcome back. In this video we're going to set up the view model that allows us to pass the data over, basically displaying the location of the user and the sense of latitude and longitude. So therefore let's go ahead and create a new data class here, which I'm going to call location Data. Now this data class will be very simple. It's just going to have two valves. So one will be the latitude which will be a double and the other one will be the longitude. Longitude. Wait a second. That's not right. Like so which will also be a double okay. So that's our data class. It will contain location data. Very simple. Now we can create objects of that thing. Now let's also set up a view model which basically takes care of the communication between the view and the data. Right. So the view and the model are model is the location data which is the latitude and longitude, which then allows us to basically get the data like the address from the latitude and longitude later on. So here I'm going to call this one location view model. All right. And this thing will be a view model. All right. Like so. So we need to import the lifecycle view model import here. Now what do I want to do in here. Well basically I want to have the val location. So a private variable that will be inside here which will be a mutable state of where is that. Now I need to write it mutable state of. And here I'm going to say it's going to be null at the beginning, but I need to define of what type it is if I want to do that. So I'm going to say location data nullable. So mutable state. Often we need to import mutable state of. So I want to get location data but it can be null okay. That's why I have this question mark. And as a default value I'm going to set that to null as well. So if we don't have the location we don't have the location. So we're going to accept that as a null basically as no data okay. And now let's go ahead and create a location variable that we actually can access. And that will be of state location data nullable. That will be that private location. Now we need to import state here. And I'm going to use the state lifecycle one. Let me check if that's actually the right one. Life cycle. ViewModel. Actually, it's not the right one. Let me make sure that we actually get the right state. It's the state from runtime import class state the one from Android compose runtime. So this is a tricky one. I really needed to check because sometimes there are different imports and they do different things, and suddenly the code wouldn't work and you wouldn't understand why. And that's because we just imported the different class. So here let's have a function that will allow us to update the location. Okay. So this will get us a new location based on well the type will be location data which is this data class. Right. The type will be location data. And what we're going to do here is we're going to set the underscore location dot value to be the new location. So basically we're now shielding our private location mutable state of from the outside. But at the same time we are allowing to override it using functions. Okay. So that's just a way of using setters and getters in Kotlin. And if you don't know what setters and getters are, don't worry about it. So now we have the ViewModel set up. So how does that ViewModel help us. Well the thing is I want to get the location of the user and I'm going to need that in the location display. Composable, which if you recall, was this, well, this UI here basically just these two elements. So the text as well as the button. Now if I want to. Overwrite the data inside of this text. I need to take care of the ViewModel. So I'm going to put that ViewModel in here. ViewModel location ViewModel. Like so. So whenever we want to use the location display, we need to pass the ViewModel to our. Location display here. Okay, so where do I get that from? Well, in my app I can set up. The ViewModel can pass the ViewModel directly to my app. We've seen that before where we basically said, okay, I want you to pass on the responsibility further up, and the ViewModel creation itself will happen in the Oncreate function. So here I'm going to say ViewModel. It's what I'm passing here as well as the next property. Let me see if I still get an error. No error anymore. But here, obviously I do get an error because that's where I call the my app. And I need to pass a ViewModel. Now where do I get the ViewModel from? Well, that's what we can create inside of this Oncreate. And we're going to do that inside of this set content. So Val ViewModel will be the location ViewModel, where I'm just going to say it's going to be a ViewModel, like so. And let me import ViewModel. Or actually we need to add this dependency. So. The thing is, sometimes you need to add a dependency to be able to add an import, and sometimes you just can use the import because it's already inside of the dependencies that you had. So if I do that, you see, I do two things at once. I add a dependency and I add an import. So let's click on that. And you see the Gradle now will be synced. So we have seen that before when we were using Viewmodels. Let me just quickly pass the ViewModel in here so that the error disappears. And now let's look at our Gradle to see what we have actually done. What have we done. Well we added. This. View model. Let me see where it put it here. We added this implementation. It's the last implementation in here right. So that's the one that we just added automatically. Right. But that's just one of the things that we need to add. Let me check. The other one is going to be this one here okay. So come Google Android Play Services location 21 .0.1. So this will allow us to access the location API which then let's sync it, which then basically enables us to use Google Maps so to speak, or like Google Maps location services or Google Play location services. So really powerful thing. And that then gives us the feature of, for example, knowing what latitude and longitude actually means in terms of address because we want to get the address later on. So let's go back to our main activity. We see we can now use our view model. Now. And now let's go over to our location utils. So inside of this location utils that's where I want to create another function, which then allows me to get the latitude and longitude of the user's location, so to speak. And that's what we're going to set up in the next video. So in this video we just prepared everything so to speak. In the next one we're going to finalize at least that feature so that we can see the latitude and longitude. And then after that we're going to add the actual address. So reading the address from latitude and longitude. So see you in the next one.