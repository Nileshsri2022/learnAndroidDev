Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: More Fundamentals of Kotlin
Lecture: Safe Cast and Unsafe Cast Operator
----------------------------------------

Well, come back now. We're going to look at unsafe and safe cast operators, and they are useful because sometimes you just need to cast something, right? So you have something in a specific type and you need to cast it in another type. And that's where the key word comes in. And this is an unsafe cast operator. Sometimes it is not possible to cast a variable and it throws an exception. And this is called an unsafe cast. The unsafe cast is performed by the effects operator. SE And let's have a look at an example here. A knowledgeable string with a question mark here cannot be cast to a non notable string. So a string without the question mark because this then throws an exception. So let's look at this function main here we have this value object of a type, any notable which is null and then we have this value string or str, which is of type string. And what we try to do is we try to cast object, so object as string and then print that string that we created. So the problem is that we get an error here, an exception to be precise. It says exception in thread main Kotlin type cast exception null cannot be cast to non null type in Kotlin string. So here of course this doesn't work because the object of type any none of all is actually null. So it's empty but this frozen error. So this really crashes our software, our program, and we want to avoid that at all cost. Right. So what can we do? Well, let's have a look in a second. But first, it generates a class cast exception. Trying to cast an integer value of the any type into a string type leads to a class cast exception. So here we have object of type any 1 to 3 and we try to cast that to a string. So we say string or string of type string should be object as string. This throws a class cast exception because any could be any type, right? It's just interprets whatever data it gets and either interprets in this case that 123 is an integer. So it's going to say that integer cannot be cast to a string. We need to use another approach here. Right? We cannot cast we would need to use type, conversion or parsing here. All right. So now let's look at an example where this actually works. So the source and the target variable needs to be numbers for casting to work. So here we have the object any which is of type knowable. So we have this question mark and has a string in there. So a string is hidden, so to speak, in the any object. And then we have a string, which is also another ball. So what we can do is we can say object should be. Cast into a string so it should be elf type string Nullarbor. So this will output the string on save cast. There is an operator however, which makes our life even easier. So there is something called safe cast operator and there is a question mark. So the Safe Cast operator provides a safe cast operation to safely cast to a type. It returns a null if casting is not possible rather than throwing an class cast. Exception. So this is very good because then our program will not crash. If it doesn't work, it will just return null. All right. So here, example of safe cast operation. So we have a location which is of type any and it actually is a string, right? So in the background there is a string and as you can see, it's not notable any. Then we have a safe string, which is a string that is infallible. And what it tries to do, it tries to get the location as a string, so it uses the safe cast operation here. Then we have a safe int, which is an integer ineligible and it tries the same thing. So it tries to get the location as an integer. But of course a text cannot be an integer, right? Or at least a word as we see here. Kotlin cannot be of type integer, not even integer. So it will just be null. Right. So the output here is Kotlin and null. All right. Kotlin for the save string and null for the safe int because it wasn't able to convert it because Kotlin is not convertible or cast, so to speak. It's not cast into an integer. All right. So that's it for unsafe and safe casting operators for now. And yeah, furthering the course we will use it and then it will make a lot more sense because here the examples of course are very abstract and they are very purpose driven. But later on we will see how it actually is going to work. All right. So see you in the next video.