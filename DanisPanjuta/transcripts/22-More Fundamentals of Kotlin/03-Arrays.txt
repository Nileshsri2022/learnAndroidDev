Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: More Fundamentals of Kotlin
Lecture: Arrays
----------------------------------------

Welcome back. In this video, we're going to look at the rays and rays as we saw earlier, allow us to store multiple pieces of data in one variable, so to speak. So let's create our first rate for the integer type and also see the different ways we can create an array. So first of all, I'm going to use the keyword vol and I'm going to call this variable numbers and it will be of type int array. So using int array. And then I need to use this method called int array off where I can then assign values. So int values and multiple ones so far are elements of type int. Which means I can just go ahead and just add a couple of numbers, separate it with a comma. So I have these values here, 1 to 6. So that's one way of creating it. But then because of type interference, we can just go ahead and use a simpler approach where we can get rid of this int array here entirely. So let me copy that. And here we can get rid of this part. Therefore I need to commit this out. So because we are using int array of numbers knows that it should be an array of integers. So let's type inference here. We don't need to specifically state that it's going to be of type int array. So by the way, there is an even simpler way of doing it. So another way of using type inference and I'm going to again copy the line of code comment the other way out. And by the way, all of them are valid, right? These are just more descriptive. So they are really showing to the point what's going on here. And then we have this one where we don't even need to say that it's an entire array. We just say that it's an array of so now let's go ahead and print an array. We can just go ahead and say print numbers and let's run this real quick. Then we will see that it just prints this information for us. So we would have expected to get the numbers here. Right. But what we're getting is actually this year, so we are getting the address of the array, which is where it is stored in memory. This is because we cannot just print a race with the print method. We will need to use a utility method provided in Scotland for that purpose. So what we can do is we can use numbers and that's a cool thing of the array off here. So this is an array now and we can just go ahead and use a method called content. To string. So what that will do is it will transfer the content into a string converted for us and then we can now display the numbers and you can see now it says, okay, this is an array and that's why we have those squared brackets here. One, two, three, four, five, six. Okay, so now we can see the values in the right format. So now let's traverse through in a way, meaning go through the items of the array and print them individually. So that would be a different approach. Let's look at it. And I'm going to simplify the code here. And the easiest way to do it is to use a for loop, where I'm just going to say an element inside of numbers. And this element is just a variable name. You could have called it I or whatever you want it. And I'm just going to say the element in numbers is what I want to print. So let's print element here. So what's happening now is that it goes through this numbers array and for every single item, starting from the first, it will store that in element in the first iteration and the second iteration it will store the two in element. In the third iteration it will store the three in element and so forth. So it's going to go through all the numbers inside of this numbers array and we'll store them in element and then print them out and then go to the next number, print it out, go to the next number and so forth. So here we're just printing, but you could do whatever you wanted to with that particular element. You could modify it and store it somewhere else, you could display it and so forth. So we're just going to display it on the console. Using a print statement, you can see one, two, three, four, five and six. So all the numbers that are inside of numbers are now being displayed. So now let's actually do a modification here. So I'm just going to go ahead and add an empty space here and add two to it. So here plus two, for example. Now if I run that, I will see that it will just display one plus two, two plus two and so forth. It's not doing the actual calculation. Do you recall how to actually do the calculation here when we were using string manipulation here? Well, we need to add the curly brackets around it. So let's run it again. And now it will add the number to the element. But that by itself will not change the numbers itself. So if we were to now do the same thing once again after that and we print out numbers without adding the two to it, therefore we don't need to have those brackets. Let's run this code and you will see that our numbers are not actually impacted. So three, four, five, six, seven, eight. But the actual numbers are still one, two, three, four, five. So we didn't change the value itself. We just displayed it. So we didn't override it. So now let's modify a value in the array. And since arrays are indexed based on starts counting from zero, it means the position of one in our array is going to be zero. The index of two or the position of two will be one and so forth. It's a little confusing, but let's look at it in order to understand it a little better. So I'm going to get rid of this for loop here. I'm going to print numbers. And now if I want to access a particular item inside of the numbers array, I just need to use the squared brackets here. So if I, for example, want to access index zero, this will give me one. Let's test this. And you will see that it displays one in effect because it starts at index zero. But the first entry that we have is one at this position. So now let's use index three for example, and that will give us the four. Let's run it again. And here you see four. So this has nothing to do with the actual numbers that are in here. It just has to do with the position that we're looking at. So the index of that array. So here, let's say I'm entering five and I get the number at position zero. Now it will give me the five instead of the one because that's what is stored at position zero. Okay. Let me go back to one here. So now let's modify our arrays a little bit. So now let's modify the array a little bit. I want to store a different value at position zero. For example. In order to achieve that, I just use the name of the variable, which is numbers. Then I use the square brackets with the index which I want to change, and then I assign the value that I want to assign. So in this case, let's say at position zero, I want to use the number six. And at position one, I want to use the number five. And then for index four, I'm going to use the number two. And finally for index five. I'm going to use the one. Okay. So let's now look at what happened. So first of all, I'm going to print the initial value of my numbers and I'm going to call to call this one initial. Values. And then use the square or the dollar sign with. The round the brackets surrounding it like so. And then I'm going to print it with the. Change to values. So. With the new line, by the way. Therefore I'm using the backslash n and then gonna call it final values. Okay, so let's run this and see how this has impacted our array here. We could see initial values will one, two, three, four, five, six. And the final values were six, five, three, four, two, one. So now since a have a fixed size, we cannot increase the size or try to add a new index since we have six values and the index starts from 0 to 5. If we try to add something to numbers at the index seven, for example, this would not work. So we can get into a bounce out of bounds exception doing that. So let's say I want to assign two to position seven. Let's run this. And you see we get an error here. It says exception and thread. Main array index out of bounds exception. So index seven out of bounds for length six. So we try to access an index that doesn't exist. So the same would even go for index six because we only have six entries and it starts at zero cost all the way to five. So here exception thread main index sticks out of bounds for length six because it starts with zero. That's why six is already out of bounds. Now we can use the same with the double array. So numbers D for example, this would be our double array just using the double array keyword, double array off. And of course you could have simplified this the same way. And here I'm going to override the double array like so. But then you have to specifically use double numbers, which means you have to add a floating point value to them, otherwise it wouldn't work. So you cannot assign an integer to a double array. You couldn't see that directly here because we got an error here otherwise. Or you can go ahead and use an array of four days. So let's go ahead and use that here. So vault days will be an array of and you just use Sunday, for example, then Monday and you see you separate the entries even if you're using strings each time with a comma. So each entry is added like this inside. Of our brackets. So then if you want to display it, you would do the same thing as we saw earlier with content to string. So here I have all of the days of the week and now I'm printing them and. This will have not any impact here. Let's also get rid of this statement here and let's just display the days of the week and you see Sunday, Monday, Tuesday, Wednesday and so forth. We can even go as far as to store entire objects inside of an array. So therefore I'm going to create a small data class outside of this function, although outside of our main function here at the bottom. And I'm going to call this one fruit. And this fruit will need a name so well, name of type string. And it will need a price, for example, of type double. Okay. So that's how the structure of our data class will be. And now we can just go ahead and create a fruits list, which will be an array of. The different fruits. So here we can now create fruit objects. So fruit object one will be an apple and it has a price of, let's say a kilo price of $2.5 or euros. And then we create another fruit. Which will be, for example, grape, and that will then cost €3.5 per kilo. Just as an example, and we can print the fruits in the same way as we would with the days. So here it's just print fruits with content string and let's run it real quick. And we can see here we have the fruit name, apple price 2.5 and the other fruit grape and so forth. This will be super useful and we're going to use that quite a bit in the future. Now we can also access individual items. Of the fruit and printed. Instead of printing all the fruits content, you can just go ahead and just display the fruit's name. So I can just go ahead and say where the fruit is at which index. So here, let me use the quotation marks rounding. Basically, I'm saying fruits, give me the index as well. And in order to get the index, we, of course, need to. Lose a for loop. So for index in routes and then indices. This gives me the indexes like so. And then inside I would just do print index. So this doesn't give me the fruit itself, but it gives me the index of the position of the fruit. So here I want to get the name of the fruit at a particular index. That would be a weight and. Let me add the quotation here as well. This is one way to get the indexes, but if you just want to get the fruits, there is an easier way. You can just use a for loop where you go through each individual fruit inside of fruits and you just print the fruit name. So here you can just go ahead and say fruit dot name. This will give you the fruit name. So let's run it. And you will see that we now get the fruit names. Let me actually get rid of the days here so we can see it a little better and we can see apple and grape. And then Apple is at index zero. Grape is in index one. So this was apple and grape and this was the indices. By the way, you can also have an array which uses multiple different types of data. So here let's use this days, for example, and instead of days now we have this little mix up where we use Tuesday and then we just assign a numbers like so it's just a new number. And then here we also have a fruit object. So we can just go ahead and say fruit apple with a 2.5 price. So this would be not taste but mix and then let's print those as well. So here, mix dot content to string. And therefore let me comment those other parts out and let's run our code. So you see, we have Sunday, Monday, Tuesday, 1 to 3 fruit with the name Apple and price of 2.5. So the beauty about write off is really that it can hold different types of data. And we saw earlier that there are these double arrays, for example, they can only hold doubles and interest, they can only hold ends. There will be more efficient as well as safer. So why would you use an inter ray if you can just go ahead and use an array of. Well, the idea is that inter rays are super on point and they can only hold numbers. So imagine you get the data from somewhere else and for some reason the database gave you an integer at the position where you would have expected an int and then your code would directly figure it out and you could get a way around it, except you could add an exception that will then manage that problem and go about it and that wouldn't be recognised if you have such a mixed array, that doesn't care what kind of data it has. Okay. So that's pretty much it for this video. See you in the next one.