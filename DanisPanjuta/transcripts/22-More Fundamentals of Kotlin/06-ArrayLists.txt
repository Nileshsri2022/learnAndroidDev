Course: The Complete Android 14 & Kotlin Development Masterclass
Chapter: More Fundamentals of Kotlin
Lecture: ArrayLists
----------------------------------------

Welcome back. In this video, we're going to look at ArrayList and we're going to use the rails quite a bit in our future project. So it's important to see how they are used and understand them. So they're used to create dynamic arrays. And arrays are types of collections. So we're going to use collections here. So why should you use an array lists? Well, ArrayList are used to create a dynamic array, which means the size of an array list can be increased or decreased according to your requirements. So it's not limited to a specific size, which is pretty cool. The array list class provides both read and write functionality. Then the array list follows the sequence of insertion order. So it matters when you put something in there and an array list is not synchronized and it may contain duplicate elements. So it's not going to be limited to one entry of the same thing, so to speak. The constructor of an array list looks like this, so either it is used to create an empty array list. So that's the basic constructor. Then you have another constructor where you can actually pass in the capacity. So you can say what the maximum amount of elements that can fit into this array list should be. And then you can create an array list based on a collection so you can fill it with the elements of a collection and use it then as you need it. Examples of functions of ArrayList are, for example, the ADD function used to add the specific element into the collection. Then the clear function is used to remove all elements from a collection. The get function allows you to get an element as the specified index. So for example, if you want to have the fifth entry, you could enter index four in there and you would get that. And then the remove function is used to remove a single instance of the specific element from the current collection if it's available and hereby I say a single instance. So let's say you have the value five multiple times in there. Only the first five would be removed if you call this remove function with a value of five. Then there are of course, many more functions in the ArrayList class and we're going to use multiple of them. But these are the most important ones. All right. Let's have a look at an actual ArrayList so you can see we create an array list and we create it via the valid keyword. So we're not using VAR here because even though the ArrayList might be a vol, so an immutable array, it's still adjustable. So we cannot replace the array list object, but we can replace the content of this ArrayList object. In this case, it's an array list object of type string, so we only can add strings in there. For example, the array list, add one and then array list add two. So I'm adding one and two to this ArrayList, meaning the written words, and then we can print this array list by using the four in loop here. So for I in ArrayList we print I and that will output one and two. The red is using collections. Let's have a look at that. We can see we have again the function main and then we create an ArrayList. This time we set this ArrayList as being limited to five entries. So this is the one example where you can limit and read is to add a certain amount of values. So here this one, and then we have a list which is a mutable list of strings, and we put values in there and then we add all of those values to our array list. Then we use an ArrayList iterator, called it in our case, and we say as long as this iterator has something, so as long as our array list has another entry in there, we run to run this wild loop. So we're just printing it. R Next, so the next entry in our ITER, which in this case means in the ArrayList iterator, which pretty much means in the ArrayList itself. And then we can also see that we can get the size of an array list by using its size property. So that will give us the following output one, two. And then of course the size of ArrayList is two because there are only two entries in there. Now let's look at the get function so you can see we have this main function here again with. So then let's look at the get function. You can see again we have a main function here using an array list of strings and then we add one and two and then we'll loop through it with this for loop. But we also used to get function here in order to see what we will get. So if I use get one, I will get two. So this is a little confusing, but that has to do with the counting starting at zero. So the first entry here is going to be at index zero. The second entry will be at index one. And what we're doing here is we are accessing index one, which means we get the two. And that's why the output is to in this case. All right. This was just a little introduction to ArrayList. We're going to use them quite a bit, and then they will become natural as usual. So no worries there. It's like learning a new word in a foreign language. The more contexts you see this word end, the more it makes sense and the better of an understanding you get for this world. So see you in the next video.