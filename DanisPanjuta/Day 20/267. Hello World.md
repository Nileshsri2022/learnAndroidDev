# Hello World

---

## Overview

Run the **Empty Views Activity** project — verify `Hello World` appears in the emulator. Introduces **Project view**, `MainActivity.kt` (`setContentView(R.layout.activity_main)`), and `AndroidManifest.xml`.

---

## Recap / cheat sheet

- **Views entry:** `setContentView(R.layout.activity_main)`.

