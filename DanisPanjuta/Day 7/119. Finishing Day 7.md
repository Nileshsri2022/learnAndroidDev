# Finishing Day 7 — Week 1 Done

---

## Overview

**Lecture:** Finishing Day 7

Two apps this week (Unit Converter + Shopping List). The shopping list is already a real CRUD-on-screen toy: add, edit, delete, unbounded rows.

If the last `map` / `copy` / `find` / `let` block was fuzzy — expected. The course repeats the pattern until it feels normal.

---

## What you covered today

| Topic | Where it showed up |
| :--- | :--- |
| `LazyColumn` / `items` | Only visible rows are composed |
| `data class ShoppingItem` | `id`, `name`, `quantity`, `isEditing` |
| State as a **list** | `var sItems by remember { mutableStateOf(listOf()) }` |
| File split | `ShoppingList.kt` vs `MainActivity` |
| `AlertDialog` as a form | Title, two fields, Add / Cancel |
| Custom composables | `ShoppingListApp`, `ShoppingListItem`, `ShoppingItemEditor` |
| Lambdas as parameters | `() -> Unit`, `(String, Int) -> Unit` |
| `IconButton` + `Icons.Default` | Edit / Delete |
| `map` / `copy` / `let` | Update one row in a list |
| Conditional UI | `if (item.isEditing)` editor else row |

---

## Still missing

**Disk storage.** Kill the process, list is gone. Room comes later so the list survives.

---

## Tomorrow — start of week 2

**ViewModel** and **MVVM** in Jetpack Compose. Same kinds of apps, but state moves out of the composable so the code is maintainable — the architecture you need for junior jobs.

---

## Instructor outro

Tea: Milford “Great Vibes” sweet berry. Roughly a third of the course by video time. Take a break if you want; otherwise see you for ViewModels.
