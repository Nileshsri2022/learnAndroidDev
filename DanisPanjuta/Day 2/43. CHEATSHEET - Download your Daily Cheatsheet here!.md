This item is a **downloadable resource, not a video lecture** — it has no spoken transcript.
It provides a one-page cheatsheet recapping the day.

---

### **What it is**

A PDF/reference sheet you can download from the lecture's attachment, summarising Day 2's
syntax so you don't have to scrub back through two hours of video to check how something is
written.

---

### **What it covers**

The same ground as the [Day 2 summary](42-summary-of-day-2.md):

* **Variables** — `val` vs `var`
* **Data types** — `Int`, `Long`, `Short`, `Byte`, `Float`, `Double`, unsigned, `Boolean`,
  `Char`, `String`
* **Comparison operators** — `>`, `<`, `>=`, `<=`, `==`
* **Logical operators** — `&&`, `||`, `!`
* **Control flow** — `if` / `else` / `else if`, `when`
* **Loops** — `while`
* **Console I/O** — `readln()`, `toInt()`, `println()`

---

### **How the course uses these**

There is one cheatsheet lecture at the **end of every day**, alongside the day's summary. The
intended workflow:

1. Watch the day's lectures and do the exercises
2. Read the **summary** for the narrative recap
3. Keep the **cheatsheet** beside you as a syntax reference while you code

---

### **Key Takeaways**

1. This is an **attachment**, not a video — nothing to watch.
2. It is a **quick-reference sheet**, not a teaching resource; the explanations live in the
   day's lectures and summary.
3. Use it while **writing your own code** — that's what it's designed for.
