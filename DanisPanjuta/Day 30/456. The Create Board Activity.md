# The Create Board Activity

---

## Overview

The FAB on the main screen opens **CreateBoardActivity** — a board image (circular), a board name field, and a CREATE button. The user's name is passed along as the board creator.

---

## MainActivity

```kotlin
private lateinit var mUserName: String

fun updateNavigationUserDetails(user: User) {
    mUserName = user.name
    …
}

fab_create_board.setOnClickListener {
    val intent = Intent(this@MainActivity, CreateBoardActivity::class.java)
    intent.putExtra(Constants.NAME, mUserName)
    startActivityForResult(intent, CREATE_BOARD_REQUEST_CODE)
}
```

---

## Layout

```xml
<de.hdodenhof.circleimageview.CircleImageView
    android:id="@+id/iv_board_image"
    android:layout_width="@dimen/board_image_size"
    android:layout_height="@dimen/board_image_size"
    android:src="@drawable/ic_board_place_holder" … />

<AppCompatEditText android:id="@+id/et_board_name" android:hint="@string/board_name" … />

<Button android:id="@+id/btn_create" android:text="@string/create"
    android:background="@drawable/shape_button_rounded" … />
```

Toolbar title: "Create Board".

---

## Recap / cheat sheet

- FAB → `CreateBoardActivity` with `Constants.NAME` extra (creator).
