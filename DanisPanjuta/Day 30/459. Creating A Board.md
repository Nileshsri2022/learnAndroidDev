# Creating A Board

---

## Overview

Defines the **`Board`** model (Parcelable) and builds a board object with the creator in its `assignedTo` list.

---

## Board model

```kotlin
data class Board(
    val name: String = "",
    val image: String = "",
    val createdBy: String = "",
    val assignedTo: ArrayList<String> = ArrayList(),
    var documentId: String = "",
    var taskList: ArrayList<Task> = ArrayList()
) : Parcelable {
    constructor(parcel: Parcel) : this(
        parcel.readString()!!, parcel.readString()!!, parcel.readString()!!,
        parcel.createStringArrayList()!!, parcel.readString()!!,
        parcel.createTypedArrayList(Task.CREATOR)!!
    )
    override fun writeToParcel(parcel: Parcel, flags: Int) = with(parcel) {
        writeString(name); writeString(image); writeString(createdBy)
        writeStringList(assignedTo); writeString(documentId); writeTypedList(taskList)
    }
    override fun describeContents() = 0
    companion object CREATOR : Parcelable.Creator<Board> {
        override fun createFromParcel(parcel: Parcel) = Board(parcel)
        override fun newArray(size: Int): Array<Board?> = arrayOfNulls(size)
    }
}
```

| Field | Meaning |
| :--- | :--- |
| `createdBy` | Creator's name |
| `assignedTo` | UIDs of users who can see the board |
| `documentId` | Firestore document ID (set after loading) |
| `taskList` | Lists → cards (added later) |

---

## createBoard()

```kotlin
private fun createBoard() {
    val assignedUsersArrayList: ArrayList<String> = ArrayList()
    assignedUsersArrayList.add(getCurrentUserID())

    val board = Board(
        et_board_name.text.toString(),
        mBoardImageURL,
        mUserName,
        assignedUsersArrayList
    )
    FirestoreClass().createBoard(this, board)
}

fun boardCreatedSuccessfully() {
    hideProgressDialog()
    setResult(Activity.RESULT_OK)
    finish()
}
```

---

## Recap / cheat sheet

- `Board(name, image, createdBy, assignedTo = [currentUid])`.
