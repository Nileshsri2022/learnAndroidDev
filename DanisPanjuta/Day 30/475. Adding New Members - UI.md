# Adding New Members - UI

---

## Overview

A **+** icon in the Members toolbar opens a **custom dialog** where the user enters the **email** of the person to add.

---

## Menu & dialog layout

`menu/menu_add_member.xml` → item `action_add_member` with `ic_action_add_white_24dp`, `showAsAction="always"`.

`dialog_search_member.xml`:

```xml
<androidx.cardview.widget.CardView …>
    <LinearLayout android:orientation="vertical" android:padding="…">
        <TextView android:text="@string/add_member" android:textStyle="bold" … />
        <View android:background="@color/divider_color" android:layout_height="1dp" … />
        <TextInputLayout …>
            <AppCompatEditText android:id="@+id/et_email_search_member"
                android:hint="@string/email" android:inputType="textEmailAddress" … />
        </TextInputLayout>
        <LinearLayout android:orientation="horizontal" android:gravity="end">
            <TextView android:id="@+id/tv_add" android:text="@string/add" … />
            <TextView android:id="@+id/tv_cancel" android:text="@string/cancel" … />
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>
```

---

## Code

```kotlin
override fun onCreateOptionsMenu(menu: Menu): Boolean {
    menuInflater.inflate(R.menu.menu_add_member, menu)
    return super.onCreateOptionsMenu(menu)
}

override fun onOptionsItemSelected(item: MenuItem): Boolean {
    when (item.itemId) {
        R.id.action_add_member -> { dialogSearchMember(); return true }
    }
    return super.onOptionsItemSelected(item)
}

private fun dialogSearchMember() {
    val dialog = Dialog(this)
    dialog.setContentView(R.layout.dialog_search_member)
    dialog.tv_add.setOnClickListener {
        val email = dialog.et_email_search_member.text.toString()
        if (email.isNotEmpty()) {
            dialog.dismiss()
            showProgressDialog(resources.getString(R.string.please_wait))
            FirestoreClass().getMemberDetails(this, email)
        } else {
            Toast.makeText(this, "Please enter members email address.", Toast.LENGTH_SHORT).show()
        }
    }
    dialog.tv_cancel.setOnClickListener { dialog.dismiss() }
    dialog.show()
}
```

---

## Recap / cheat sheet

- Toolbar "+" → custom email dialog → look up user.
