# Finishing Day 16

---

## Overview

Day 16 completes the **Music App** scaffold. The drawer, bottom bar, and bottom sheet now cooperate, and the `TopAppBar` title stays correct. Tea: not specified — the lecture focuses on verification and the upcoming **Firebase Chat App**.

---

## What you built and learned

- **BottomBar** (`BottomNavigation` + `BottomNavigationItem`).
- **BottomScreen routes** (`Home`/`Browse`/`Library` with `bRoute`/`bTitle`/`icon`).
- **Home sticky header**, **Browse grid**, **Library list**.
- **Selected-color fix** (`currentRoute == bRoute`, `selectedContentColor=White`).
- **ModalBottomSheetLayout** (`Hidden`/`Expanded`, `scope.launch show/hide`).
- **Scaffold actions** (⋮ → bottom sheet).
- **Title sync** (`LaunchedEffect(currentRoute)` → `viewModel.title`).

---

## Next

Day 17 — **Firebase Chat App** (project setup, Auth). The Music App's navigation/drawer/sheet patterns repeat in the chat app, so mastery here pays off.

---

## Recap / cheat sheet

- Full scaffold: `ModalBottomSheetLayout` → `ModalDrawer` → `Scaffold(topBar, bottomBar)` → `NavHost`.
- `ViewModel` title + `currentBackStackEntryAsState` = single source of truth for `TopAppBar`.

