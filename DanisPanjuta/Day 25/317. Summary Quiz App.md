# Summary Quiz App

---

## Overview

The Quiz App is complete. Suggested next step: make **your own quiz** with your own images and questions, publish it, and share the link for feedback.

---

## What you learned

| Area | Details |
| :--- | :--- |
| **Manifest** | Activities, launcher, themes (NoActionBar) |
| **Models** | `data class Question` |
| **Constants** | `object` with data provider + intent keys |
| **UI** | `CardView`, `TextInputLayout`, `ScrollView`, `ProgressBar`, custom shape drawables |
| **Interaction** | `View.OnClickListener`, `when (view.id)`, selection state |
| **Multiple activities** | `Intent`, `startActivity`, `finish()` |
| **Passing data** | `putExtra` / `getStringExtra` / `getIntExtra` |
| **Release** | Generating a signed APK |

---

## Recap / cheat sheet

- **Next:** Day 26 — the Kids Drawing App (custom views, Canvas, permissions, coroutines).
