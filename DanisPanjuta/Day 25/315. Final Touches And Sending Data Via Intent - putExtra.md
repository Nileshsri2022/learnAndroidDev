# Final Touches And Sending Data Via Intent - putExtra

---

## Overview

Passes the **user's name**, **number of correct answers** and **total questions** from screen to screen with **Intent extras**, using constant keys defined in `Constants`.

---

## Constant keys

```kotlin
object Constants {
    const val USER_NAME: String = "user_name"
    const val TOTAL_QUESTIONS: String = "total_questions"
    const val CORRECT_ANSWERS: String = "correct_answers"
    …
}
```

Using constants avoids typos between the sender and the receiver — the convention is **UPPER_SNAKE_CASE**.

---

## MainActivity → QuizQuestionsActivity

```kotlin
val intent = Intent(this, QuizQuestionsActivity::class.java)
intent.putExtra(Constants.USER_NAME, etName.text.toString())
startActivity(intent)
finish()
```

```kotlin
// QuizQuestionsActivity
private var mUserName: String? = null
mUserName = intent.getStringExtra(Constants.USER_NAME)
```

---

## QuizQuestionsActivity → ResultActivity

```kotlin
else -> {
    val intent = Intent(this, ResultActivity::class.java)
    intent.putExtra(Constants.USER_NAME, mUserName)
    intent.putExtra(Constants.CORRECT_ANSWERS, mCorrectAnswers)
    intent.putExtra(Constants.TOTAL_QUESTIONS, mQuestionsList?.size)
    startActivity(intent)
    finish()
}
```

---

## ResultActivity

```kotlin
override fun onCreate(savedInstanceState: Bundle?) {
    super.onCreate(savedInstanceState)
    setContentView(R.layout.activity_result)

    val tvName: TextView = findViewById(R.id.tv_name)
    val tvScore: TextView = findViewById(R.id.tv_score)
    val btnFinish: Button = findViewById(R.id.btn_finish)

    tvName.text = intent.getStringExtra(Constants.USER_NAME)

    val totalQuestions = intent.getIntExtra(Constants.TOTAL_QUESTIONS, 0)
    val correctAnswers = intent.getIntExtra(Constants.CORRECT_ANSWERS, 0)
    tvScore.text = "Your score is $correctAnswers out of $totalQuestions"

    btnFinish.setOnClickListener {
        startActivity(Intent(this, MainActivity::class.java))
    }
}
```

- `getIntExtra(key, defaultValue)` — a default is required for primitives.
- `finish()` removes the quiz from the back stack so Back doesn't return mid-quiz.

---

## Recap / cheat sheet

- **Send:** `intent.putExtra(KEY, value)`.
- **Receive:** `intent.getStringExtra(KEY)`, `getIntExtra(KEY, 0)`.
- **Keys** as `const val` in `Constants`.
